---
type: delivery
status: merged
env: taller
delivered:
tags: [reliability, roles, background-tasks, queue, outbox, prd, planning]
prs: [1944, 1945, 1946, 1947, 1965, 1969]
fe_prs: []
tickets: [23871, 23872, 23873, 23874, 23875, 23876, 23877, 23971, 23974]
prd: "https://app.notion.com/p/3abaedca11f081ba904afd1f73277444"
---

# Enhancement queue v2 Postgres-backed (PRD 7444)

> **Status 2026-08-03:** M0–M3 all MERGED to dev (#1944 / #1945 / #1946 / #1947, M3 merged 2026-08-03). Azure tasks 23872–23875 Closed; US 23871 Ready to Test (open for QA). Infra #10299 MERGED to master 2026-07-30 (worker live in dev, flag off). PRD 7444 synced. Pending before prod: M4 cutover (23876), M5 sweep (23877), Grafana dashboards/alerts **DONE 2026-08-03** (dashboard `enhance-role-worker-echo-backend` + 5 alert rules live), EQT remediation, 9-pending Loki forensics. **Prod-enable infra PR now tracked as Task 23971** (assigned to Gonzalo, under US 23871) — the item we forgot for the dispatcher last time. **QA on dev 2026-08-03**: queue verified end-to-end (201 + transactional enqueue + worker drain 50–60s + dedup 409s); one bug found & fixed — standalone create with no description → 500 (**Bug 23974 / [#1965](https://github.com/taller-projects/echo-backend/pull/1965) → dev, MERGED to dev `c3f48ff0` + RE-TESTED 2026-08-03 (no-JD create → 201, placeholder requirements=''); **Bug 23974 CLOSED****). **QA cont'd**: multi-tenant worker-isolation + `is_internal` serialization verified (seeded CC `member` row); dead-letter path verified (injected malformed/unknown-kind jobs → `dead`, no retry); retry-path fault injection: **#1968 CLOSED** (rejected — don't bake a raise-hook into `run_enhancement_pipeline`) → replaced by **[#1969](https://github.com/taller-projects/echo-backend/pull/1969) → dev** (independent **`PROJECT_TEAM_BUILDER_USE_MOCKSERVER`** toggle — reroutes ONLY the team-builder integration to MockServer, per-integration; first cut used a shared `MOCKSERVER_ENABLED` gate which coupled it to TrackerRMS — user flagged it, redesigned to a dedicated flag) + infra **[ttit #10479](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10479) → master** (MockServer expectations: JD 500 on a `MOCKFAIL` sentinel + a default 200 stub JD; separate from `tracker-rms-*`). See "Testing non-happy paths via MockServer" below. **Fairness (CORRECTED — not a bug)**: the per-tenant cap is **fleet-wide** (claim SQL counts running jobs per tenant across all pods); default 8 = `ceil(2-pod prod fleet 16 / 2)`, correct for the intended 2-pod prod sizing. Dev runs **1** worker pod (fleet 8 = cap 8), so contention fairness isn't observable in dev — lower the cap in dev Vault or run 2 pods to test it.

## Testing non-happy paths via MockServer (retry → dead-letter)

> **CONFIRMED LIVE 2026-08-03**: both PRs merged; dev Vault set `PROJECT_TEAM_BUILDER_USE_MOCKSERVER=true` + `MOCKSERVER_URL=http://ttit-echo-backend-dev-mockserver:1080`. A `MOCKFAIL`-named role hit the mock 500 → worker retried with growing backoff (attempts 1→2→3, `last_error`=ExternalApiException 500) → converging to `dead` + role `FAILED`; a control role enhanced via the literal `[MOCK]` stub JD (selective failure + happy-path-via-mock both work). **Gotcha: BOTH vars required** — flag alone no-ops (guard is `and MOCKSERVER_URL`).

Requires: echo-backend **#1969** merged + deployed to dev; infra **ttit #10479** merged (MockServer expectations); both worker flags on (`ENHANCEMENT_QUEUE_ENQUEUE_ENABLED` + `ENHANCEMENT_WORKER_ENABLED`).

1. dev Vault: set **`PROJECT_TEAM_BUILDER_USE_MOCKSERVER=true`** (+ `MOCKSERVER_URL` = the dev mockserver). This reroutes ONLY team-builder (JD generation) to MockServer; TrackerRMS etc. stay on real endpoints (independent toggles).
2. **Failure path:** create a role whose **name contains `MOCKFAIL`** → the JD call hits MockServer's 500 (`team-builder-job-description-mockfail-500`) → `run_enhancement_pipeline` raises → worker retries on backoff `[30,120,600]` (~12 min) → job `dead` + role `FAILED`. Assert in dev DB: `background_job.status='dead'`, `last_error` set, `role.enhancement_stage='failed'`, `attempts` climbed.
3. **Recover:** rename the role to drop `MOCKFAIL` (or clear the flag) + `POST /projects/{p}/roles/{r}/retry-enhancement` → hits the 200 stub → `enhanced`.
4. **Happy path via mock:** a normal role → MockServer's default 200 stub JD (`team-builder-job-description-success`) → enhancement proceeds through the real downstream services → `enhanced` (no real team-builder AI).
5. Turn `PROJECT_TEAM_BUILDER_USE_MOCKSERVER` off to restore the real team-builder.

**Independence:** TrackerRMS is mocked separately (point `TRACKER_RMS_API_URL` at the mockserver); the two toggles are independent, so one integration can be on mock while the other hits real services. Expectations live in `mockserver-configmap.yaml` (`team-builder-*` ids, kept separate from `tracker-rms-*`); Stakater Reloader (`reloader.stakater.com/auto`) auto-restarts MockServer on ConfigMap change. JD endpoint = `POST /job_descriptions/job_descriptions_v2`, happy shape `{"job_descriptions":[{"job_description":"…"}]}`.

Move the role **AI enhancement pipeline** (2-5 min per role) out of FastAPI `BackgroundTasks` into a **persistent job queue in Postgres**, drained by a **dedicated worker pod** that clones the operational skeleton of the already-in-prod outbox dispatcher (`FOR UPDATE SKIP LOCKED` claim, lease + expiry, backoff retries, dead-letter, Prometheus metrics, Vault flag, same image / own entrypoint, own Helm+ArgoCD block). This is the **v2 iteration of Pedro's May PRD** and its one substantive revision is the **broker decision**: Dramatiq + managed Redis → **Postgres queue, outbox-style**. **PRD Draft as of 2026-07-28: no code, no Azure tickets.**

## Azure / docs

- PRD v2 (Tier C, Draft, owner Gonzalo): [Cola off-request para enhancements — v2 (Postgres-backed) — PRD Técnico](https://app.notion.com/p/3abaedca11f081ba904afd1f73277444)
- PRD v1 (Pedro Rocha, 2026-05-11, Draft): [Cola de tareas off-request para enhancements de larga duración](https://app.notion.com/p/35daedca11f081d988fbeefa56bb11b6) — lives in the "Echo roadmap (test)" DB. **v2 inherits its diagnosis, prod evidence, technical criteria and milestone shape and does not re-argue them.**
- Sibling PRD: [[Create Role from Open Job (PRD 887e)]] — new creation path feeding the same enhancement; absorbed at cutover, no contract change.
- Business PRD: N/A (pure engineering — reliability/scalability).

## The argument (why Postgres, not Redis)

**Inherited from v1**: `BackgroundTasks` is awaited inside the request task, so a uvicorn worker is tied up 2-5 min per enhancement; under ~50 req/min bursts in-flight concurrency explodes (~250) and saturates the threadpool (40) + pooler. Prod evidence 2026-05-11: 297 `POST /internal/roles`, 100% >30s, max 5m09s, 8-9 5xx/min.

**What changed since May** (the three facts that flip the decision):
1. **The outbox + dispatcher pattern now exists and runs in prod** (since July): separate pod `python -m app.dispatcher`, claim with `FOR UPDATE SKIP LOCKED`, lease TTL + expired-lease release, exponential backoff + jitter, dead-letter, Prometheus, Sentry `component=dispatcher`, Vault flag, own Helm/ArgoCD block. In May "custom PG queue" was dismissed as "reinventing Procrastinate" — it has since been invented, operated and runbooked here.
2. **Same incident class recurred**: [[Role enhance pool timeout unhandled (Bug 23808)]] — the enhancement background task died on a `QueuePool` checkout timeout (30s, pool 20+10) during prod pool-exhaustion windows, roles stuck `PENDING`. Contained, but proof the current mechanism won't survive growth (projection: 5k users, 100-500 concurrent creations).
3. **Durability became an explicit requirement**: no pending enhancement may be lost across deploys/downtime.

**Core argument**: the enqueue INSERT rides **the same transaction that commits the role**, so a role can never be committed without its job — crash, deploy or broker outage. Persisted Redis (Azure Cache Premium RDB/AOF or Azure Managed Redis) protects the *broker* but does **not** close the dual-write window between commit and enqueue; closing it needs an outbox in Postgres anyway — and if the outbox is needed regardless, the PG queue is already paid for. Verified: **zero Redis anywhere in echo-backend's repo or charts today** (Airflow/anvil have their own).

**Three sub-decisions answered**: new jobs table (**not** `integration_outbox` — different semantics: work queue vs integration events; sharing couples retention/metrics/purge and drags the TrackerRMS dispatcher into a foreign domain); **new worker** (the dispatcher is sequential-per-batch and runs as a fixed `BYPASSRLS` role — the worker needs bounded concurrency and per-job multi-tenant context reconstruction); **Redis discarded for v1**.

**Re-evaluation trigger toward Redis** (a commitment, not a wish): sustained `oldest_pending_age > 10 min` for >30 min with the worker pool at the max agreed with infra, or the poll/claim showing up in PG's top load → v3 with Dramatiq + Redis **using this same table as the enqueue outbox** (nothing built is thrown away). Procrastinate: dropped in v2 (its marginal value collapsed once the in-house pattern shipped). Azure Service Bus / Storage Queues: same dual-write problem. No-queue mitigations (in-flight cap + 503 + `Retry-After`, v1's M6): parachute only.

**Capacity math**: enqueue is a trivial INSERT; drainage with `C` concurrent jobs/pod at 2-5 min ⇒ ~`C × 12-30` jobs/h/pod (2 pods × 8 ⇒ ~190-480/h), horizontally linear. The real ceiling becomes the AI providers' rate limits — which is exactly why worker concurrency must be **bounded and configurable** (today concurrency = incoming traffic, unbounded).

## Scope + schema

**IN**: new jobs table + migration; new worker deployment (`echo-backend-worker` [tentative], same image, Vault flag); migrate role enhancement on **`POST /roles`, `POST /internal/roles`, `POST /projects/{id}/roles` (public + internal), `POST /roles/{id}/retry-enhancement`**; move the standalone-without-JD **synchronous** JD generation into the job; tenant context serialized in the job and rebuilt in the worker (RLS intact); idempotency by role state + retries + dead-letter + alert; queue metrics/logs/dashboard/alerts; flag `USE_QUEUE_FOR_ENHANCEMENT` [tentative, inherited name] with runtime rollback to `BackgroundTasks`; final sweep of heavy `background_tasks.add_task` sites.

**OUT**: optimizing the pipeline's internals; fanning the internal `WorkerGroup` out into separate jobs (worker v2); replacing EventBus; touching `integration_outbox`/the dispatcher; client-driven idempotency keys; small (<10s) background tasks; existing schedulers (the stale-role cleanup stays as a backstop); **Kforce** (the outbox pattern doesn't exist on `kforce-dev` → copy+adapt with its own PRD).

**Table** [tentative name `background_job`]: `id`, `kind` (e.g. `role_enhancement`), `payload` JSONB (ids + serialized context), `tenant_id`, `status` (`pending`/`running`/`completed`/`failed`/`dead`), `attempts`, `last_error`, `available_at`, `locked_at`, `locked_by`, `completed_at`, timestamps; partial dispatch index on pending by `available_at` (mirroring `ix_outbox_dispatch`); optional dedup by `(kind, entity)` while active [dev's call in M1]. `integration_outbox*` untouched. One alembic revision, starts empty, rollback = drop table (runtime rollback = flag).

## Criteria (v1's, kept as contract, + new durability ones)

- `201 < 500ms p95` on the three creation endpoints under 100 req/min × 5 min (k6).
- **Zero jobs lost** on deploy or crash: `kill -9` mid-job → re-executed after the lease expires; rollout with N queued → queue drains fully on return. Both are chaos-test criteria.
- Zero `SSL connection has been closed unexpectedly` from role/project service during burst; ≥99% enhancements `ENHANCED` in <10 min.
- Retries 3× with backoff [tentative 30s / 2min / 10min] → `FAILED` + alert. **At-least-once**, idempotent by terminal role state (stages are overwrite-safe).
- A dead worker releases its jobs within the lease TTL; a live job never loses its lease (renewable lease or TTL > p99 — **the dispatcher's 60s TTL explicitly does NOT apply** to 2-5 min jobs).
- Logs per stage (`enhance.start` … `enhance.complete`/`failed`) with `role_id`/`tenant_id`/`job_id`/`attempt`/`stage_duration_ms`; Prometheus queue depth, `oldest_pending_age`, in-flight, error rate, p50/95/99 per stage; Grafana panel beside the dispatcher's.

**Estimate**: 8-13 dev days + 8-12 h QA + ~1 DevOps day (v1 was 9-15 + 1-2 infra days — the broker provisioning that blocked v1's M0 disappears). Milestones: **M0** table + worker skeleton (1-2 d, *no longer infra-blocked*) → **M1** roles through the queue (2-3 d) → **M2** worker DB hygiene + lease calibration (1 d) → **M3** observability (1 d) → **M4** cutover dev→qa(24h)→prod behind the flag (0.5-1 d) → **M5** sweep of heavy background tasks (3-5 d).

## Code-verified facts (checked on `dev` @ `b9465656`, 2026-07-28)

- Dispatcher config to mirror (and the numbers **not** to copy) — `app/core/config.py:255-270`: `OUTBOX_DISPATCHER_ENABLED=False`, `POLL_SECONDS=2`, `BATCH_SIZE=50`, **`LEASE_TTL_SECONDS=60`**, `DB_ROLE="echo_dispatcher"`, `DELIVERY_TIMEOUT=30`/`CONNECT_TIMEOUT=10`, `MAX_ATTEMPTS_5XX=10`, `MAX_ATTEMPTS_409=20`, `SKIP_WRITEBACK=True`, `OUTBOX_RETENTION_DAYS=7`, `RETENTION_PURGE_INTERVAL_SECONDS=3600`, `OUTBOX_METRICS_PORT=8001`. The PRD's "7-day retention, same mechanism as the dispatcher" and "60s TTL does not apply" both trace to real values here. Entry point: `app/dispatcher.py` + `app/modules/outbox/dispatcher.py`.
- **Zero Redis** in `app/` or `pyproject.toml` (only false positives in bundled font licenses) — the "first Redis piece in the stack" cost is real.
- The 5 enhancement call sites: public `POST /projects/{id}/roles` `app/modules/role/routers.py:91` (also enqueues `_reload_solution_with_context`), internal twin `internal_routers.py:38`, public standalone `POST /roles` `routers.py:355`, internal standalone `internal_routers.py:170`, retry `routers.py:276` — all going through `_enhance_role_with_context` → `enhance_role(role_id)`.
- In-request **synchronous** JD generation confirmed: `ProjectService.create_role_with_placeholder_project` calls `regenerate_job_description` inline when the payload has no JD — `app/modules/project/service.py:747-748`. This is the one externally visible change and the Changelog entry the PRD promises.
- `RoleService.enhance_role` (`app/modules/role/service.py:449`) is already **never-raises** with best-effort `FAILED` marking (from [[Role enhance pool timeout unhandled (Bug 23808)]]), and `RoleCleanupSchedulerService` sweeps stale `pending`/`enhancing` at 15 min → the double safety net the PRD keeps during the transition.
- M2's "sessions released around external calls (today partial)" = the two explicit `self.repo.db_session.close()` calls at `service.py:485` and `:501`, before the JD call and before the parallel `WorkerGroup`.
- M5 sweep candidates, confirmed in code: `role_service.update_vector` (`role/routers.py:272`, `role/internal_routers.py:114` and `:225`), `talent_service.update_talent_vector_by_id` (`talent/routers.py`, `talent/internal_routers.py`, 3 sites in `talent/experience/routers.py`), `wg.compute_vector` in `organization/job/service.py`, and solution regeneration (`_reload_solution_with_context`, `project/service.py`'s `generate_solution` path).

## Pedro's review — 9 gaps, now a section inside the PRD (2026-07-28)

Signed review against `dev` `b9465656`. **He endorses the approach** ("bien sostenido") and explicitly verified OK: the outbox/dispatcher pattern as described, zero Redis, the sequential+`BYPASSRLS` dispatcher argument for a separate worker, and the in-request sync JD generation. Bonus: **the FE risk can be closed now, not in M1** — `useRoleEnhancementPolling` polls every 60s with no timeout while the stage is `pending|enhancing`, and `shouldShowEnhancementLoader` covers "no JD yet", so deferring the standalone JD to the worker is FE-safe. Everything below I re-verified in the repo — **all of it holds**.

**Blockers**

1. **The cleanup scheduler kills queued jobs → enhancements lost in silence.** `role_cleanup_scheduler_service.py:88-99` runs every 5 min and blind-`UPDATE`s `role SET enhancement_stage='failed' WHERE stage IN ('pending','enhancing') AND updated_at < now() - 15 min`. A job that waits >15 min in queue — exactly what a queue *does* under burst — leaves `updated_at` frozen, gets swept to `failed`, and then the PRD's own idempotency rule ("terminal → skip + ack") makes the worker discard it. No error anywhere. It also races the lease: with a TTL "> p99" (~8-10 min) a post-kill re-execution starts near the sweeper's threshold. The "double safety net" framing in the Anexo is wrong — it's an active conflict. Also: skip-on-terminal must distinguish swept-`failed` from retries-exhausted-`failed`.
2. **`≥99% ENHANCED < 10 min` is arithmetically incompatible with this PRD's own capacity math.** The committed load test (100 req/min × 5 min = 500 jobs) against the proposed sizing (2 pods × 8 = 16 in-flight, 2-5 min/job ⇒ ~4.5 jobs/min) drains in **~110 min**, p99 ≈ 108. Hitting 99% <10 min would need ~150-175 concurrent jobs — past the AI providers' rate limit the PRD itself names as the ceiling. The criterion was inherited from v1, where concurrency *was* incoming traffic; v2 deliberately trades queue latency for stability and must say so. Split the SLO into (a) 201 p95 <500ms, (b) time-to-start in steady state, (c) "the queue drains in X" under burst. **Corollary: open question 3 (sizing) can't be answered before this — sizing is a consequence of the SLO.**
3. **"Serialize the context" is not enough — `is_internal` is a correctness guard, not tenancy metadata.** `outbox/writer.py:25-35` never emits when `is_internal`, precisely to avoid a TrackerRMS sync loop, and both internal creation endpoints set it via `mark_sync_context` (`role/internal_routers.py:131`). If the job doesn't persist and restore `is_internal`/`is_sync_operation`/`authenticated`, an enhancement originating in `/internal/roles` **will emit outbox from the worker** — the exact loop of [[Outbox skips internal-originated emits (Bug 23656)]]. Worse, RLS reads `auth.jwt() ->> 'sub'` and sessions are built from `req_ctx.jwt` via `set_config('request.jwt.claims', …)` (`database/base.py:348-364`), so "rebuild the context" means **storing JWT claims in the jobs table**: PII/quasi-credential material in `payload` JSONB with 7-day retention, plus expiry (a 10-min retry runs on a dead JWT; a deactivated user keeps "acting"). Precedent for the alternative exists: `candidate_email_worker_service.py:299-302` synthesizes `RequestContext(authenticated=False)` + explicit tenant scoping. **Decision: (A) impersonate the creator vs (B) synthetic context — he recommends B**, and asks the PRD to enumerate the context fields verbatim instead of saying "multi-tenant context".

**Important**

4. **Zero multi-tenant fairness.** FIFO on `available_at` + bounded concurrency ⇒ one tenant importing 300 roles parks every other tenant's single manual role behind them. Needs a `priority` column, round-robin claim by tenant, or "global FIFO accepted" as a conscious decision. This — not aggregate throughput — is what users perceive (ties into #2).
5. **Dedup can't stay "optional".** With at-least-once + leases on 2-5 min jobs, *concurrent* double execution of the same role is expected. "Stages are overwrite-safe" (inherited from v1) is a property of *sequential* re-execution: `create_match_applications` is an upsert and tolerates overlap, but `evaluate_application_batch` and `update_screening_questions` re-run whole → double AI spend and results stomping each other. Commit to a partial unique index on active jobs by `(kind, entity_id)`, and define `retry-enhancement`'s semantics (409 if a job is active, or enqueue anyway?).
6. **Connection budget unquantified.** The pipeline doesn't use one connection per job: `enhance_role` spawns a `WorkerGroup` and each thread gets its own `DatabaseResource` ⇒ ~3 connections/job ⇒ ~24/pod ⇒ ~48 new Supavisor clients at 2 pods. With `DB_USE_NULLPOOL` (prod via pooler 6543) worker concurrency **is** the connection limit — a DevOps sizing item missing from dependencies. Minor: the PRD quotes prod pool as 20+10; repo defaults are 30+100 (`core/config.py:53-54`) — say those are Vault values.
7. **No deploy shutdown policy, only a chaos test.** With 2-5 min jobs and the default `terminationGracePeriodSeconds` (30s), **every deploy kills all in-flight jobs**, which then wait out the lease TTL. The dispatcher can cut between events; this worker can't. Commit to grace ≥ p99 + a preStop that stops claiming and drains, or accept re-execution per deploy — in which case #5 becomes critical.
8. **It's 5 enqueue sites, not 4 — plus a sixth path M4 doesn't cover.** Real sites: `role/routers.py:112`, `:290`, `:374`, `role/internal_routers.py:55`, `:181`. The missing amplifier is `project/service.py:491`: `generate_team` fans `enhance_role` out over every role of the project in a `WorkerGroup(max_threads=5)` **and joins in-request**. The PRD pushes it to M5, but M4's "Done" declares §3's criteria met in prod — with that path still in-request, "zero SSL-closed" isn't covered for `POST /projects`. Structural point: today the enhance decision lives in the **router** (`add_task`) while `_create(enhance_role=False)` disables it in the service; a transactional enqueue must live in the service (like `write_outbox`) → the PRD should mandate **one single decision point**, or the flag ends up split across two owners.
9. **Flag rollback strands queued jobs.** If the flag also stops the worker (as `OUTBOX_DISPATCHER_ENABLED` stops the dispatcher, `app/dispatcher.py:28-30`), queued jobs sit there and the sweeper marks their roles `failed`. Needs two flags or drain-then-disable: the flag cuts *enqueue*, the worker keeps draining.

**Minor**: a **third queue pattern already exists** in the repo and the PRD doesn't mention it — `CandidateEmailWorkerService` (APScheduler inside the web pods, advisory lock, atomic claim, stale reaper); it's the precedent for both the synthetic context and the reaper, and today it's hidden under "OUT: migrate existing schedulers" — say whether it converges into `background_job`. • `OUTBOX_METRICS_PORT=8001` is taken — the worker needs its own port + ServiceMonitor. • The metric that governs the *user-visible* SLO is missing: **queue wait / time-to-start** (`oldest_pending_age` alone can't tell "long job" from "clogged queue"); add an `attempts` histogram too. • Schema needs `updated_at`/heartbeat if the lease is renewable (only `locked_at` today), and must state whether `payload` holds JWT claims (changes the table's data classification and retention). • `tests/system/test_outbox_atomicity.py` already exists as the template for M1's atomicity test.

## Resolutions — folded into the PRD 2026-07-28 (status → "Draft, revisión de Pedro resuelta, listo para `Approved`")

- **1 Sweeper** → the cleanup `UPDATE` gains `AND NOT EXISTS (… background_job … status IN ('pending','running'))`, **and** the worker's skip rule changes: skip only on `ENHANCED` or exhausted attempts; a `FAILED` role *with an active job* is re-driven. Lands in **M1**, not M4. During the transition the sweeper only covers roles that never went through the queue.
- **2 SLO split into four**: (a) 201 p95 <500ms; (b) steady state — time-to-start p95 <60s **and** the inherited ≥99% `ENHANCED` <10 min, which is where it was always achievable; (c) under the 500-job burst — full drain, zero losses, **gate ≤120 min** at the initial sizing; (d) ≥99% of jobs reach a terminal state without manual intervention. **Closes open question 3 by dependency**: sizing is derived, starts at 2×8, recalibrated in week 2.
- **3 Context → option B, synthetic, zero JWT at rest.** The payload carries exactly `tenant_id`, `user_id` (attribution only), `is_internal`, `is_sync_operation`, `authenticated=False`, trace id. Worker uses its own DB role + per-job `SET LOCAL request.tenant_id`. Consequences declared: the jobs table holds no PII (7-day retention needs no special treatment), matching stops varying with the creator's data scope (a fix, cf. 23594), and `is_internal` is mandatory or the TrackerRMS loop reopens — dedicated M1 regression test with `tests/system/test_outbox_loop.py` as template.
- **4 Fairness** → `priority` smallint + claim on `(priority, available_at)` **plus a per-tenant in-flight cap** (default `ceil(concurrency/2)`), which is what actually stops head-of-line blocking. No round-robin in v1.
- **5 Dedup committed**: partial unique index `(kind, entity_id) WHERE status IN ('pending','running')`; `retry-enhancement` → 409 when a job is already active.
- **6 Connections**: formula `pool = concurrency × conns_per_job + headroom`, `conns_per_job` measured in M2 (expected ~3), initial budget ~48 Supavisor clients, DevOps confirms **before M4**. 20+10 clarified as Vault values vs repo defaults 30+100.
- **7 Shutdown**: grace ≥ p99 (**proposed 600s**) + preStop that stops claiming and drains; lease-expiry re-execution accepted as fallback *because* item 5 makes it safe.
- **8 Five enqueue sites + `generate_team`, which moves M5 → M1** (M4's gate is unfalsifiable for `POST /projects` otherwise), and the enqueue decision moves into `RoleService` — routers stop calling `add_task`, `_create(enhance_role=…)` becomes the single switch, and PRA's endpoint enters through the same point.
- **9 Two flags**: `ENHANCEMENT_QUEUE_ENQUEUE_ENABLED` + `ENHANCEMENT_WORKER_ENABLED`, drain-then-disable; turning both off at once is forbidden in the runbook. Replaces the single inherited `USE_QUEUE_FOR_ENHANCEMENT`.
- **Minors adopted**: `CandidateEmailWorkerService` cited as the third precedent (does *not* converge in v1); own metrics port + ServiceMonitor; `queue_wait_seconds` + `attempts` histograms; `heartbeat_at` + `priority` on the table, payload explicitly JWT-free; `test_outbox_atomicity.py` as the M1 template; FE risk closed now instead of validated in M1.
- **Estimate 8-13 → 10-15 days.** Open questions: 2 closed (dedup index suffices, no client keys), 3 closed as a starting value, 4 closed at 7 days, 5 closed (alert on `queue_wait_seconds` p95 >5 min sustained 15 min; Redis trigger unchanged); 1 (failed-UX) and 6 (kforce) stay with product/FE and a separate PRD.

## Gotchas

- The worker **cannot** reuse the dispatcher's `BYPASSRLS` role posture: enhancement runs tenant-scoped code, so the job must carry the serialized `RequestContext` and the worker must `SET LOCAL request.tenant_id` per job. This is the PRD's risk #1 and the one an implementer is most likely to get subtly wrong — see the `background_request_context` gotcha recorded in [[Outbox skips internal-originated emits (Bug 23656)]] (`TenantScopedRepository` only auto-injects the tenant inside a request context; otherwise INSERTs fall back to the hardcoded Taller `server_default`).
- Chaos criteria (`kill -9`, rollout with a loaded queue) live in `tests/system` — **CI runs `tests/unit` only**, so they will never gate a PR. Plan them as manual/scripted verification.
- The alembic revision must use a real random id, and heads must be checked with `alembic heads` (not regex) — repo rules.

## Production evidence — 2026-07-29 incident (added to the PRD by Pedro)

One user's bulk-load script (tenant EQT, Python-urllib, **220 roles**, 16:30–18:25 UTC) broke prod at current volume — no 5k users needed: each `POST /roles` held ~3 connections for ~50s (max 109s), the pod pool (Vault 20+10, 30s timeout) exhausted, and **every endpoint 500'd for every tenant** 17:05–18:10 (Sentry [ECHO-BACKEND-BR](https://taller-wn.sentry.io/issues/7640609250/), release `6959c12`). Outcome on data: 158 `enhanced` / 53 `failed` / **9 `pending` stuck hours later** — 28% silently lost (Bug 23808 class). Pedro mapped it item-by-item against the resolved design: **fully covered, no failure mode outside it**, and turned it into the M4 acceptance gate (incident replay in QA: ~220 concurrent single-tenant POSTs; zero QueuePool timeouts; other-tenant p95 ≤ baseline; cross-tenant time-to-start <60s; 100% legitimate-terminal). New pending it added: explain the 9 unswept `pending` roles before cutover (a sweeper hole is also a transition-backstop hole), plus EQT data remediation via existing retry at low concurrency.

**Lead hypothesis for the 9 unswept (mine, code-read, unverified)**: `RoleCleanupSchedulerService._cleanup_stale_enhancements` skips the run whenever the session-level advisory lock isn't acquired — a pod session that held the lock and hung (without dying) during pool exhaustion silences every sweep across all pods for as long as that connection lives. Loki check: `"Role enhancement cleanup completed"` vs `"Another instance is already running role cleanup, skipping"`, 2026-07-29 17:00–24:00 UTC. Also: the sweep itself needs a pool checkout, so it fails *during* the exhaustion window by design — the anomaly is only that it never caught up after.

## Implementation — kicked off 2026-07-30 (PRD → In Progress)

- **Azure**: US [23871](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23871) + tasks 23872 (M0) → 23877 (M5), one per milestone, PRD linked.
- **M0 delivered**: [#1944](https://github.com/taller-projects/echo-backend/pull/1944) → dev (branch `23872/background-job-worker-skeleton`, commit `e1884805`). Migration `n3vw8kq2r7pd`: `background_job` + partial dispatch index `(priority, available_at) WHERE pending` + **partial unique `(kind, entity_id)` on active** + `echo_worker` role (NOLOGIN BYPASSRLS, mirrors `echo_dispatcher`). `app/modules/background_job/` (models / writer / repository / worker / metrics) + `app/worker.py` behind `ENHANCEMENT_WORKER_ENABLED` + docker-compose `worker` + 25 unit tests (CI-gated, in `tests/unit/background_job/`).
- **Design decisions locked in M0** (dev-level, PRD delegates them): `attempts` increments **at claim**, so kill-loop poison jobs converge to `failed` via `release_expired_leases` (CASE on `attempts >= max`) instead of looping; lease renewable via `heartbeat_at` renewed every poll (TTL 180s bounds worker death, not job duration); `priority` ASC = lower value first (bulk enqueues higher values); purge deletes **completed only** (failed/dead stay visible, dispatcher precedent); worker metrics port **8002** (8001 = dispatcher); retry schedule list-driven (`[30,120,600]`, exhausted → failed) with no separate MAX_ATTEMPTS knob to drift.
- **PRD deltas folded in**: 6th enqueue site `organization/job/routers.py:113` (PR #1933 shipped post-review) added to M1's checklist; all line numbers re-pinned at `dev@bcd2c6b5`.
- **M0 rubric review round 1 (2026-07-30, /pr-review, 3 parallel reviewers) → all findings fixed same-day in `bfd90b34`**. Verdict was CHANGES REQUESTED but narrowly: the only blocker was **test-only** — the retry-budget boundary (`attempts == len(backoff) == 3`) was unpinned in both paths, so a `<=`→`<` mutation survived the suite; now pinned (worker last-retry schedules +600s; lease-expiry at attempts=3 → `pending` not `failed`) plus the worker-level lease-lost branch. Nits fixed in the same commit: `_maybe_purge`/`_refresh_gauges` failure-isolated like the dispatcher (an unguarded stats/purge error aborted `_poll` → backoff → heartbeats stalled → healthy leases expired → duplicate execution — the arch reviewer's best catch); `processed_total` gained `completed`/`lease_lost` outcomes and a lease-lost attempt no longer lands in the `completed` duration bucket; `_in_flight: dict[uuid.UUID, Future]` + `log: structlog.stdlib.BoundLogger` (repo's configured wrapper_class, `app/core/logging.py:52`); `purge_all_jobs` conftest helper removed the claim-order coupling between sibling tests (28 tests now, were 25). **Ripple: the stacked children must rebase over `bfd90b34` — #1947 (M3) also rewrites `_execute`/`_observe`, expect conflicts there.**
- **Pedro's review (comment 5131325371, 2026-07-30 13:17 UTC, ran against `fadbcded` — pre-`bfd90b34`, so half his nits were already fixed) → addressed in `f00a6aa8`**. His verdict CHANGES REQUESTED, 27/28 PRD-compliant, 0 scope creep. **His blocker was real and mine missed it**: `queue_depth` gauge never clears drained `(kind,status)` buckets — `get_queue_stats()` only returns groups with rows *now*, so a drained bucket froze at its last non-zero value forever (permanent false "queue non-empty" for M3 alerts); dispatcher precedent `pending_count._metrics.clear()` at `outbox/dispatcher.py:345`. It survived my review because every worker test ran `metrics=None` — **lesson: reviewers must ask "which code paths do the tests instantiate collaborators for?"**. Fixed: clear-before-repopulate + `TestGauges` drain-the-bucket test via `registry.get_sample_value` (claim moves the bucket pending→running, pending label pair must vanish). Also taken from his nits: `fail_job`/`mark_dead` bools no longer discarded (`_note_lease_lost` helper — no more logging transitions that didn't happen), JWT-free contract restated in `enqueue_job` docstring, his 3 still-missing tests (SIGTERM drain within grace via threading.Timer+asyncio.run, `_reap_finished` executor-crash branch, late heartbeat on released lease). **33 tests now.** Declined with reason: job-retry jitter (he himself says fine as-is given dedup index), `status` CHECK constraint (he marks optional, outbox-consistent). His 4 Questions **ANSWERED on the PR 2026-07-30 ([comment](https://github.com/taller-projects/echo-backend/pull/1944#issuecomment-5132553098))** + Azure task texts updated: Q1 tenant cap → implemented in #1945, added to task 23873; Q2 attempts histogram → implemented in #1947, noted on task **23875** (M3 — an earlier note here said 23876, wrong); Q3 idle pod never opens :8002 → **verified #10299 defines NO probes** (dispatcher template shape, containerPort only) so the dark pod stays Running and the PodMonitor target just shows down; metrics-in-idle (start_http_server before the flag check) offered as follow-up, Pedro's call whether it rides #1945 or the cutover; Q4 executor `shutdown(wait=False, cancel_futures=True)` post-drain-timeout = intentional (grace 570 < termination 600; running threads can't be cancelled; abandoned jobs may finish in the 30s window, else lease-reclaim + dedup cover re-execution). **Round 2 verdict: APPROVED — mergeable once the Sonar-503 CI re-run finishes.** Review QUESTIONS deliberately left open (author's call, not addressed): job-retry jitter omitted vs PRD's "backoff + jitter" wording; tenant-keyed RLS on `background_job` when M1 adds request-session writes; deleted-entity semantics for M1 handlers (`entity_id` has no FK); idle-mode never starts the metrics server → #10299 probes/PodMonitor must tolerate a dark pod not listening on 8002.
- **M1 delivered (2026-07-30)**: [#1945](https://github.com/taller-projects/echo-backend/pull/1945) **DRAFT, stacked on #1944** (base = the M0 branch so the diff is M1-only; rebase onto dev + retarget after #1944's squash-merge — user's stacked-PR directive). Single decision point in `RoleService._create`/`request_enhancement` (routers pass `background_tasks` through, none call `add_task` for enhance anymore, `generate_team` enqueues at `BULK_PRIORITY`); transactional enqueue via `super().create(commit=False)` + job add + one commit (placeholder project rides too); synthetic context in `background_job/context.py` (taxative: user_id, is_internal, is_sync_operation, trace_id = `req_or_thread_id()`); pipeline split `run_enhancement_pipeline` (raises, worker entry) vs `enhance_role` (never-raises, Bug 23808 contract); handler re-drives FAILED-with-active-job and marks role failed on exhaustion; sweeper `NOT EXISTS` exclusion; `retry-enhancement` → 409 `enhancement_in_progress` checked BEFORE the stage reset; per-tenant in-flight cap in the claim SQL (`BACKGROUND_WORKER_TENANT_IN_FLIGHT_CAP`, default 8); worker builds the full `DependencyInjector` + `apply_bindings()` (EventBus runs; schedulers deliberately not started).
- **M2 delivered (2026-07-30)**: [#1946](https://github.com/taller-projects/echo-backend/pull/1946) **DRAFT, stacked on #1945** — sessions released around every external call (`update_vector`/`_generate_skills`/`_generate_matching_instructions` now materialize scalars + `close()` before their LLM round-trips, mirroring the pipeline's existing JD-call pattern; loaded scalars survive `close()`, only `commit()` expires); worker sizes its own engine (`BACKGROUND_WORKER_DB_POOL_SIZE=28` = 8×3+4, review item 6 formula) and `DependencyInjector` stamps `application_name` per process (`echo-backend-worker`) so the Supavisor budget is a `pg_stat_activity` filter; `tests/system/test_worker_lease_chaos.py` pins dead-worker re-execution (attempts=2 via claim-time increment) and live-lease-never-stolen. Real pod kill -9 stays an M4 runbook step (CI never runs tests/system).
- **M3 delivered (2026-07-30)**: [#1947](https://github.com/taller-projects/echo-backend/pull/1947) **(→ dev, IN REVIEW since 2026-07-31 — was draft stacked on #1946)** — per-stage structured logs from `RoleService` (`enhance.start` → `enhance.stage` ×5 → `enhance.complete`/`failed`, fire in both worker and legacy paths); `structlog.contextvars.merge_contextvars` added to the processor chain (was absent!) + handler binds job_id/tenant_id/attempt/trace_id so pipeline logs carry job identity; `stage_duration_seconds{stage}` + `attempts{kind,outcome}` histograms via a **ContextVar observer** (`stage_observation`) the worker installs per job — RoleService reports through `observe_stage()` which no-ops in the web process (no dependency on the worker's metrics instance). Contextvars do NOT cross into the pipeline's internal WorkerGroup threads (match/screening detail stays in track_time logs). Grafana dashboard/alerts = ops step blocked on Grafana access; spec in the PR body.
- **M1 open-questions audit (2026-07-30, user-requested) → found + fixed a real cross-tenant leak** (`a0f7e3b4` on #1945): `retry_enhancement`/`request_enhancement` consulted `has_active_job` (UNSCOPED — the dedup index has no tenant column) *before* the tenant-scoped role fetch → a foreign role id with an active job answered **409 before 404**, leaking existence+enhancing state across tenants (pre-M1 retry 404'd first). Fix: `get_by_id` first in both paths + regression test `test_foreign_or_missing_role_is_404_before_the_409`. RLS-on-`background_job` decision documented on the PR ([comment](https://github.com/taller-projects/echo-backend/pull/1945#issuecomment-5132983930)): **no policies in v1** — `integration_outbox` precedent, JWT/PII-free payload, worker = BYPASSRLS `echo_worker`, web access always behind tenant-resolving services; additive migration if M4 pentest asks. Deleted-entity semantics confirmed pinned (handler acks missing role, dedup frees at terminal). **Lesson: `has_active_job`-style unscoped lookups must never run before the tenant-scoped fetch on request paths.**
- **M1 gotchas** (cost a debugging cycle): (1) the flag-off fallback with `background_tasks=None` must SKIP, not run `enhance_role` synchronously — pre-queue semantics were "creation never enhances in-service" and 11 tests (open-job unit + standalone-creation) broke against baseline until fixed. (2) `background_request_context` deliberately resets `is_sync_operation` on fork — the worker handler restores it from the payload AFTER entering the context. (3) `test_roles.py` standalone shows 42 pre-existing failures (fixture/order-dependent 404 pattern) vs 0 in the full suite — only full-suite diffs against baseline are meaningful. (4) claim SQL now reads `BACKGROUND_WORKER_TENANT_IN_FLIGHT_CAP` — any settings-patching test fixture must include it or psycopg2 chokes on a MagicMock bind.

## M1 review rounds (#1945) — 2026-07-30

- **Round 1 (own rubric review, pre-undraft)**: CHANGES REQUESTED — 2 test-only blockers ((a) flag-on `_create` atomicity untested, (b) PRD item-3 is_internal outbox-loop regression missing) + ~10 nits. All addressed in `a3281ff0`: TestCreateFlagOnAtomicity (real `_create` + standalone, rollback leaves no role/project/job), TestWorkerOutboxSuppression (is_internal=True -> 0 outbox rows, False control -> 1), ACTIVE_JOB_STATUSES + is_final_attempt/max_executions centralized in background_job/models.py, JobContextPayload pydantic both sides, request_enhancement honest return + role= param, IntegrityError-backstop / generate_team-branch / attempts=3 / legacy-None-skip tests. Full unit+mt failure set diffed vs TRUE stash baseline: byte-identical (30). The 409-before-ownership leak was fixed separately in `a0f7e3b4` (parallel session).
- **Leo's review (comment 5133234973, at `a0f7e3b4`)**: 5 blockers / 5 nits. B3 (atomicity tests) + B4 (outbox-loop test) + nits 1-2 (retry arithmetic, sweeper literals) already covered by `a3281ff0` (crossed in flight). Still open after that commit: **B1 generate_team sets TEAM at enqueue -> FE useProjectGenerationPolling stops polling (verified in FE hook: polling only while GENERATING)**; **B2 tenant-cap correlated COUNT has no index (his EXPLAIN: 476ms/3k pending -> queue stall at ~130k; partial index (tenant_id) WHERE status='running' -> 2.9ms)**; **B5 worker tenant-isolation tests (tests/multitenancy) — worker runs authenticated=False -> admin BYPASSRLS role, isolation rests on TenantScopedRepository which silently drops the filter w/o context**; nit 3 ENHANCED-guard belongs in pipeline (legacy path unguarded); nit 4 N+1 partially fixed (role= param); nit 5 (_create commit outside handle_commit_errors) — declining w/ reasoning: fresh UUID can't collide on the dedup index at commit.
- **Leo round DELIVERED in `bdc611c5`**: B1 = worker-side completion signal — `on_terminal` hook on RoleEnhancementJobHandler fired by `_notify_terminal` strictly AFTER complete_job/terminal fail_job (fired from __call__ the drain check would see its own still-running job and never complete — the design gotcha of this round); `ProjectService.complete_generation_if_team_done` is job-based (not stage-based: FAILED-with-active-redrive-job must keep project GENERATING) via `has_active_job_for_any`; generate_team leaves GENERATING, zero-roles case completes immediately. Residual matching legacy: job reaped without any handler run leaves project GENERATING (legacy pod-crash did too). B2 = `ix_background_job_running_tenant` partial index, migration `w7qk2mv9x4tp` (plain CREATE, table dark until M4 — no prod rows). B5 = tests/multitenancy/test_worker_tenant_isolation.py (real handler + real DI, both directions, pins SET LOCAL request.tenant_id + TenantScopedRepository filter; role stage PINNED pending — polyfactory would randomize to ENHANCED and skip the probe). Nit 3 = ENHANCED guard inside run_enhancement_pipeline (handler keeps fast-ack). Nit 5 DECLINED w/ reasoning: commit-time IntegrityError on a fresh role UUID unreachable (dedup index can't collide; role constraint violations surface at flush inside decorated save). Worker hook tests incl. ordering contract (hook sees status=completed). Verification: 76/76 background_job+isolation, full unit+mt failure set byte-identical to baseline (30), ruff clean, single alembic head. PR reply to Leo pending.

- **Leo round 2 (comment 5135457513, at `bdc611c5`)**: 4/5 round-1 blockers closed (mutation-verified by him); 3 NEW blockers all in the on_terminal mechanism I introduced: R2-B1 terminal signal lost on 5 paths (reaper SQL, lease-lost, hook crash, malformed payload, deleted role) with NO project backstop -> stuck GENERATING forever; R2-B2 per-role commits let a stray hook TEAM a half-built fan-out; R2-B3 drain check loaded 85 cols incl vector per finishing job. He warned round 3 = protocol iteration limit; asked to pin the design in the PRD first.
- **Round 2 DELIVERED in `aa69178b`** (design = edge fast path + level convergence, pinned in PRD "Resoluciones round 2" section): payload carries project_id (is_internal now REQUIRED - False is the unsafe default); on_terminal parses defensively + delegates to RoleService.on_enhancement_settled (kills project<->background_job import cycle); reaper RETURNING -> poll fires hooks for handlerless terminal rows; ValidationError -> NonRetryableJobError -> mark_dead + hook; backstop sweep _complete_stale_generating_projects rides RoleCleanupScheduler (<=2 cycles convergence); request_bulk_enhancement = 1 tx + savepoints; drain condition stage-aware AND job-aware (closes the pre-existing-job residual race deterministically); get_enhancement_snapshot projection; Q1 resolved by GATING the ENHANCED guard behind the flag (req 20 verbatim again); Q3 = PRD declares bulk=generate_team only; Q2 = RLS premise corrected in PRD (execution runs as admin role, echo_worker only claims); application_name nit answered by stacked #1946. TerminalAwareHandler Protocol, repo.commit() under handle_commit_errors, fairness purge, real-row exhaustion assert, index-drift DDL test. 97 queue tests + isolation, full suite byte-identical to baseline, ruff clean. GOTCHA: test helper `payload or {default}` swallowed the explicit-{} malformed case - use `if payload is not None`. PR reply to Leo pending; dev E2E (Q4) still undemonstrated (needs flag on in dev post-merge).
- **Leo round 3 ([comment](https://github.com/taller-projects/echo-backend/pull/1945#issuecomment-5136205962), at `aa69178b`)**: 3/3 round-2 blockers closed (mutation-verified by him, 9/12 mutations caught), but CHANGES REQUESTED again — 4 new blockers, all in the round-2 machinery: R3-B1 the backstop wasn't gated by `ENHANCEMENT_QUEUE_ENQUEUE_ENABLED` (violates R2-4 — flag-off must be pre-queue dev verbatim; worse, it TEAMs projects whose roles the sweep just failed, and `generating→team` is irreversible with no lever short of killing the whole role sweep); R3-B2 `complete_generation_if_team_done` stamped TEAM unguarded over the stage it read (could resurrect a concurrent FAILED/DISABLED); R3-B3 mutation M9 — `get_enhancement_snapshot`'s tenant filter is the ONLY isolation on that path (worker session bypasses RLS) and no test pinned it; R3-B4 mutation M3 — deleting the backstop call site left 97 tests green (tests drove the private method directly).
- **Round 3 DELIVERED in `00132dde`**: B1 = `if settings.ENHANCEMENT_QUEUE_ENQUEUE_ENABLED` around `_complete_stale_generating_projects` + R2-4 rationale in docstring (gate chosen over declaring flag-independence, so PRD Changelog row 2 stays accurate — NO PRD edit needed); B2 = new `ProjectRepository.complete_generation_if_still_generating()` — single tenant-scoped `UPDATE … WHERE generation_stage='generating'` returning won-bool, replaces `repo.update` in the drain path (also kills the 26-col re-select perf leg); B3 = `test_enhancement_snapshot_of_a_foreign_project_is_empty` in tests/multitenancy (real handler context; membership assert because `mocked_project` is module-scoped); B4 = scheduler-entrypoint convergence test (1 cycle, `queue_on`) + flag-off-dark companion via module-level `run_cleanup_scheduler()` helper; + `TestGuardedCompletionWrite` real-row pair pinning the B2 guard. All 4 mutations re-applied locally and confirmed killed. 185 passed (background_job + multitenancy), ruff clean. GOTCHA: when renaming mocked `repo.update` asserts, `TestGenerateTeamQueueBranch` must KEEP `repo.update.assert_not_called()` — that's generate_team's own stamp mechanism, not the drain path. Still open from the round: Leo's pre-merge prod row measurement (no prod creds in `.env`; query = stale GENERATING projects with no pending/enhancing roles), PR replies to Leo (rounds 1-3) unposted, dev E2E evidence (req 23, third round). Round 4 = verification only.
- **Round 3 nits DELIVERED in `11c2b679` (2026-07-31)** — all "Nuevos" items from Leo's round-3 comment: dead code from the R2-B2 fix removed (`request_enhancement` lost `priority`/`on_active_job`/bool return + `OnActiveJob` alias; single 409 semantic, commit-race translated via `DuplicateError.constraint == ACTIVE_ENTITY_UNIQUE_INDEX` → now a real 409, fixing the 400 nit for the reachable path); drain-rule stage literals bound from new `role/schemas.py::IN_FLIGHT_ENHANCEMENT_STAGES` + `ProjectGenerationStage` params in both scheduler UPDATEs; scheduler docstring covers the project backstop; reap bounded per poll (`release_expired_leases(limit=BATCH_SIZE)` + SKIP LOCKED subselect — the self-amplifying-reap nit) with warning on handlerless terminal rows; `_notify_terminal` checks `callable(on_terminal)` (runtime_checkable isinstance only checks presence); savepoint plumbing moved to `writer.enqueue_job_if_absent` + batch pre-check `active_job_entity_ids` (kills the per-role has_active_job N+1) + bulk closes via `repo.commit()`; `CrossTenantRoleError` + struct-log error replaces the bare ValueError; `OrganizationJobSQLRepository.commit()` shadow deleted; `from_db_string` cross-table branch (no model misattribution, no index-name leak to FE toast); `parse_job_payload_or_default` salvages `project_id` (`_salvage_uuid`) — the R2-B1 path-4∩5 intersection; dead-letter reason uses `exc.errors(include_input=False)` (no payload values in `last_error`); `enqueue_job` payload typed (`JobContextPayload`, serialization inside); mutation M8 (requeued ∉ terminal, both boundary tests) + M12 (two fresh roles pin the single commit) + reap-limit test; TestGenerateTeamQueueBranch / TestOnEnhancementSettled / TestProjectCompletionSignal rewritten against real rows (real DI services + `_create_project_at`/`_create_role_at` helpers); perf: `ProjectRepository.get_generation_stage` scalar early-exit, `generate_team` queue branch returns None (callers discard), partial index `ix_project_generating_updated_at` (migration `tpls3jaidxpa`); circular-dep TODOs reworded as why-comments; cap-overshoot comment states the cross-pod READ COMMITTED caveat. **Declined/deferred with reasoning**: `application_name` + worker pool → stacked #1946 (M2, verified there); `SessionLivenessEngine` binding → lazy engine, worker never resolves it, zero connections (candidate cleanup for #1946); last_error retention for failed/dead rows → M0-locked operator-visibility decision (dispatcher precedent), write-site sanitized instead; 400→409 for `_create`'s dedup collision → unreachable (fresh UUID), but constraint-based translation now exists if it ever becomes reachable. PR body rewritten (Pregunta 2). **Leo round 4 ([comment 5144388726](https://github.com/taller-projects/echo-backend/pull/1945#issuecomment-5144388726), at `11c2b679`): READY WITH NITS, ZERO blockers** — 4/4 round-3 blockers closed and mutation-verified live (M3/M8/M9/M12 all caught), 20✅/1⚠️/2🚫, zero scope creep; the only ⚠️ is req-23 dev E2E evidence (process, not code). **Round-4 carryover DELIVERED in `1846e0c2` (2026-07-31, user-directed)**: `DependencyInjector` gains `application_name` (default `echo-backend`; worker passes `echo-backend-worker`) plumbed into `create_sqlalchemy_engine` — implemented VERBATIM like stacked #1946's version so the rebase collapses (its pool-size change stays M2); `SessionLivenessEngine` binding made LAZY (zero-arg callable provider + singleton scope — injector CallableProvider) so the worker process never constructs the web-only liveness pool (only `SessionValidationService` resolves it); pinned by `TestWorkerProcessIdentity` (engine stamps the process application_name; liveness factory not called until first `injector.get`; worker entrypoint wiring test patching the module). GOTCHA: don't patch `deps.EventBus` around `apply_bindings` — the mock becomes the BINDING KEY and injector rejects it; EventBus.__init__ doesn't start its thread, so the real one is test-safe. Also fixed own round-4-flagged typo (duplicated assert in `test_or_default_parser_drops_an_unreadable_project_id` → second now pins `is_internal`). Round-4 nits left OPEN (small, user's call): migration docstring misjustifies plain CREATE INDEX (partial build still seq-scans under SHARE lock — argument wrong, outcome fine); `enqueue_job_if_absent` catches IntegrityError broadly (FK violation reads as slot-taken); `TerminalAwareHandler` Protocol now doc-only; failed/dead `last_error` retention hygiene. PRD housekeeping asks: 409-with-job-state promise vs static message + Changelog row; R2-1 reaper fast-path oversell + dead-letter as undeclared third terminal outcome; zero-roles stale project converging to team deserves a PRD line. GOTCHA: `_service_with_session` now builds a bare `RoleRepository.__new__` (not SimpleNamespace) so `repo.commit()` keeps `handle_commit_errors` translation — the 409 race test depends on it. Suite: 105 tests/unit/background_job + 2 isolation; full unit+mt failure set byte-identical to stash baseline; ruff clean; head `tpls3jaidxpa`.

## M2 review round (#1946) — 2026-07-31

- **Leo round 1 ([comment 5145795201](https://github.com/taller-projects/echo-backend/pull/1946#issuecomment-5145795201), at `1567a67b`)**: CHANGES REQUESTED — 2 blockers, zero scope creep (compliance matrix 6✅/4⚠️/2❌). B1 = `_generate_skills` was the only session-close site the suite couldn't make fail (its lone test builds the service via `__new__` + MagicMocks, so a DetachedInstanceError or tenant regression in the post-close `repo.update` passes green). B2 = the worker inherited the web's generic 5-min `idle_in_transaction_session_timeout` through the shared `after_begin` listener — the PRD prod-evidence section explicitly forbids exactly that, M2's Hecho is "no idle-in-transaction >30s", and no other milestone owns it. Also ~7 nits, 4 questions (lease numbers vs ticket text, chaos-split declaration, burst-test evidence, conns-per-job measurement landing spot), 3 PRD corrections — deliberately NOT addressed this round (user scoped the session to blockers).
- **Blockers fixed in `c16aeb31` (2026-07-31, pushed)**. B1 → `test_generate_skills_persists_after_session_release` (tests/unit/test_roles.py) mirrors `test_generate_matching_instructions_persists`: real injector service, only `MatchingDiffService.get_technologies_input` patched (class-level `patch.object`; the service is bound REAL in the test container, no Mocked* swap exists), `skills_required`/`skills_nice_to_have` pinned EMPTY (factory randomizes them; any content early-returns before the close-then-update under test), both fields asserted persisted via a fresh session. B2 → new `BACKGROUND_WORKER_IDLE_IN_TRANSACTION_TIMEOUT_MS` (default 30000 = the M2 bound) plumbed with the `application_name` mechanics: `worker.py` → `DependencyInjector(idle_in_transaction_timeout_ms=…)` → `DatabaseResource` binding (zero-arg callable provider, same idiom as the lazy liveness engine) → the listener binds `self.idle_in_transaction_timeout_ms` instead of the global setting; web passes nothing and keeps 300000 via the def-time param default (`admin_db_role` idiom). Extension beyond Leo's letter: `BackgroundJobRepository._session` (claim/heartbeat/reap — worker-only, constructed solely in `worker.py`) also moved off the generic value; without it "the worker doesn't inherit" held only for handler sessions.
- **5 pins, one per link**: `TestWorkerIdleInTransactionBound` in test_background_job_worker.py (entrypoint kwarg wiring; DI-plumb via the now module-level `_bound_injector`; live-GUC probe `SELECT setting FROM pg_settings` on a real transaction = the bound; web-session contrast keeps the generic default) + `TestSessionIdleBound` in test_background_job_repository.py (claim-session GUC = worker bound). **All 6 mutants killed locally** (B1 update-skip; listener reverts to global; apply_bindings drops the plumb; entrypoint drops the kwarg; claim path reverts).
- **GOTCHAS**: (1) restoring a mutation with `git checkout <file>` DESTROYS unstaged fixes in the same file — restore by reverse-replace, never checkout, when mutation-testing uncommitted work; (2) zsh kills `echo ===` via equals-expansion (`=cmd` lookup) — it aborted the mutation batch mid-script, which is exactly how gotcha 1 fired on `database/base.py`; (3) the new repo setting must ride EVERY dict that patches `background_job.repository.settings` (conftest `_REPO_SETTINGS`, `TestFairnessClaim`'s inline mock, chaos `_SETTINGS`) or psycopg2 chokes on a MagicMock bind (known M1 gotcha, new key).
- Verification: background_job + multitenancy 198 passed + 1 xpassed; lease chaos 2/2 (local Docker); ruff clean; **full unit+mt suite FULLY GREEN post-change: 3270 passed, 1 xfailed, 0 failures (4m57s)** — no failure-set diff needed, and this also closes the "baseline diff still running at undraft" pending from the update ritual. Round 2 = verification only. PR reply draft prepared in-session, NOT posted (rounds 1-4 replies on #1945 also still unposted). Leo's nits/questions/PRD corrections remain open, author's call.
- **All 7 round-1 nits DELIVERED in `6abd5189` (2026-07-31, pushed)**: (1) worker pool now a HARD cap — `max_overflow: int | None = None` plumbed `DependencyInjector` → `create_sqlalchemy_engine`, `worker.py` pins `0` (28 was really 28+100 under QueuePool); config comment corrected; pinned at all 3 links (`TestWorkerPoolHardCap`: entrypoint kwarg / container plumb / `engine.pool._max_overflow == 0` with `DB_USE_NULLPOOL` patched False) + existing exact-kwargs `assert_any_call` in `TestWorkerProcessIdentity` updated with `max_overflow=None`. (2) `create_match_applications` loop: reads role + instructions, then `release_db_session()` before each `evaluate_application_batch` dispatch — NOTE the call is `fire_and_forget` (thread), so Leo's "N blocking round-trips" premise is off; the real leak was the loop's read transaction idling in the match thread after the last iteration. Safe because `bulk_create` commits before the loop and `expire_on_commit` never fired post-refresh: building `unique_roles` reloads every returned Application, so `app.id`/`app.talent_id` survive detachment for `expand_matches_for_role`'s response. (3) release pattern dedup'd → `BaseService.release_db_session()` (why lives once, greppable), 5 close-sites in `RoleService` + the new application site converted; the `close()+remove()` pre-wg.join site left as-is. (4) `_generate_skills` except path returns `None` (was leaking the detached `role`), typed `Role | None`. (5) `update_vector` docstring pins the "call only with a clean session" contract + dead `return` dropped to honor `-> None`. (6) new mt pin `test_tenant_context_survives_mid_pipeline_session_release`: probe does `close()` → GUC re-read → scoped-read filter → tenant-scoped `repo.update` asserted persisted from a fresh session. (7) chaos hygiene: `_insert_job`/`_job_row` → unit conftest's `insert_job`/`fetch_job` (cross-suite conftest import precedent already exists in tests/unit/background_job), `_worker` contextmanager shuts the executor (`shutdown(wait=True)` — was leaking 2 threads/test).
- **Verification of the nits round**: targeted 144 passed (background_job + mt isolation + chaos 2/2 + industry-propagation + matching-diff); ruff clean. Full unit+mt: **30 failed / 3244 passed — vs TRUE baseline run of pristine `c16aeb31` via `git archive` tree (no git writes): 30 failed / 3240 passed, failure sets BYTE-IDENTICAL** (all 30 = test_application_comments/owner_snapshot/process_status_filter TestClient-404 flake; +4 passed = exactly the new tests). Lesson: the local suite is BIMODAL — the c16aeb31 run was fully green, this one hit the 30-flake mode on both sides; only same-session A/B diffs mean anything. `git archive HEAD | tar -x` + symlinked `.venv` + copied `.env` = stash-free baseline (no git write needed). Still open: Leo's 4 questions + 3 PRD corrections (author's call), PR replies unposted.
- **Conflict vs dev resolved in merge `e0bb6bf8` (2026-07-31, pushed; PR back to MERGEABLE)**: Pedro shipped **[#1953](https://github.com/taller-projects/echo-backend/pull/1953) (`4b3d9c73`) as an urgent prod mitigation** — the SAME three session-release hunks #1946 owned (his PR body: byte-identical to blob `5a3e902c` = #1946's pre-nits state, "whichever merges second rebases clean") + a regression test `test_enhance_role_releases_db_session_during_external_calls` (probes `in_transaction()` at the 4 main-thread external boundaries, counterfactually verified; WorkerGroup tasks unprobed — fresh threads can't inherit a tx). The clean-rebase assumption broke because `6abd5189` (nits) rewrote those hunks into `release_db_session()`. Resolution: 3 conflicts in `role/service.py`, HEAD kept in all (helper is the strict superset; post-merge `git diff HEAD -- role/service.py` = 0 lines); `test_roles.py` auto-merged (union: his test + ours — his passes unchanged against the helper since it probes at the service boundary, implementation-agnostic); #1958 (touchpoints/future_interaction) auto-merged. Note: #1953's incident attribution says the dominant idle-in-tx sampled holder may be ELSEWHERE (`SELECT role.…`-last vs these tenant-select-last sites) — forensic follow-up + controlled dev burst = M2's Hecho gate, now shared context. Verified: his test + our persists pin 2/2; module+mt-worker+chaos suites green (20 standalone-mt TestClient-404 flakes = the documented count, zero in worker/queue files); full-suite A/B vs pristine origin/dev CONFIRMED post-push: merged 30 failed/3246 passed vs dev 30/3236, failure sets BYTE-IDENTICAL (same 30 TestClient-404 flakes; +10 = #1946's own tests), merge regression-free.

## M3 review round (#1947) — 2026-07-31

- **Round 1 = double review, same head `7be17d0f`**: own rubric review (/pr-review, 3 parallel reviewers) → READY WITH NITS, zero code blockers; **Leo's round 1 (PR comment 2026-07-31 20:44 UTC) → APPROVED, READY WITH NITS** — the two reviews converged on the same finding set independently (dead-path attempts coverage, contextvars-reset pin, type annotations; both also flagged the reaped-terminal attempts gap and the worker-configure_logging observation as questions, not code changes). Compliance: all code-side M3 requirements ✅ (Sentry `component=worker` verified pre-shipped in M0 — `app/worker.py:26`); the ❌s are the Grafana dashboard/alerts + burst done-gate, deferred on access, plus ServiceMonitor riding infra #10299. Zero scope creep; `run_enhancement_pipeline` rewrap verified instrumentation-only (statement order + M2 session-release points byte-identical vs origin/dev).
- **All nits (mine + Leo's, union) DELIVERED in `8bfe30c2` (2026-07-31, pushed)**: (1) types — parameterized `_stage_observer: ContextVar[Callable[[str,float],None]|None]`, `stage_observation(observer) -> Iterator[None]`, handler `_run(ctx: RequestContext, log: structlog.stdlib.BoundLogger)`, `_enhancement_stage -> Iterator[None]`; (2) **`tenant_id` now on every `enhance.*` event** (was only on `enhance.start` in the legacy BackgroundTasks path where no job contextvars are bound) — captured once as str before the try, threaded as a new `_enhancement_stage(role_id, tenant_id, stage)` param; worker path duplicates the contextvar with the same value, harmless; (3) dead-path attempts tests — `NonRetryableJobError` and unknown-kind both assert `background_job_attempts_count{outcome="dead"} == 1` against a real row (the existing dead tests run `metrics=None`, so those observe sites had zero coverage); (4) contextvars reset pinned in `TestRoleEnhancementHandler`: raising job then clean second job on the same thread, `get_contextvars() == {}` after each; (5) **`enhance.*` log contract pinned** (`TestEnhanceLogContract`): full success sequence start → 5 named stages → complete with field names via `structlog.testing.capture_logs`, `run_enhancement_pipeline` driven end-to-end by patching `ProjectTimelineRequest`/`TeamBuilderRole` (the two real-validation schemas) + MagicMock collaborators on a `__new__` service — a rename now fails CI, not the dashboards; failed path pins `enhance.start → enhance.failed` + re-raise; (6) style — `ClaimedJob` import at module top, retry-budget comment on `attempts=4`, `_running_job`/`_claimed` helpers dedup the row setup. Suite: 127 background_job (was 122, +5) + 66 queue file + 3 mt worker-isolation, ruff clean.
- **Left open deliberately (author's call, not code)**: Leo's Q1 legacy-path `job_id`/`attempt` semantics (impossible without a job; tenant_id half now closed by nit 2), Q2 error-rate as `processed_total` derivation vs dedicated metric, Q3 reaped-terminal jobs skipping the attempts histogram (both reviews; suggest help-string tweak if intentional), Q4 flag-off now emitting `enhance.start` vs PRD item 9 "verbatim" (log-only, needs a ruling); the worker never runs `configure_logging()` (M0-wide property, dispatcher identical — worker logs render via structlog defaults incl. `merge_contextvars` by luck of structlog 25.4 defaults, ConsoleRenderer not JSON; candidate one-liner in `app/worker.py::main`, check Loki parsing first); milestone-closure decision on the Grafana deferral (don't close Task 23875 on merge).

## Bug 23974 (#1965) — QA-found standalone-create 500, review round (2026-08-03)

- **The bug** (pre-existing, found during dev QA of the queue): `POST /roles` / `POST /internal/roles` with no `job_description` and no `short_description` → `create_role_with_placeholder_project` built `ProjectCreate(requirements=None)` (`short_description or job_description` with both falsy) → pydantic `ValidationError` on the required-`str` field → unhandled 500. Blocked the PRD's headline path (create without JD, worker generates). Fix = coalesce `or ""` at `app/modules/project/service.py:869` ([#1965](https://github.com/taller-projects/echo-backend/pull/1965) → dev, [Bug 23974](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23974)).
- **Own /pr-review round 1 (3 reviewers): READY WITH NITS** — zero blockers (arch 11/0, tests-security 7/0, ticket 5/5 applicable). Both nits fixed same day: (1) regression test parametrized over BOTH pre-fix-broken combos — `(None, None)` and whitespace-only `short_description` (stripped to `""` by `str_strip_whitespace`, and `"" or None` is still `None` → same 500) — commit `093a396b`; (2) PR body corrected — it claimed the create-from-open-job path also 500ed, but that path always passes `requirements=job.description` explicitly and `OrganizationJob.description` is a non-nullable `str` (`default=""`), so it was never affected.
- **Layer decision worth remembering**: fix belongs in the service's DTO construction, NOT in relaxing `ProjectCreate.requirements` to accept `None` — that schema is the live request body for `POST /projects` and the internal create, so tolerating `None → ""` there would silently downgrade a client 422 to acceptance on two public endpoints.
- **Verified downstream**: `""` requirements validate on both flag states (`ProjectTimelineRequest.requirements: str`, no `min_length`) — `None` would have crashed one layer later anyway; generating a JD from empty requirements is exactly the PRD's intended behavior.
- **Kforce sibling bug found during review**: `origin/kforce-dev:app/modules/project/service.py:446` carries the identical pre-fix expression (no `or ""`). If kforce exposes standalone create, it 500s the same way → needs its own Bug ticket + copy+adapt PR to `kforce-dev` (do NOT cherry-pick; the method diverged).

## Prod worker Deployment (Task 23971 / ttit [#10519](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10519)) — 2026-08-04

- **What**: add a `worker:` block to `applications/echo-backend/values-prod.yaml`, mirroring the dev worker (`worker-deployment.yaml` / `worker-podmonitor.yaml` templates from #10299). Structural diff vs dev worker block = **exactly two fields**: `replicaCount` 1→**2** and `releaseLabel` `ttit-prome-thanos-nonprod`→**`ttit-prome-thanos-prod`**; resources / ports 8002 / 600s grace / podMonitor identical. Branch `23971/deploy-enhancement-worker-prod`, commit `72e6b1bae`. PR linked to Task 23971 via ArtifactLink.
- **Why safe to merge ahead of the M4 cutover** (the whole point of splitting infra from the flag flip): `app/worker.py` idles in a `sleep` loop while `ENHANCEMENT_WORKER_ENABLED` is unset/false — **before** it builds the DI container or SQLAlchemy engine, so an idle pod opens **zero** pooler connections and starts no metrics server. The worker Deployment has **no HTTP probes** (checked the template), so an idle pod can't CrashLoop on a missing readiness endpoint. `ENHANCEMENT_QUEUE_ENQUEUE_ENABLED` also defaults false → prod web keeps enhancing in-request (legacy path), nothing enqueued. **No Vault flags are set in this PR.** Caveat: prod ArgoCD auto-syncs from HEAD, so merging rolls the *idle* pod to prod immediately (that is the deploy).
- **Sizing rationale for replicaCount 2**: `BACKGROUND_WORKER_CONCURRENCY=8`/pod → 2 pods = 16 concurrent slots; the per-tenant in-flight cap of 8 (`BACKGROUND_WORKER_TENANT_IN_FLIGHT_CAP`) is `ceil(fleet/2)`, so one tenant can't starve the fleet. With 1 pod the cap equals the whole fleet and fairness is unobservable (the exact dev limitation). Idle pods cost nothing, so shipping 2 now makes M4 a **pure Vault flag flip** — no infra change at cutover.
- **QA/staging decision — do NOT deploy the worker there**: `values-qa.yaml` and `values-staging.yaml` have **no `dispatcher:` and no `worker:` block** — those envs run the web app only (deployment / ingress / HPA / PDB / secrets), confirmed live via the `taller-qa` and `taller-staging` ArgoCD apps. Background processes (dispatcher, and now worker) run **only in dev + prod** in this repo. Adding a worker to QA would break that pattern and nobody is enabling the queue in QA (flags default off, legacy path stays). So this is a **prod-only** change; the earlier "do we need QA too?" resolved to **no**.
- **M4 pre-checks before flipping the flags** (tracked on US 23871, out of scope of this PR): verify the prod Supabase/Supavisor pooler has headroom for `2 × BACKGROUND_WORKER_DB_POOL_SIZE (28) = 56` worker connections; set a Grafana contact point + unpause `enhrole-worker-down`. Recommended flag order at cutover: `ENHANCEMENT_WORKER_ENABLED` on first (worker still idle w/ nothing to claim), then `ENHANCEMENT_QUEUE_ENQUEUE_ENABLED` on. Rollback = enqueue off (legacy path resumes instantly; drain the queue then worker off).

## Pending

- **Infra PR [#10299](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10299) → master MERGED 2026-07-30** (worker Deployment mirroring #9139 + PodMonitor port 8002 mirroring #9174, nil-safe guards from the start, grace 600s values-overridable, values-dev only — qa/prod render nothing; prod enable at M4 like #9981). ~~Merge prerequisite: Vault flag first~~ **Resolved 2026-07-30 (user call): `fadbcded` on the M0 branch makes the disabled worker IDLE (sleep loop) instead of exiting** — a missing/false `ENHANCEMENT_WORKER_ENABLED` in Vault is now harmless (no CrashLoop), so #10299 can merge without touching Vault; the flag is flipped on when we want the worker live. SIGTERM drain is in the app, so no preStop hook needed. Note the repo auto-syncs from HEAD via ArgoCD: merging deploys. Only dev merges now; prod stays untouched until M4.
- **Grafana observability DELIVERED 2026-08-03**: access restored — reads via `GRAFANA_API_KEY` (viewer SA token), writes via an Editor SA token `GRAFANA_EDITOR_TOKEN` in `~/.zshrc` (non-interactive shell does NOT source `~/.zshrc`; read it from the file). Dashboard **`enhance-role-worker-echo-backend`** ([link](https://grafana.taller.ai/d/enhance-role-worker-echo-backend), ECHO folder, 25 panels, mirror of the dispatcher) + **5 alert rules** (folder `alertas`, group `enhance-role-worker`, 60s eval, `X-Disable-Provenance` so UI-editable; `enhrole-worker-down` PAUSED until cutover; no contact point yet → default policy) applied via the provisioning API. All show **No Data** until the dev worker runs. Grafana 11.1 gotchas: expr node needs `datasourceUid:"-100"`; bulk rule-group PUT 500s (POST rules individually, set interval via group PUT without rules); no UI file-import for alerts.
- **9-pending Loki forensics**: now UNBLOCKED (Grafana/Loki reachable again). Still TODO — check `"Role enhancement cleanup completed"` vs `"Another instance is already running role cleanup, skipping"` for 2026-07-29 17:00–24:00 UTC. Gates M4, not M1.
- **#1944 MERGED to dev 2026-07-30 (merge commit `87f4823f` — NOT squash, so the M0 branch history incl. `bfd90b34`/`f00a6aa8` is in dev's ancestry)**. GitHub auto-retargeted #1945 to dev on branch delete. **#1945 updated via `git merge origin/dev` (clean, zero conflicts — no force-push needed thanks to the merge-commit strategy)**, title/body refreshed via `gh api` REST (`gh pr edit` still broken by Projects Classic), 51/51 module tests post-merge; undraft gated on the full-suite baseline diff. Azure: 23872 → Developed, 23873 → In revision. **#1945 MERGED to dev 2026-07-31 (merge commit `7a7337fd` — again NOT squash, M1 history in dev's ancestry). #1946 update ritual DONE same day: GitHub auto-retargeted it to dev; `git merge origin/dev` into `23874/worker-db-hygiene` (merge commit `1567a67b`) — merge-base was the OLD M1 tip `c7d2d313` (in dev's ancestry), so only M2's own 5-file commit reconciled; ONE conflict (`app/worker.py` pool_size line — kept M2's `BACKGROUND_WORKER_DB_POOL_SIZE`, the application_name kwarg was identical on both sides), `app/dependencies.py` collapsed out of the diff entirely (M1's `1846e0c2` copied #1946's application_name verbatim — the planned payoff). Effective M2 diff = 4 files (config setting, role/service session releases, worker pool_size, chaos tests). Title de-stacked + body refreshed via gh api (notes application_name half landed in M1), **UNDRAFTED — ready for review**. Verified: 110/110 module+isolation post-merge, 2/2 lease chaos (system, local Docker), ruff clean; full-suite baseline diff was still running at undraft (user-directed push) — confirm when it lands. Next: same ritual for #1947 (M3) once #1946 merges (it also rewrites `_execute`/`_observe` — expect real conflicts there), and #10299 can merge in parallel.**
- **#1946 MERGED to dev 2026-07-31 (merge commit `59d1023c`, NOT squash). #1947 (M3) update ritual DONE same day: GitHub auto-retargeted to dev; `git merge origin/dev` into `23875/worker-observability` (merge commit `7be17d0f`) — merge-base `54511c2a` predates ALL post-stack M1/M2 fixes, so the predicted real conflicts landed: 3 files.** Resolution shape: `handlers.py` — dev's `parse_job_payload` dead-letter flow kept in `__call__`, M3's `__call__`/`_run` split + contextvars binding kept on top, `data: JobContextPayload` threaded into `_run` (auto-merge had left `data.is_sync_operation` referenced out of scope — semantic, not textual, repair); `worker.py` — `stage_observation` wraps `handler(job)` inside dev's rewritten `_execute`; `attempts` histogram now observes ALL terminal outcomes **including the `NonRetryableJobError` dead-letter path M3 never saw** (consistency call, mirrored on the no-handler dead path); dropped M3's pre-lease-check completed-observe (dev's `_note_lease_lost` restructure is strictly better); `role/service.py` — `enhance.*` stages wrapped around dev's final pipeline (ENHANCED guard BEFORE `enhance.start` — a skip is not a run; `release_db_session()` positions preserved verbatim). One dev-side test fixed: `test_flag_off_pipeline_re_runs_an_enhanced_role_verbatim`'s SimpleNamespace role gained `tenant_id` ([[reference_dunder_new_test_fixtures]] pattern — `enhance.start` reads `role.tenant_id` before the `repo.update` sentinel). Title de-stacked + body refreshed via `gh api` REST (added "Post-merge reconciliation" section), **UNDRAFTED — ready for review, MERGEABLE**. Verified: 122/122 `tests/unit/background_job`; full unit+mt A/B gate PASSED — merged 30f/3252p vs pristine-dev git-archive baseline 30f/3246p, failure sets byte-identical (the known `test_application_comments`/`owner_snapshot`/`process_status_filter` TestClient-404 trio), +6 passed = exactly M3's 6 new observability tests; ruff clean; single head `tpls3jaidxpa`. PR diff vs dev = M3's 6 files + the 2-line fixture fix, nothing reverted.
- EQT remediation (53 failed + 9 pending, re-drive at low concurrency) — coordinate before the queue exists.
- DevOps items to confirm before M4: Supavisor connection budget (~48 clients at 2×8), `terminationGracePeriodSeconds` 600s + preStop drain, worker Deployment + own metrics port/ServiceMonitor, per-env Vault flags (now two).
- Azure Feature/US + milestone tasks (nothing created yet). M1 is now bigger: enqueue + sweeper coordination + dedup index + `generate_team`.
- Still with product/FE: failed-enhancement UX. Still a separate PRD: the Kforce backport (copy+adapt).
- **Kforce sibling of Bug 23974**: same `requirements=None` 500 latent at `origin/kforce-dev:app/modules/project/service.py:446` — file a kforce Bug + copy+adapt PR (see the Bug 23974 section).
- DevOps dependency: new worker Deployment in Helm/ArgoCD + per-env Vault flag/secrets. **No new data infra** — the difference that unblocks v1's M0.
- Confirm with the FE that `useRoleEnhancementPolling` needs no change (expected: none; validate in M1).

## Related

- [[Create Role from Open Job (PRD 887e)]] · [[Role enhance pool timeout unhandled (Bug 23808)]] · [[Outbox poll backoff (Bug 23522)]] · [[Outbox skips internal-originated emits (Bug 23656)]] · [[Sentry observability rollout (US 22295)]] · [[similar_role null-vector 500 (Bug 23362)]] · [[Map - Observability & Reliability]] · [[Map - TrackerRMS integration]]
