---
type: delivery
status: merged
env: taller
delivered: 2026-08-21
tags: [feature, documents, talent, battle-tested]
prs:
  - "https://github.com/taller-projects/echo-backend/pull/2127"
  - "https://github.com/taller-projects/echo-backend/pull/2137"
  - "https://github.com/taller-projects/echo-backend/pull/2159"
fe_prs:
  - "https://github.com/taller-projects/echo-frontend/pull/3235"
tickets:
  - "https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24143"
  - "https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24528"
prd: "https://app.notion.com/p/3c3aedca11f081ad974ff672582238a2"
---

# Retain original resume file (US 24143)

Battle Tested customer request (Jake Gomez, Aug 2026): opening the "original" resume showed the Echo-regenerated PDF because the internal upload flow (`POST /talents/resume`) is parse-only — the file was never persisted. M1 (backend) formalizes the resume Document tag convention so the FE can retain and retrieve the true original via the existing talent documents endpoints.

## Azure / docs
- [US 24143](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24143) — In development, assigned Gonzalo
- PRD: [Retener el CV original subido (US 24143) — PRD Técnico](https://app.notion.com/p/3c3aedca11f081ad974ff672582238a2) — Tier B, **In development**; Opción A decidida 2026-08-21

## PRs
- **Promotion 2026-08-26 (via the team's BATCH release train, not my targeted PRs)** — M3 (#2159) + rename (#2165) went out with the batch: **qa DEPLOYED** via [#2168](https://github.com/taller-projects/echo-backend/pull/2168) (dev→qa, MERGED) · **prod PENDING** in [#2169](https://github.com/taller-projects/echo-backend/pull/2169) (qa→main, OPEN). My targeted [#2166](https://github.com/taller-projects/echo-backend/pull/2166)/[#2167](https://github.com/taller-projects/echo-backend/pull/2167) were CLOSED (superseded). M1 #2127 & Q5 #2137 were ALREADY on qa/main. **Gotcha:** batch promotes land as cherry-picks, so dev SHAs (`ed572aa9`) are NOT ancestors of qa/main — verify promotion by CONTENT (`create_talent_cv_version`/`TalentCvService`/`Resume — ` present), not `git merge-base`. Safe ahead of FE: endpoint dormant, no migration. Conscious override of the PRD full-feature-QA gate.
- [#2165](https://github.com/taller-projects/echo-backend/pull/2165) → dev — **MERGED 2026-08-26** (branch `24143/rename_cv_to_resume_filename`). User-facing rename **"CV" → "Resume"** in the generated-version name (client feedback: "CV" is ambiguous). `create_cv_version`: title `"CV — …"` → `"Resume — …"`; filename fallback `or "cv"` → `or "resume"` (unnamed talent downloads `resume.pdf`, not `cv.pdf`). **Internal `cv` tag/`CV_TAG`/`CV_SOURCE_*` deliberately kept** (stored contract; renaming breaks FE retrieval + needs data migration). Docstring/OpenAPI summary left as-is. 15 tests green. FE (24528) should use `title`/basename as the download name.
- [#2159](https://github.com/taller-projects/echo-backend/pull/2159) → dev — **MERGED 2026-08-26** (merge commit `ed572aa9`; branch `24143/store_generated_cv_versions`; `979759bf`+`f7aa6289`+`e13dcb00`). **QA en dev OK 2026-08-26** (Bruno collection "CV Versions (M3 24143)" + curl on Test HOLA): dedup (identical request → same id), plain-Echo AND tailored both stack, tags `[cv, echo|tailored, sha256]`, titles, combined `resume,cv` list all verified. **M3: store GENERATED CV versions** (tailored + plain Echo) as talent documents, alongside uploaded originals. New endpoint `POST /talents/{talent_id}/cv-versions`: renders (tailors when `job_description` present, same render as `/export/talent`) → dedup → persist as Document → returns the current version's `DocumentResponse`. `/export/talent` **unchanged = preview**. `DocumentService.create_talent_cv_version` + `_get_latest_talent_cv_version`; tags `[cv, tailored|echo, sha256-<hash>]`. **Dedup key = the generation REQUEST hash, NOT output bytes** — WeasyPrint stamps a creation date/id so rendered PDFs are non-deterministic; `compute_cv_content_hash(pre-tailor talent payload + jd + jt + format + template)` makes a repeated generate a no-op while any edit → new version. Generated carry `cv` (not `resume`) so the "current original" query is untouched; "all versions" = `tags__array_overlap=resume,cv`. No schema/migration. Tests: `test_cv_version_dedup.py` (hash + dedup branch, no DB) + `test_talent_cv_versions_routes.py` (route persistence/tags/source/dedup). Taller only. **/pr-review (Leo) r1 CHANGES REQUESTED — addressed in `f7aa6289`**: blocker = tailor+render was copy-pasted from the export router (drift risk = exactly what M3 prevents) → extracted `TalentCvService.render` in `export/service.py`, called by BOTH `/export/talent` (preview) and `/talents/{id}/cv-versions` (store); did NOT add ResumeParserService to `FileExportService.__init__` (would break ~20 `FileExportService(MagicMock(),MagicMock())` test constructions) — new composing service instead; `test_export_custom_cv_logic.py` moved with the logic to target `TalentCvService.render`. Nits: `_get_latest_talent_cv_version` size=1 (was unbounded); `max_length` on job_description(20000)/job_title(500); +edge tests (cross-tenant, DOCX, A→B→A=3 docs, nonexistent talent → **400** via repo FK translation, NOT 500 as the review guessed); dedup-race documented as accepted follow-up (hash is in the `tags` array, a hard guarantee needs a unique-index/schema change). 278 tests green. **r2 APPROVED (review 5031446191) — one new nit fixed in `e13dcb00`**: the r1 refactor introduced a hash regression — I'd simplified the dedup hash to pass the RAW `user.tenant.cv_template`, but the render uses the EFFECTIVE `cv_template if has_custom_cv else None`; toggling `cv_custom_fields` (template stays set) would flip the render plain↔custom without busting the hash → false dedup. Restored hashing the effective template (matches Round-1). 15 CV-version tests green.
- [#2137](https://github.com/taller-projects/echo-backend/pull/2137) → dev — **MERGED 2026-08-24** (squash `b64c483d`; Leo approved, review 5011361221; CI green). Open question 5 (server-side 5 MB hardening): `UploadPolicy` gains optional `max_size_bytes` (default `None`); `validate_upload` rejects oversize with 400 (`detail`), measuring **actual stream bytes** via `_file_size` seek/tell — not the spoofable `Content-Length`/`UploadFile.size`; `DOCUMENT_POLICY` + `RESUME_POLICY` capped at `MAX_DOCUMENT_SIZE_BYTES`, `BLOB_POLICY` left uncapped (data-team large CSV/JSON). **Cap = `5 * 1000 * 1000` (decimal MB), aligned EXACTLY to the FE** (`CVUpload.tsx:74` + `DocumentForm.tsx:96`, both `5_000_000`) — the first commit `b8311246` used `5*1024*1024` (5,242,880, ~237 KB looser), corrected in `cff36095` (+ message divisor so it still reads "5 MB"). Cap lives on the shared policy singletons → **every `DOCUMENT_POLICY`/`RESUME_POLICY` consumer inherits it**, incl. the internal backend-to-backend route, org and project (confirmed intentional; the only internally-built `UploadFile`, rate card, doesn't pass `validate_upload`). No schema/migration/endpoint change. Review-nit test hardening in `47ec255e`: `max-1` lower-boundary accepted case, a `.docx` oversize case (gate is type-agnostic within the allowlist), defensive `_sized` padding-helper assert — **67 unit tests green**. /pr-review (3 agents): READY WITH NITS, 0 blockers.
- [#2127](https://github.com/taller-projects/echo-backend/pull/2127) → dev — **MERGED 2026-08-21** (squash `3f133298`; Leo approved (review r1 `5c15a511`: RESUME_SOURCE_APP renamed `app`→`echo-app`, `-id` tie-break on DocumentFilter.order_by, file renamed test_talent_documents_routes.py; r2 nits `83bc9e25`: direct same-created_at tie-break test + cross-tenant upload rejection test; nit 2 = order_by default now `-created_at,-id` for org/project listings too — FE radar, no change). 10/10 file tests green. /pr-review r1 (full, 3 reviewers): READY WITH NITS, 0 blockers; nits landed in `5c15a511`.

## How
- **Option A** (decided): FE re-uploads the original to the existing `POST /talents/{talent_id}/documents` after talent create/patch; backend contract = tag convention only. Option B (multipart on `POST/PATCH /talents`, public-API parity) documented in the PRD as fallback if hard server-side retention is ever required.
- `app/modules/document/schemas.py`: `RESUME_TAG="resume"` + `RESUME_SOURCE_API/SELF_APPLIED/APP` constants (`APP="echo-app"`); "current original" = most recent `resume`-tagged document (default `-created_at,-id` ordering of `DocumentFilter` — `-id` tie-break added post-review for determinism).
- Constants applied at the two pre-existing public-API producers (`PublicTalentWriteService._attach_resume`, apply flow) — zero behavior change.
- `tests/unit/test_talent_documents_routes.py` (8 tests; renamed from `test_talent_documents.py` — basename collided with the public_api sibling): upload persists convention tags/tenant/created_by, upload policy 400, nonexistent talent → translated 400 (composite-FK violation → `ReferencedError`, not 500), tagless upload → `tags=[]`, `tags__array_overlap=resume` latest-first, cross-tenant isolation of the listing, literal tag values pinned (renaming would orphan stored docs).
- No schema change, no migration, no OpenAPI surface change.

## Decisions
- Option A over B: reuses shipped infra, no content-type migration on core endpoints; soft-fail orchestration pattern already accepted for public API. Recorded in PRD (pre-Approved, so no changelog entry needed).
- Tag source for internal flow = `echo-app` (renamed from `app` post-review — ambiguous vs the `Application` entity; zero rows persisted so the rename was free; PR body, US comment, FE Task 24461 and PRD annex all updated).
- `DocumentFilter` default order gained `-id` tie-break (affects talent/org/project document lists; only OpenAPI delta is the param default).
- No backfill: historical originals don't exist anywhere.

## Gotchas
- **(M3, QA 2026-08-26) Intermittent 502 from the external doc-loader mirror (`EchoDocumentService`) on dev** — hits the new cv-versions endpoint AND the pre-existing M1 upload/delete equally (shared infra flake, not M3). The mirror runs *after* the Document row is committed, so **a 502 still persists the version** — the "soft-fail" M3 doc bullet is aspirational; the mirror failure currently surfaces as a 5xx. Retrying is safe (dedup → no duplicates). Follow-up: wrap the mirror so its failure doesn't break the response. Same behavior affects DELETE (remove_document mirror).
- **(M3) WeasyPrint output is non-deterministic** — it stamps a PDF creation date + document id, so two renders of identical data differ byte-for-byte. Content-dedup on output bytes would NEVER dedup; M3 dedups on a hash of the generation *request* (`compute_cv_content_hash`: pre-tailor talent payload + jd + jt + format + template). Also robust to the tailoring service (LLM) being non-deterministic.
- **Local Protected-route 404 gotcha has a workaround**: talent route tests 404 locally when `scripts/venv.sh` is sourced (leaks `ENABLE_ACCESS_CONTROL`/`POSTGRES_*`). Run them green locally WITHOUT sourcing venv.sh: `env -u ENABLE_ACCESS_CONTROL uv run pytest <file>` — Docker still required (testcontainers). This is how M3's route tests were verified locally.
- Documents are downloaded via raw S3 `key` → **unauthenticated CloudFront URL** (no presigned URLs); pre-existing PII exposure, deliberately OUT of scope — needs a separate security ticket (unfiled).
- ~~No server-side max file size anywhere (5MB is FE-only)~~ **Server-side 5 MB cap now enforced on document/resume uploads (#2137, `b64c483d`).** Still NO network-level (ingress) body limit — the cap is app-layer, running after Starlette spools the full body; an `client_max_body_size` follow-up is unfiled.
- `document` table has NO Postgres RLS (app-level `TenantScopedRepository` only).
- `DocumentService.create_document` latent crashes for programmatic callers: `Path(None).suffix` and `",".join(None)` when tags omitted.
- Deleting a document never deletes the S3 object.
- Ticket description said `POST /talent/resume` — actual route is `POST /talents/resume`, and it has no `talent_id` (corrected via ticket comment).

## Pending
- **M3 (NEW, 2026-08-25): store GENERATED CV versions (tailored + Echo)** as documents, not just uploaded originals. Origin: test feedback "si subo un cv se guarda como Original Resume, pero si lo exporto tailored no se guarda como document" — which was correct/by-design for M1/M2 (export was fenced out); Product decided to un-fence it. Backend PR [#2159](https://github.com/taller-projects/echo-backend/pull/2159) → dev OPEN (new endpoint `POST /talents/{id}/cv-versions`). **FE = [Task 24528](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24528)** (unassigned, FE): generate against the new endpoint (not on preview); Download becomes **pure-FE** from the stored version's `key` (no `/export/talent` on download); list uploaded + generated versions together. PRD updated with an M3 section. Taller only.
- M2 (FE-owned): [Task 24461](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24461) — **MERGED** ([echo-frontend #3235](https://github.com/taller-projects/echo-frontend/pull/3235), 2026-08-25): upload original with tags `resume,echo-app` on add-candidate + replace-resume via `useOriginalResumeUpload`; original visible in the Documents tab. Title used = "Original Resume".
- **Q5 RESOLVED + MERGED 2026-08-24 (Gonzalo): server-side hardening shipped — PR [#2137](https://github.com/taller-projects/echo-backend/pull/2137) squash `b64c483d` (5 MB cap on `DOCUMENT_POLICY`/`RESUME_POLICY`, decimal-matched to the FE). Q3/Q4 deferred to the FE tickets. Follow-up (UNFILED): network-level ingress body limit (`client_max_body_size`/`max_request_size`) — this cap is app-layer only (runs after Starlette spools the full body).**
- PRD open questions 3–4 (UI placement, doc title — FE-owned) + Capa 1 formalization. **Q2 RESOLVED 2026-08-21: full history retained — replace never deletes the prior original (PRD changelog row; Task 24461 updated).** Review also flagged two PRD wording fixes still pending: annex says `RESUME_POLICY` but the endpoint enforces `DOCUMENT_POLICY` (XLSX/CSV could become "the original"), and the soft-fail warning-log criterion has no backend home under Option A (re-home to M2/FE).
- Security follow-up ticket for unauthenticated CloudFront document access (unfiled).
- qa/main promotion after dev QA; Kforce port OUT unless requested.

## Related
- [[Generic proposal template & case studies (US 23670)]] (Battle Tested driving client) · Battle Tested rollout epic 23300
