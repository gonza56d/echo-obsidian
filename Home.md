---
type: home
tags: [home]
---

# 🏠 Echo delivery memory

Personal vault: one note per delivered feature/bugfix (echo-backend + echo-frontend), so "what did we do 15 days ago and what was left pending?" takes one search instead of an archaeology session. Conventions: [[CLAUDE]] · Template: [[Delivery]].

**Maps**: [[Map - Contact Relationships]] · [[Map - Kforce]] · [[Map - TrackerRMS integration]] · [[Map - JazzHR integration]] · [[Map - Pentest 2026]] · [[Map - Observability & Reliability]]

---

## 🚧 In flight (open PRs / gated work)

- [[Unification 9c organization_people deprecation (US 24167)]] — PR [#2027](https://github.com/taller-projects/echo-backend/pull/2027) → dev **OPEN** (2026-08-10). **Dev-side DROP EXECUTED in the PR** (brought forward from Phase 6): routers unmounted, `app/modules/people/` deleted, org-merge relink cleaned, migration `mq7xw2vk4dpe` (DROP VIEW+TABLE+TYPE, IF EXISTS, kforce-replay-safe); suite green 3701. Traffic-check "blocker" reclassified with freshness evidence (table frozen 2026-02-04, prod reader consumes dead snapshot; 83 smart queries, 0 view refs). Review r1 (Pedro) addressed `c74f8c9c` (cycle test, checkfirst, waiver); **merge gated on the heads-up to script owner `607b7da5-…` + Pedro's ack on the ticket**. Follow-ups: kforce twin PR, FE cleanup PR, prod pg_dump + `pg_depend` preconditions, openapi baseline re-record with the twin.
- [[Stamp last_status_update on status change (Bug 24165)]] — PR [#2026](https://github.com/taller-projects/echo-backend/pull/2026) → dev **OPEN** (2026-08-10). `ApplicationService.update` stamps `last_status_update` on real status changes when the caller doesn't send it (explicit ATS back-sync value wins via `model_fields_set`); covers promotion out of matching. Full unit suite green (3620). Pending: merge → Bug Resolved; FE consumption is Patricio's.
- [[Not Eligible for Rehire talent filter (US 24155)]] — **BE MERGED to dev 2026-08-07** (PR [#2022](https://github.com/taller-projects/echo-backend/pull/2022) squash `97544232`; Leo approved, nits fixed `15937726`; Task [24156](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24156) **Closed**). `not_eligible_for_rehire` on `TalentFilter`: correlated EXISTS over `placement`, ANY-placement semantics, no backend tenant gate. Still open: FE Task [24157](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24157) (control + overlay badge `.some()` fix, unassigned), US [24155](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24155) Active until FE + QA; qa/main promotion unrequested; kforce port not evaluated.
- [[Roles listing NULL-row hardening (Bug 24132)]] — **PR [#2008](https://github.com/taller-projects/echo-backend/pull/2008) MERGED to dev 2026-08-06** (squash `2d9b3e7c`, Leo approved; review r1 mechanism correction `1b751804` included). CI never ran pre-merge (GH Actions outage) — post-merge dispatch on dev **GREEN**. Left to do: Bug 24132 → Resolved; ticket 3 follow-ups (`PublicRoleResponse` same gap, `virtual_interview` `'{}'::jsonb` default drift, `ApplicationResponse` sibling).

- [[Push Applications Echo to JazzHR (Feature 24051)]] — **M1 [#1986](https://github.com/taller-projects/echo-backend/pull/1986) MERGED to dev 2026-08-05 (squash `d24e5007`); M2 [#1987](https://github.com/taller-projects/echo-backend/pull/1987) + M3 [#1988](https://github.com/taller-projects/echo-backend/pull/1988) + system tests [#1989](https://github.com/taller-projects/echo-backend/pull/1989) still open** (stacked — merge order #1986 → #1987 → #1988 → #1989; **#1987 + #1988 MERGED to dev; #1989 (system tests) conflicts resolved 2026-08-05 via dev-merge `836548d0` + Pedro/Leo nits `b7acd959` → MERGEABLE — whole stack conflict-free, #1989 is the last to merge**). M3 = DELETE 409 (global); **#1989 system tests** (test-only, stacked on M3) add the PRD's testcontainers E2E coverage — **reviewed 2026-08-05 READY WITH NITS (0 blockers), 4 nits fixed in `b44ed1b3`** (added the missing `tracker_rms` `application.created` E2E, strengthened the link-preference assert, `app`→`application` + `_entity_type` cleanup). All JazzHR/application unit + system tests green (5 unrelated adoption tenant-isolation tests fail pre-existing on dev). Inverts application sync for Taller: Echo becomes the origin, event rides the outbox to a new `jazz_hr` handler → entity-resolution (`created`→create-or-get, `updated`→`change_stage`, workflow 637230, raw status names). Feature [24051](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24051) under Epic [23305](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23305), US per milestone: [24052](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24052) (M1 create) / [24053](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24053) (M2 change_stage) / [24054](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24054) (M3 FE+cutover). Verified vs `dev`: the **matching emission rule** fixes an active Navitec leak (`matching_diff`→tracker fallback-POST→shortlist off one match). M1, M2 + M3 backend all /pr-review'd (READY WITH NITS, 0 blockers) with nits fixed. **Delivery stays disabled** until M3 + the data-team ER v2 contract (open q#2/#4/#5); back-sync adaptation blocks cutover. FE work noted inside US 24054 (no separate FE ticket). **2026-08-05: follow-up [#1996](https://github.com/taller-projects/echo-backend/pull/1996) → dev aligns the create path to the finalized `apply_to` contract** (was still on legacy `sync_data`). **2026-08-06: LIVE TEST on dev vs REAL Jazz ✅ — both `apply_to` (create-or-get + dual-write link) and `change_stage` (stage verified moved in Jazz) delivered E2E through the real dispatcher; freshness guard + email redaction verified live. Flag flipped back to enabled=false after. 2 enablement blockers found: JazzHR index lag (~5-6 min) makes every NEW-candidate create 404→immediate dead-letter (needs retryable-404 decision), and numeric `talent.jazz_person_id` never matches ER's `prospect_…`-only `talent_jazz_ids` (no email fallback → guaranteed dead-letter when set). Details in the note's Live test section. Blocker 2 → Bug [24118](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24118), fix PR [#2006](https://github.com/taller-projects/echo-backend/pull/2006) → dev OPEN (reads `ats_external_ids`, prospect_-prefix filtered — data-team-confirmed answer to open q#4).** → [[Map - JazzHR integration]]
- [[On Hold candidates invisible in pipeline buckets (Bug 24010)]] — [#1973](https://github.com/taller-projects/echo-backend/pull/1973) → dev **open, in review**; both On Hold variants → In Process + "ON HOLD"→"On Hold" normalization (validator + migration `1u4ash4rwbxj`). /pr-review round 1 done 2026-08-04: blocker (alembic fork vs `tp7dq2mk9v4x`) + nit (pipeline-health exclusion test) fixed in `346b8225`; 3 questions open on the PR. Pending: merge → Ready to Test; follow-up ticket for the other 7 `category=other` orphan statuses NOT filed yet.
- [[Create Role from Open Job (US 23849)]] — **[#1933](https://github.com/taller-projects/echo-backend/pull/1933) MERGED** (2026-07-29, `e9e4fc04`) and **verified end-to-end on dev**; follow-up **[#1943](https://github.com/taller-projects/echo-backend/pull/1943) → `dev` open** with the six post-merge review nits (authz mirror moved to `ProjectService`, plus an eager-`getattr` fix that was costing one `SELECT "user"` per dependency on every authenticated request) — **reviewed READY WITH NITS (6/6, 0 blockers) and the 8 review nits fixed (`663883b8`)**; awaiting CI + Pedro. Still owed: FE ticket + heads-up, PRD Changelog entries, `vault/Echo/` note on the `short_id`-sequence trap, qa/main promotion, ticket states.
- [[Create Role from Open Job (PRD 887e)]] — Tier B. **Two review rounds resolved 2026-07-28** (13 items, then 6 more on the resolution itself — three of which corrected my own answers). Final shape: authorization lives in the **service** and raises 403 (`Protected` returns 404, and a permission gate can't cover the INSERT policy's data-scope clause); 404 = job absent or no `companies`; write order declared so no external call runs inside the atomic transaction; the shared-helper failure-contract change to `POST /roles` + `POST /internal/roles` is declared, not incidental; `retry-enhancement` widens to `roles.edit` to close the create-but-can't-recover dead end; 409 with structured `detail.role_id`; JD-overwrite accepted. **Estimate 5-7 days, QA 5-6 h.** Only the Capa 1 business PRD stays open. **Next: sign-off → Azure tickets → FE ticket.**
- [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — **M0–M3 ALL MERGED to dev** ([#1944](https://github.com/taller-projects/echo-backend/pull/1944) / [#1945](https://github.com/taller-projects/echo-backend/pull/1945) / [#1946](https://github.com/taller-projects/echo-backend/pull/1946) / [#1947](https://github.com/taller-projects/echo-backend/pull/1947), M3 merged 2026-08-03 `e832fa94`). Infra worker Deployment (dev) [#10299](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10299) **MERGED** 2026-07-30 (ArgoCD auto-sync; worker in dev, flag off). Azure: tasks 23872–23875 **Closed**, US [23871](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23871) **Ready to Test** (open for QA). PRD 7444 synced 2026-08-03. **Gated on**: flip both dev Vault flags on + test load; **M4 cutover** (task 23876) incl. **prod-enable infra — task [23971](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23971) → PR [ttit #10519](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10519) OPEN** (values-prod worker block, replicaCount 2, ships idle until the Vault flip; QA/staging excluded — they run no background processes), Supavisor budget (56 conns at cutover), grace 600s; **M5 sweep** (task 23877); **Grafana dashboards/alerts** — DONE 2026-08-03 (dashboard `enhance-role-worker-echo-backend` + 5 alert rules live in Grafana). **QA on dev DONE 2026-08-03** (queue verified end-to-end; bug [23974](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23974) fixed in [#1965](https://github.com/taller-projects/echo-backend/pull/1965) — **reviewed READY WITH NITS, nits fixed `093a396b`, awaiting CI + merge**; kforce sibling bug found, needs its own ticket); remaining QA: failure/dead-letter, generate_team drain, multi-tenant fairness, is_internal, chaos, ~220 burst. EQT remediation; 9-pending Loki forensics (now unblocked). **2026-08-05: LIVE in prod** — both Vault flags flipped, verified end-to-end (retried a failed test role -> job `completed` + role `enhanced`). Now capping concurrency to a **hard 3**: prod worker -> 1 replica (ttit [#10553](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10553), open, linked Task [24024](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24024)) + Vault `BACKGROUND_WORKER_CONCURRENCY=3` pending (tenant cap alone is a soft snapshot bound that bursts overshoot).
- [[Role enhance pool timeout unhandled (Bug 23808)]] — [#1911](https://github.com/taller-projects/echo-backend/pull/1911) → dev **open; 2 review rounds — round 2 found 1 blocker in the round-1 tests, fixed + pushed (`a8cb45a7` tests, `a35af463` signature narrowing)**; full `tests/unit` green (2757). [Bug 23808](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23808) still Active → move to Ready to Test; re-request review (approval covers commit 1 only). Follow-ups still uncreated as tickets: pool-exhaustion investigation (03:00 UTC bulk-matching window) + hardening pass over unprotected `add_task` call sites.
- [[Roles company filter (Feature 23719)]] — [#1907](https://github.com/taller-projects/echo-backend/pull/1907) → dev + cherry-picks [#1908](https://github.com/taller-projects/echo-backend/pull/1908) → qa / [#1909](https://github.com/taller-projects/echo-backend/pull/1909) → main all **open, reviewed (READY, 0 blockers), nit addressed on all 3**; merge order dev → qa → main (cherry-picks via merge commit). FE half untracked on the Feature — flagged.
- [[Generic proposal template & case studies (US 23670)]] — backend (M1–M4 + Pedro's design-fidelity/contract follow-ups #1887/#1891/#1899/#1903) **+ FE ungating ([#3025](https://github.com/taller-projects/echo-frontend/pull/3025)) IN PROD since 2026-07-24**; US **Ready to Test**, tasks 23671–74 Closed, PRD status updated. Still open: **TB accept-all-tenants** (external long pole — generation/export still 409 for unregistered tenants), **FE data-readiness warnings (R5)**, feature QA vs the approved design.
- [[Outbox skips internal-originated emits (Bug 23656)]] — [#1871](https://github.com/taller-projects/echo-backend/pull/1871)/[#1872](https://github.com/taller-projects/echo-backend/pull/1872) **merged 2026-07-21 → fix in prod**; Bug in **Testing**. Pending: confirm dispatcher prod re-enable + Layer-2 hardening (change-detection, coalescing, company semantics, stale external_ids, alerts). Pedro shipped adjacent role-sync fixes meanwhile (#1888/#1893/#1896/#1900; #1905 open) → [[Map - TrackerRMS integration]].
- [[Interview assessment change history (US 22248)]] — in prod via the combined #1875/#1876 merge (2026-07-21); US **Ready to Test** (Melina, 07-24) — awaiting QA.
- [[Pentest 4.1.7 input validation (23018)]] — PRs [#1625](https://github.com/taller-projects/echo-backend/pull/1625) (dev) + [#1626](https://github.com/taller-projects/echo-backend/pull/1626) (kforce-dev) **still open**
- [[Pentest 4.3.1 CORS allowlist (23024)]] — PRs [#1632](https://github.com/taller-projects/echo-backend/pull/1632) + [#1633](https://github.com/taller-projects/echo-backend/pull/1633) **still open**; live re-test pending post-merge
- OpenAPI 500 via `partial_model` (Sentry `7600828395`) — fix verified, **PRs to dev + kforce-dev not created yet** → [[Map - Observability & Reliability]]
- [[Kforce-main code unification (PRD 3b2aedca)]] — **Pedro's Tier-C program to retire the Kforce fork** (PRD **Draft** 2026-08-04, no code/tickets yet). One codebase (base = `dev`), **Kforce becomes a tenant** on dedicated infra/DB (structural convergence only, no cross-env data migration). Additive gated reimplementation — **never** `git merge dev→kforce-dev`; single migration tree via merge-revision + canonicalization queue (zero stamping); golden snapshots + unification ledger as the parity nets. 8 open decisions forced in Phase 0 (incl. `hire_pay_rate` data-loss, chat-RLS re-enable, `has_echo`, `/internal` dual-auth shim). When it lands it obsoletes the whole [[Map - Kforce]] backport doctrine. → [[Map - Kforce]]
- [[Kforce Last Contacted By filter (PR 1846)]] — round 2 [#1850](https://github.com/taller-projects/echo-backend/pull/1850) (kforce) + Taller port [#1851](https://github.com/taller-projects/echo-backend/pull/1851) **merged 2026-07-17**, Bugs [23638](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23638)/[23639](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23639) **Closed**; still pending: kforce-dev deploy + live dropdown re-test, kforce-master + Taller qa/main promotion
- Kforce [Task 23375](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23375) — gated `last_relationship_type` column DROP → [[Kforce Contact Relationships port (US 23370)]]
- Taller QA/PROD matview gate for migration `mx7qkw9n2r4v` → [[Generic Contact Relationships (US 23240)]]
- Kforce group-hierarchy: 3 latent seed bugs + prod seed → [[Kforce multilevel groups (US 23339)]]
- Industry-agnostic: admin surface + prod `generic` gated on matching-products data promotion → [[Industry-agnostic Echo (PRD 398aedca)]]
- Outbox dispatcher prod re-enable/quantification (infra [#9981](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/9981)) → [[Map - TrackerRMS integration]]
- WeasyPrint: Taller proposal manual QA → [[WeasyPrint 62 to 68 upgrade (US 23479)]]
- Open Jobs residuals (feature itself **SHIPPED**, US Closed): confirm Data-team notification + Pedro-review reply went out, post-deploy wipe sanity check, `ApplicationRepository` vector-value refactor → [[Open Jobs 1-5 matching (US 23640)]]

## 📌 Recent activity
- **2026-08-10 (Unification 9c — review r1 addressed, merge gated on heads-up)** — [[Unification 9c organization_people deprecation (US 24167)]] — Pedro reviewed #2027: code clean, 1 process blocker (the courtesy heads-up to prod reader `607b7da5-…` — the compensating control of the reclassification — still undone). Fixes in `c74f8c9c`: **permanent migration-cycle test** (`mx7qkw9n2r4v` pattern; replay no-op → downgrade recreates enum/table/view from `.sql` → real drop), `checkfirst=True` on downgrade enum, `[openapi]` waiver (2 routes + 3 schemas, NOT PeopleInvolved). **Own-error caught + corrected in PR body**: the unit suite builds schema via `create_database()`, NOT alembic — my "CI exercises the migration" claim was false until the new test made it true. Row 11 gained the `pg_dump` prod-promotion precondition. Waiting on: heads-up (Gonzalo, prod access), Pedro's ack on the ticket. → [[Map - Kforce]]
- **2026-08-10 (Unification 9c — DROP EXECUTED for dev after evidence-based unblock)** — [[Unification 9c organization_people deprecation (US 24167)]] — Gonzalo challenged the "parked" call ("does the prod thing actually block it?"); answered with data instead of the ticket's label: dev table frozen since **2026-02-04** (`max(created_at)`), prod **0 write-path calls in 28d retention**, and the external SQL-builder's 83 real generated smart queries reference **neither** the view nor the table → the prod reader consumes a dead snapshot → blocker reclassified to courtesy heads-up. Shipped the drop in #2027 commit `375bbdca`: routers unmounted, module deleted, org-merge relink removed, migration `mq7xw2vk4dpe` (guarded, kforce-replay-safe; downgrade recreates empty shape + view — note the DB enum has 9 values, one more than the model). Suite green **3701**. Live dev DB `pg_depend` verified (only the view depends). Kept the view's `.sql` file — 3 historical migrations `open()` it at upgrade. PR title/body updated, AB#24167 commented. → [[Map - Kforce]]
- **2026-08-10 (Unification 9c — drop attempt → BLOCKED by its own pre-drop check)** — [[Unification 9c organization_people deprecation (US 24167)]] — Gonzalo asked to bring the step-2 drop forward into #2027; ran the blocking traffic check first (Loki, 7d, 4 envs) and it **failed**: Taller prod has a real reader (12 GET 200, `python-requests`, single user `607b7da5-…`, multi-org) + 8 vestigial PATCH 404; kforce-dev internal POST 201/DELETE 204 (1-second create+delete ≈ synthetic probe, but ingestion path proven alive); kforce-prod 5 PATCH 404 (4 users, latest same day). No drop code written — unmounting routers would break the prod reader. Amended ledger row 11 with the dated evidence + unblock list (`cea37a57`), updated PR body, commented AB#24167. Prod-DB caller lookup permission-blocked in-session (query saved in the note). → [[Map - Kforce]]
- **2026-08-10 (Unification 9c — ledger row PR #2027)** — [[Unification 9c organization_people deprecation (US 24167)]] — Pedro's US 24167 inverted step 9c's premise: instead of adding the physical `contact_id` FK to dev, `organization_people` is legacy in both envs (replaced by `organization_employees`, zero consumers) → shipped [#2027](https://github.com/taller-projects/echo-backend/pull/2027) → dev adding ledger row 11 (`deuda-cola`): spec of the Phase-6 drop in BOTH envs (routers unmount, module delete, drop migration + dead view cleanup, FE PR, blocking pre-drop traffic check). Re-verification found 2 nuances the ticket missed (commented on AB#24167): the FE PATCH no-op claim is **dev-only** (kforce `PeopleBase` accepts + persists `contact_id`), and `openapi.json` is in the snapshot corpus → baseline re-record at drop time. → [[Map - Kforce]]
- **2026-08-10 (Bug 24165 — last_status_update stamp, PR #2026)** — [[Stamp last_status_update on status change (Bug 24165)]] — Patricio's ticket from the JazzHR stage UI: `PATCH /applications/{id}` never wrote `last_status_update` (only the ATS back-sync did — the exact fact documented in Bug 24149), so Jazz workflow-less apps had no stage-change date. Shipped [#2026](https://github.com/taller-projects/echo-backend/pull/2026) → dev: stamp in `ApplicationService.update` on real change + not-explicitly-sent (`model_fields_set` guard keeps the back-sync timestamp authoritative); promotion NULL→first-stage included; 24149's `last_activity_at` docstring updated. 5 new client-level tests; 3620 unit green. **Gotcha:** sourcing `scripts/venv.sh` before pytest exports `.env` (allexport) and hijacks the testcontainer → every client test 404s; run plain `uv run pytest`.
- **2026-08-07 (Bug 24149 CLOSED — PR #2018 merged to dev)** — [[Talent last_application recency (Bug 24149)]] — [#2018](https://github.com/taller-projects/echo-backend/pull/2018) **merged to `dev` 15:27 UTC** (merge commit `6c383870`, Pedro approved, 2 review nits fixed pre-merge in `7edfb2d4`); post-merge CI on dev **green**; **Bug 24149 → Closed**. `last_application` now ranks by `GREATEST(step-history, last_status_update, created_at)` with a deterministic `id` tie-break, via a shared `last_activity_at()` helper applied to all three duplicated sites. **Still open, unticketed:** "most recent" ≠ "active" (some talents' newest application is terminal → overlay gets that one) and qa/main promotion.
- **2026-08-07 (US 24155 — nits, CI flake, MERGE + Task 24156 Closed)** — [[Not Eligible for Rehire talent filter (US 24155)]] — Leo APPROVED #2022 (READY WITH NITS, 0 blockers): added the 422 case + an explicit cross-tenant isolation test in `15937726` (docstring spells out it locks the app-layer `TenantScopedRepository` scoping since testcontainers bypasses RLS); **declined the move-to-`tests/system/` nit with the CI-runs-unit-only rationale** on the PR. First CI run failed on a SonarQube **503** (tests+lint step green) — `gh run rerun --failed` cleared it; note `gh pr checks --watch | tail` masks the exit code, read the output instead. Squash-merged `97544232`, Task 24156 → Closed, US stays Active for the FE half.
- **2026-08-07 (Not Eligible for Rehire filter — tickets + BE PR #2022)** — [[Not Eligible for Rehire talent filter (US 24155)]] — Gonzalo's tech spec verified against code (one correction: the `enabled_process_status` flag it mentions doesn't exist — the Status filter has NO backend gate, so we gate FE-only, confirmed). Created US [24155](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24155) + child Tasks [24156](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24156) (BE) / [24157](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24157) (FE control + overlay badge ANY fix). Shipped PR [#2022](https://github.com/taller-projects/echo-backend/pull/2022) → dev: `_apply_rehire_filter` correlated EXISTS mirroring `_apply_applications_exists`, + 5 route-level tests. **Gotcha worth remembering**: older talent test modules capture `ENABLE_ACCESS_CONTROL` at import time → collection-order-fragile restores (would have failed my file in CI's alphabetical order); my module toggles it in a run-time fixture instead. Also: azure-devops skill's PAT extraction assumes double quotes, `~/.zshrc` uses single → all calls 302 until re-cut.
- **2026-08-07 (Bug 24149 — last_application recency: triage, ticket rewrite, fix + PR #2018)** — [[Talent last_application recency (Bug 24149)]] — Patricio reported the detail endpoint omitting `id`/`talent_id` inside `last_application`; **disproved it empirically before writing any code** (local backend on the dev DB + deployed dev, both endpoints emit the identical shape with `id`). Real cause = the recency key: `coalesce(max(step_history.created_at), last_status_update) DESC NULLS LAST`, where `last_status_update` has **no writer in `app/`** and no DB trigger — only ATS sync payloads set it — so UI-created applications rank NULL and lose permanently. Also disproved the follow-up "terminal stage" framing (891/2587 affected talents have a non-terminal winner; cleanest case = two `New` applications 80 s apart returning the older) and found the ordering was outright **non-deterministic** when all keys are NULL. Rewrote AB#24149 title + description + **Repro Steps** (Patricio had fixed the description but left the stale root cause in the repro field → self-contradictory ticket), then shipped [#2018](https://github.com/taller-projects/echo-backend/pull/2018) → dev: shared `last_activity_at()` helper using `GREATEST(..., created_at)` + `id DESC` tie-break, applied to the three duplicated sites so they can't drift. 3643 unit green; 5 new tests verified failing on `dev`; heaviest query 523→294 ms. Open: merge, and the product question "most recent" ≠ "active".
- **2026-08-07 (JazzHR push — Task 24058 observability: alerts created + Loki-drop finding + PR #2017)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — verified 24058 was NOT done, then worked it: **jazz_hr dead-letter alert rules created** (`jazzhr-dead-letter` nonprod + `jazzhr-dead-letter-prod`, folder *alertas*, new group `outbox-dispatcher`, mirror of `enhrole-dead-letter`; note there was NO generic outbox dead-letter alert to mirror); dashboard needs no changes (already by-`destination`); **PR [#2017](https://github.com/taller-projects/echo-backend/pull/2017) → dev**: `configure_logging()` in dispatcher+worker entrypoints (structured JSON parity + honor LOG_LEVEL; closes Leo's worker question) + `import logging.config`. **FINDING (old diagnosis disproven)**: dispatcher INFO *does* reach Loki — the missing Aug-6 jazz delivery logs were **Loki ingestion drops of whole windows** (proof via `integration_outbox.locked_by`: all 6 successes ran in-cluster; windows 13:28–13:42Z + 14:06–17:17Z lost) → infra ticket needed (unfiled). **Both Thanos datasources broken today** (nonprod 504, prod "No StoreAPIs matched") → dashboard visual check pending + the 5 enhance-role alerts are currently blind. **Task 24058 → CLOSED same day (user-directed); residuals out-of-ticket: Loki-drops infra ticket UNFILED, Thanos visual check, PR #2017 merge.** PR #2017 reviewed by Pedro (READY WITH NITS, 0 blockers) → 3 nits fixed in `e636731b` (configure_logging patched in entrypoint tests, called-once+before-sentry contract tests, docstring) — awaiting merge. → [[Map - Observability & Reliability]]
- **2026-08-06 (JazzHR push — PROMOTION PRs to qa + main)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — feature-scoped cherry-pick promotion (6 commits d24e5007/ae0a6752/608956d3/d6c2b0d8/4854d154/d5b69095, ZERO migrations): PRs [#2013](https://github.com/taller-projects/echo-backend/pull/2013) → qa + [#2014](https://github.com/taller-projects/echo-backend/pull/2014) → main (identical trees, verified; **merge-commit only, #2014 after QA**). qa==main at cut time + touchpoints already promoted → all picks CLEAN; promoted files byte-identical to dev; 3419 unit green on the qa base. Ships inert (no tenant_integrations row in qa/prod); on-merge deltas = DELETE→409 + emission rule (fixes the Navitec matching_diff leak in prod). #2008 roles hardening NOT included (promotes independently). Turn-on still gated (Task 24057 + data team + FE + QA). → [[Map - JazzHR integration]]
- **2026-08-06 (PR #2008 MERGED to dev)** — [[Roles listing NULL-row hardening (Bug 24132)]] — squash `2d9b3e7c` at 18:58Z (Leo approved, Gonza merged) incl. review-r1 fixes `1b751804`. GH Actions outage swallowed EVERY CI event for the branch (creation run auto-cancelled, synchronize + merge-push dropped) → merged with zero CI; validated post-merge via manual `workflow_dispatch` on dev → **GREEN**. Pending: Bug → Resolved, 3 follow-up tickets.
- **2026-08-06 (PR #2008 review r1 → mechanism corrected + fixes pushed)** — [[Roles listing NULL-row hardening (Bug 24132)]] — 3-agent /pr-review: verdict CHANGES REQUESTED on one doc-only blocker — comment/PR body/ticket claimed "nullable columns" but all 13 are NOT NULL in DDL; real vector = jsonb JSON null. Fixed in `1b751804` (+ deepcopy comment reword, + `RoleCreate` null-rejection regression test); PR body PATCHed via REST, correction commented on AB#24132. GH Actions was down at PR creation (suites stuck queued, dispatch 500'd); recovered ~18:45Z, CI queued. Follow-ups surfaced (unticketed): `PublicRoleResponse` same failure class; `virtual_interview` dev default `'{}'::jsonb` vs model `'[]'`.
- **2026-08-06 (dev /roles 500 incident → Bug 24132 filed + fix PR #2008)** — [[Roles listing NULL-row hardening (Bug 24132)]] — Patricio's Jazz role-picker query 500'd; diagnosed via Loki (fresh Grafana key): the **minimally-seeded JazzHR fixture role** (raw INSERT, jsonb/enum columns NULL) failed `RoleListResponse` validation → 500 for EVERY `/roles` page containing it + single GET (filter params innocent; zero traffic 13:22→17:16 hid it). Healed the row by copying columns from clone-source `708ca2d4` (all 200s again, data-only). Then hardened: Bug [24132](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24132) + PR [#2008](https://github.com/taller-projects/echo-backend/pull/2008) → dev — generic `mode=before` validator coercing NULL → declared default (deepcopy'd, response-side only, mirrors the `company_name` guard) for 13 fields; +2 schema-level tests; 3528 unit green. `quantity=0` (`Gt(0)`) deliberately not coerced — data-meaning change, 0 rows exist. → [[Map - JazzHR integration]]
- **2026-08-06 (JazzHR push — FULL-API test round ✅: Bug 24118 fix verified live, On Hold case CLOSED, dup-prospect ambiguity surfaced)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — post-#2006-deploy (image `26151_dev`) round through the REAL public API (JWT, tenant Taller): DELETE in-process → 409 (M3 guard live); POST /applications → correct emission payload → delivery success attempt 1 with the **discriminator-armed talent** (numeric `jazz_person_id` + prospect `ats_external_ids` — pre-fix code would have dead-lettered ⇒ **Bug 24118 verified in dev**); PATCH → change_stage moved real Jazz to "Screened Candidate to be Reviewed"; bonus: **"On Hold" PATCH succeeded — Jazz step is "ON HOLD", ER matches case-insensitively** (PRD QA risk closed). New finding: duplicate prospects make **email-only apply_to resolution nondeterministic** (returned the dup's application, no 409) — data-team heads-up priority raised. Flag back OFF. Remaining: Blocker 1 retryable-404 (unticketed), data-team heads-up, Grafana key, 24057/58/59, back-sync, FE 24115/24116. → [[Map - JazzHR integration]]
- **2026-08-06 (JazzHR push — Bug 24118 fix PR: talent_jazz_ids ← ats_external_ids)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — the data team confirmed `talent_jazz_ids` must receive `talent.ats_external_ids` (the `prospect_…` reference ids the candidate sync appends at `talent/service.py:423-425`), NOT the numeric `jazz_person_id` the handler was sending (live-test Blocker 2, closes PRD open q#4 with the real answer). Filed Bug [24118](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24118) under Feature 24051 + fix PR [#2006](https://github.com/taller-projects/echo-backend/pull/2006) → dev (worktree branch `24118/jazz-hr-talent-jazz-ids-ats-external-ids`, commit `43bf39c1`): `get_talent_jazz_context` reads `ats_external_ids` normalized to `tuple[str,...]` (legacy scalar/junk JSON dropped at the read), create path sends all `prospect_`-prefixed values and omits the field when none remain (junk like `ATS-12345` would 404 with NO email fallback), change_stage untouched. Dev data: 30,498/30,504 populated rows `prospect_…`. 3515 unit + 74 outbox system green, ruff clean. Azure gotcha: work-item CREATE needs `%24Bug` (the literal `$Bug` 302s to MS login; reads are fine). Independent Blocker 1 (index-lag retryable-404) still needs its ticket.
- **2026-08-06 (JazzHR push — LIVE TEST vs real Jazz: both endpoints ✅ + 2 blockers found)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — controlled-outbox-event test on dev (seeded role/talent/application for test job `job_20250113141221_…` / Jazz UI 9299278), real dispatcher → real ER → real Jazz. **Create path**: `apply_to` 200 (get branch — `candidates/sync_data` side-effect had created the app), dual-write `projob_20260806132451_GZVEP0XWMUNU1E8X` into `application.external_id` + `entity_external_links(jazz_hr)`. **Status path**: `change_stage` New→Screening Scheduled delivered 1st attempt, **stage verified moved in real Jazz**. Freshness guard (stale event → no-op, no regression), `_redact_email` PII scrub, and the dead-letter replay runbook all verified live. **🔴 Blocker 1**: Jazz search-index lag ~5-6 min → `apply_to` 404 "Candidate does not exist" → 4xx = immediate dead-letter ⇒ **every brand-new candidate's create dead-letters** in the real flow; needs retryable-404 (à la `OrderingNotReadyError`) or ER-side fix. **🔴 Blocker 2**: Echo persists numeric `jazz_person_id` (`"431832457"`) but ER's `talent_jazz_ids` only matches `prospect_…` ids, no email fallback → any talent with the hint set dead-letters; cheapest fix = drop the hint from the handler. **🟡** `sync_data` duplicated a prospect under the lag (data-team heads-up); Grafana key expired again. Flag back to **enabled=false**; fixture left seeded. → [[Map - JazzHR integration]]
- **2026-08-05 (JazzHR push — live-test prep on dev; #1996 MERGED)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — [#1996](https://github.com/taller-projects/echo-backend/pull/1996) merged. Dev enablement done: `tenant_integrations(Taller, jazz_hr)` **enabled=TRUE**, `ECHO_INTERNAL_API_KEY` present; `ENTITY_RESOLUTION_API_URL` absent from dev Vault but code default = dev ER (OK for dev; **qa/prod MUST set it or they hit dev ER** → Task 24057). **⚠️ JazzHR is one shared prod+dev env** — enabling `jazz_hr` for all of Taller in dev pushes every new dev Taller app create/stage-change to **real Jazz**. Designated test job *"Test Engineer 888"* (`job_20250113141221_L0XUZ7L6G6H72ZZI`, Jazz UI 9299278) has **no Echo role in dev or prod** → must seed a role first. Awaiting Gonzalo on: seed OK, test candidate email, create-only-first vs change_stage, controlled-outbox-event method. Ticket = refine existing Task 24057 (blocked on ExitWorktree to reach the Azure PAT). See the note's *Live-test prep* section. → [[Map - JazzHR integration]]
- **2026-08-05 (JazzHR push — create path aligned to ER `apply_to` contract)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — follow-up PR [#1996](https://github.com/taller-projects/echo-backend/pull/1996) → dev (branch `24051/jazz-hr-apply-to-contract`, off `origin/dev`, commit `3c07eaf6`). The merged create handler still hit the legacy `POST /applications/sync_data`; the data team's finalized contract (verified live) is `POST /applications/apply_to` — candidate resolved by **email** (`jazz_candidate_id` gone), `echo_application_id` (PK) now required, `echo_source`/`user_id`(←`created_by_id`) nullable, `talent_jazz_ids` optional hint. **Prod audit drove it**: only ~5% of Taller talents carry `jazz_person_id` vs ~100% with email, so the old shape would dead-letter almost every create. Also: dead-letter now on missing email; response-id lookup collapsed to the single `data.jazz_application_id` (persists `external_id` on fresh-create AND "already exists in JazzHR" 200); dropped `apply_date` from change_stage; folded the talent lookups into one `get_talent_jazz_context`, removed the redundant `get_talent_jazz_person_id`. Full unit (3442) + both outbox system files (71) green. Ships inert (`jazz_hr` still disabled). **/pr-review'd READY WITH NITS (0 blockers)**; own-review nit (NULL-column coverage) fixed in `cfadb977`. **Pedro APPROVED (review `4868171785`, 0 blockers)**; 3 of his 4 nits fixed in `902a4be7` — **PII scrub of `talent_email` from jazz_hr upstream error bodies** (`_redact_email`, both paths), a named create-or-get "already exists" test, and a system E2E for the `jazz_person_id`-NULL omit branch; the unused-column nit skipped by design. `apply_date`-dropped-from-`change_stage` is a data-team confirmation, not a blocker (ships inert). → [[Map - JazzHR integration]]
- **2026-08-05 (Kforce↔main unification PRD — vault note)** — [[Kforce-main code unification (PRD 3b2aedca)]] — captured Pedro's Tier-C code-unification PRD as a program reference note: single codebase (base `dev`), Kforce as a tenant, additive gated reimplementation, single migration tree (merge-revision + canonicalization queue, zero stamping), golden snapshots + ledger, phases 0–7, 8 open decisions, consumer plan (jazz-client + entity-resolution hit by the `/internal` api-key switch). Reframed [[Map - Kforce]] (the fork is being retired → backport doctrine becomes temporary). → [[Map - Kforce]]
- **2026-08-05 (JazzHR push #1989 — conflict fix + Pedro & Leo nits)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — last PR of the stack. M3 merging to dev left #1989 [#1989](https://github.com/taller-projects/echo-backend/pull/1989) conflicting: **merged `origin/dev` in** (merge `836548d0`, force-push banned) — all 6 conflicts took **theirs/dev** (this PR's only own file is the new `test_outbox_dispatcher_jazz_hr.py`). Then 3 nits in `b7acd959`: **(Pedro)** mirrored the wrong-tenant test for the other 3 repo lookups (was only on `get_talent_jazz_person_id`); **(Leo 1)** `_enable_jazz` disables `tracker_rms` for order-independence (module-scoped rows persist); **(Leo 2)** extracted the 4 shared helpers to new `tests/system/_outbox_helpers.py` so the jazz suite stops importing the sibling test module's private surface (sibling imports from there too; dropped its now-unused `OutboxRepository`/`patch`). 76 system tests pass. **#1989 MERGEABLE** (CI re-running) — whole stack now conflict-free. → [[Map - JazzHR integration]]
- **2026-08-05 (JazzHR push M3 — Leo's DELETE review nits)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — addressed Leo's 3 non-blocking nits on M3 [#1988](https://github.com/taller-projects/echo-backend/pull/1988) (he approved) in commit `448baa21`: (1) moved the `application_deleted_in_matching` log to AFTER `super().delete()` (a failed repo.delete must not claim a delete that didn't happen); (2) the internal back-sync path now logs a distinct `application_deleted_backsync` event (was silent). Nit 3 (read-then-delete TOCTOU) left as-is — Leo marked it low-severity/non-regression/optional; row-conditional-DELETE hardening deferred. Added a test that no log fires when `super().delete()` raises + log-event asserts on both paths. 19 `test_applications` green, ruff clean; 204/409/404 contract unchanged. → [[Map - JazzHR integration]]
- **2026-08-05 (JazzHR push M3 — #1988 conflict fix after M2 merge)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — M2 squash-merging to dev left M3 [#1988](https://github.com/taller-projects/echo-backend/pull/1988) conflicting. Same play as M2 (force-push banned): **merged `origin/dev` into the M3 branch** (merge `b1e2ff4e`), NOT rebased. 4 outbox files (`dispatcher.py`, `repository.py`, `test_outbox_dispatcher.py`, `test_outbox_triggers.py`) → **theirs/dev** (M3 never touched them; dev has the full M1+M2 content + nits incl. the a640948d union); `service.py` → kept M3's DELETE override (auto-merged) + took dev's reworded `_in_matching` docstring; `test_applications.py` (M3's DELETE guard/endpoint tests) merged clean. Full unit+multitenancy suite green (3494 passed, 1 xpassed), ruff clean. **#1988 now MERGEABLE** (CI re-running); #1989 (system tests) is the last stacked PR still to dev-merge. → [[Map - JazzHR integration]]
- **2026-08-05 (JazzHR push M2 — #1987 conflict fix after M1 merge)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — M1 squash-merging to dev left M2 [#1987](https://github.com/taller-projects/echo-backend/pull/1987) conflicting (its branch still carried M1's individual commits vs dev's squash). Force-push is banned, so **merged `origin/dev` into the M2 branch** (merge `a640948d`), NOT rebased. Resolved 5 files: `service.py` + `test_outbox_triggers.py` → **theirs/dev** (M2 never touched them; delta was purely M1's re-review nits); `dispatcher.py` + `repository.py` → **ours/M2** (superset of dev's M1 — verified the only dev-only lines were the M1 `application.updated` NotImplementedError placeholder M2 supersedes); `test_outbox_dispatcher.py` → **union** (kept dev's create-path error tests, dropped the invalid placeholder test, added M2's change_stage suite + the `OrderingNotReadyError` import). Full unit+multitenancy suite green (3487 passed, 1 xfailed), ruff clean. **#1987 now MERGEABLE** (CI re-running). → [[Map - JazzHR integration]]
- **2026-08-05 (JazzHR push M1 — #1986 re-review nits fixed)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — addressed the second-round review nits on M1 [#1986](https://github.com/taller-projects/echo-backend/pull/1986) (Leo APPROVED + Pedro's M1–M3 stack comment, 0 blockers) in commit `a64c36d5`: dedicated `_resolve_outbox_status` unit tests for the step-not-found (`return None`) and raw-string status fallback branches (+ enum-value/step-name/in-matching); an **AC M1-4** test driving `create_matching_diff` end-to-end proving a still-matched app emits nothing (the TrackerRMS shortlist leak); `_in_matching` docstring reworded to stop promising exact `Application.category` equivalence (dangling step + NULL status differs); and the `_insert_talent` system-test helper rebuilt as fully-static SQL binding nullable `jazz_person_id` directly (no f-string). Verified the 10 co-failing system tests (RLS-bypass tenant-isolation + tracker-sync 403) reproduce on the clean baseline — unrelated. 121 touched-file unit + jazz_hr system tests green, ruff clean. CI (`test and lint`) green → **squash-merged to dev** (`d24e5007`) as the bottom of the stack; **#1987/#1988/#1989 now need rebasing onto dev**. → [[Map - JazzHR integration]]
- **2026-08-05 (enhancement queue LIVE in prod + hard-3 concurrency cap)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — both prod Vault flags flipped on (M4 cutover effectively done); verified end-to-end by retrying a throwaway `failed` role -> `background_job` `completed` + role `enhanced`. Decided to cap concurrency at a **hard 3**: only per-pod `BACKGROUND_WORKER_CONCURRENCY` bounds it hard (the per-tenant in-flight cap is a soft snapshot bound a burst overshoots, stacking across pods), and 2 replicas can't hit an odd 3 -> prod worker to 1 replica (ttit [#10553](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10553), open, linked Task [24024](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24024)) + Vault `BACKGROUND_WORKER_CONCURRENCY=3` pending. -> [[Map - Observability & Reliability]]
- **2026-08-05 (JazzHR push — system tests #1989 /pr-review + nits fixed)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — ran the full `/pr-review` on the test-only [#1989](https://github.com/taller-projects/echo-backend/pull/1989) (3 reviewers on the isolated one-file diff): **READY WITH NITS, 0 blockers**; all 11 tests traced against the M3 production code and confirmed non-vacuous — important because **CI runs `tests/unit` only**, so a broken system test would merge green. Fixed all 4 nits in commit `b44ed1b3` (pushed to #1989): (1) added the one missing PRD System-row scenario — a `tracker_rms` `application.created` E2E (promotion-from-matching must keep creating in TrackerRMS; no application→tracker E2E existed anywhere in the system suite), pinned tracker-only because the module-scoped tenant is shared with the jazz E2E class and writes persist; (2) the `get_jazz_application_id` link test now diverges the scalar mirror and asserts the `entity_external_links` value still wins (proving link-preference, not scalar); (3) `app`→`application` naming parity with the sibling file; (4) simplified `_entity_type`. 70 passed together with the sibling `test_outbox_dispatcher.py` (no interference). → [[Map - JazzHR integration]]

- **2026-08-05 (JazzHR push M3 — /pr-review + nits fixed)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — ran the full `/pr-review` on M3 [#1988](https://github.com/taller-projects/echo-backend/pull/1988) (3 reviewers, scoped to the M3 delta — the `ApplicationService.delete` override + tests): **READY WITH NITS, 0 blockers**; PRD 7/7 backend reqs, arch 13/0, tests-security 13/0, no scope creep. Fixed 3 of 4 nits in `11166928` (pushed to #1988): endpoint-level client tests (in-process DELETE → 409 `{detail}` + row kept; missing id → 404), a both-set (status AND workflow_step_id) 409 unit case, and an info log on the successful matching-only delete (parity with the service's other state-transition logs; internal back-sync path stays quiet). Skipped the 4th (project the `get_by_id` SELECT) by design — negligible, touches a destructive hot path. Non-blocking: read-then-delete TOCTOU (accepted — improves on the previously-unguarded DELETE) + FE handling of the new global 409; OUT-OF-SCOPE: the PRD's `Protected([Permission.Talents])` DELETE claim vs the route's `AuthenticatedUser`-only gate (pre-existing). 18 `test_applications` green, ruff clean. → [[Map - JazzHR integration]]
- **2026-08-05 (JazzHR push M2 — /pr-review + nits fixed)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — ran the full `/pr-review` on M2 [#1987](https://github.com/taller-projects/echo-backend/pull/1987) (3 parallel reviewers, scoped to the M2 delta since it's stacked on M1): **READY WITH NITS, 0 blockers**; 8/8 US 24053 acceptance criteria implemented, no scope creep. All 3 reviewers independently agreed the `OrderingNotReadyError` 5xx-retry-budget choice (vs `StaleDeleteOrderError`'s 409 budget) is correct — the idempotent create-or-get predecessor never rides the 409 budget. Addressed the 5 nits in commit `493594af`: typed `get_talent_change_stage_context` via a `TalentChangeStageContext` frozen dataclass; narrowed the `stage_mismatch` parse `except` to `(ValueError, AttributeError)`; documented the 5xx-budget rationale on `OrderingNotReadyError`; and 3 test additions (happy path asserts `user_id`/`apply_date`/`X-Echo-internal`; new ER-5xx-retryable + freshness-guard current-status-unknown). 16 `TestDeliverJazzHr` green, ruff clean. → [[Map - JazzHR integration]]
- **2026-08-04 (JazzHR push M1 — /pr-review + test-nit follow-up)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — ran the full `/pr-review` on M1 [#1986](https://github.com/taller-projects/echo-backend/pull/1986) (3 parallel reviewers): **READY WITH NITS, 0 blockers**. Reuse verified in code — `persist_sync_result(external_platform="jazz_hr")` dual-writes `application.external_id` + `entity_external_links(jazz_hr)` (application is in both `_ENTITY_SYNC_CONFIG` and `_DUAL_WRITE_ENTITY_TYPES`), and `build_application_payload` carries echo_role_id/echo_talent_id so the promotion-CREATED path never dead-letters. Addressed the actionable test nits in commit `3a11bf52` (test-only, +190/-0): 7 unit (ER 5xx; absent role/talent id guards; non-JSON→LinkPersistError; persist raises/returns-False w/ log asserts; e2e `_process_event` create-success) + 3 system `TestOutboxRepositoryGetTalentJazzPersonId` (value/None/cross-tenant isolation). Dispatcher unit file 93 green, ruff clean. M3/US 24054 gates before enabling delivery (not blockers): `talent_email`+`echo_source` in the ER body (ticket named 5 fields, PR sends 3), platform-aware role read, collapse `_JAZZ_APPLICATION_ID_KEYS`. → [[Map - JazzHR integration]]

- **2026-08-04 (JazzHR push — system tests PR)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — added `tests/system/test_outbox_dispatcher_jazz_hr.py` (branch `24051/jazz-hr-application-push-system-tests`, stacked on M3): real-Postgres coverage of the 4 jazz repo lookups + jazz_hr dual-write, and dispatcher E2E (created→link, updated→change_stage, updated-without-link→retry/never-create, reordered→superseded no-op, unsupported→no-op) with mocked HTTP. 11 pass; 69 with the existing dispatcher system file. **CI runs unit-only** so these are local-only (`./scripts/test.sh system`, Docker). PR [#1989](https://github.com/taller-projects/echo-backend/pull/1989) → dev (depends-on-#1988). Closes the PRD testcontainers plan. → [[Map - JazzHR integration]]
- **2026-08-04 (JazzHR push M3 — DELETE 409 restriction PR)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — M3 backend slice (US 24054) on worktree branch `24054/jazz-hr-application-push-m3`, stacked on M2 tip `62f2b7d1`. `ApplicationService.delete` override: in-matching → 204, in-process → **409 `ConflictError`**, **scope GLOBAL** (Gonzalo's call on open q#6), back-sync (`is_internal`) exempt. Service-level `TestApplicationDeleteGuard` + existing client 204 test kept via matching-pinned fixture. Ruff clean. All JazzHR/application unit tests pass; the 5 failing adoption tenant-isolation tests are **pre-existing on the clean M2 base/dev** (verified in the untouched M2 worktree — flaky `assert 0==1`, unrelated). PR [#1988](https://github.com/taller-projects/echo-backend/pull/1988) → dev (depends-on-#1987 note); US 24054 → Active. Remaining M3 = FE + back-sync + infra. → [[Map - JazzHR integration]]
- **2026-08-04 (JazzHR push M2 — change_stage PR)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — M2 (US 24053) on worktree branch `24053/jazz-hr-application-push-m2`, stacked on M1's merged tip `f7b3ea1a`. `_deliver_jazz_hr_update` → entity-resolution `change_stage`: ordering guard (`OrderingNotReadyError` — updated w/o link retries, never creates), freshness guard (`status_to` ≠ current Echo status → no-op `superseded`), no-op on no-status-change; repo lookups `get_jazz_application_id` (platform-aware), `get_application_status`, `get_talent_change_stage_context`. No `ApplicationService` changes. M1 placeholder test replaced by the M2 suite. Full `tests/unit` green (3336), ruff clean. PR [#1987](https://github.com/taller-projects/echo-backend/pull/1987) → dev (base=dev + depends-on-#1986 note); US 24053 → Active. Next: M3 (US 24054). → [[Map - JazzHR integration]]
- **2026-08-04 (JazzHR M1 — dev-merge conflict fix)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — `dev` advanced (touchpoints #1978/#1979) and PR [#1986](https://github.com/taller-projects/echo-backend/pull/1986) conflicted at the EOF of both outbox test files. Merged `origin/dev` into the branch (merge `f7b3ea1a`, no force-push), kept both sides' tests, dropped dev's stale stage-change assertion (superseded by my M1 emission change). 266 tests green; PR now MERGEABLE.
- **2026-08-04 (JazzHR push M1 — PR to dev)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — implemented M1 (create path) on worktree branch `24052/jazz-hr-application-push-m1` off `origin/dev@e133453f`: platform-agnostic emission rule in `ApplicationService` (`_in_matching`/`_resolve_outbox_status`, conditional `create()`, three-branch `update()`) + `jazz_hr` dispatcher handler (create-or-get to entity-resolution, SQL id resolution, dual-write persistence, no-op for unsupported types, retryable M2 placeholder for `application.updated`) + `OutboxRepository.get_talent_jazz_person_id`. Closes the active Navitec `matching_diff`→TrackerRMS shortlist leak as a side effect. 4 emission-rule tests + `TestDeliverJazzHr`; updated 3 existing tests whose premise changed. Full `tests/unit` green (3292), ruff clean. PR [#1986](https://github.com/taller-projects/echo-backend/pull/1986) → dev; US 24052 → Active. → [[Map - JazzHR integration]]
- **2026-08-04 (JazzHR push — PRD analyzed + tickets written)** — [[Push Applications Echo to JazzHR (Feature 24051)]] — analyzed Pedro's Tier-C PRD, verified every load-bearing claim against `dev` (emission rule, outbox/`entity_external_links`/`tenant_integrations` reuse, retry policy 10/20, `is_internal` suppression, status enum with both On Hold variants). Confirmed the **matching emission rule** is sound and closes an **active Navitec bug** (`matching_diff` emits `updated` per matched app → tracker fallback-POST → shortlist off a single match). Created Feature [24051](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24051) under Epic 23305 + 3 milestone US ([24052](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24052)/[24053](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24053)/[24054](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24054)), all assigned to Gonzalo. New saga → [[Map - JazzHR integration]]. 409-on-duplicate-apply decision: BE raises, FE handles.
- **2026-08-04 (Bug 24010 dev-verified + qa/main cherry-picks)** — [[On Hold candidates invisible in pipeline buckets (Bug 24010)]] — dev e2e pass (staged both variants → `category=active`, `other` empty; migration collapsed 3 legacy rows; data restored). Bonus finding: `ApplicationResponse` inherits `ApplicationCreate` → normalization applies on READ too. Cherry-picks [#1976](https://github.com/taller-projects/echo-backend/pull/1976) (qa) + [#1977](https://github.com/taller-projects/echo-backend/pull/1977) (main) opened with the migration re-chained onto `n8vc3kq7wx2m` (Gonzalo's call over my code-only proposal); single-head/lint/tests verified per branch; promotion trap documented.
- **2026-08-04 (#1973 reviewed + blocker/nit fixed)** — [[On Hold candidates invisible in pipeline buckets (Bug 24010)]] — full /pr-review (3 reviewers) on own PR: ticket compliance 7/7, arch 13/0, tests-security 9/2 → CHANGES REQUESTED on one mechanical blocker: **alembic fork** — dev merged `tp7dq2mk9v4x` (also revising `tpls3jaidxpa`) after the branch was cut → two heads on merge; CI green because the branch predates the fork. Fixed in `346b8225`: merged origin/dev + re-chained `down_revision` → `tp7dq2mk9v4x` (single head verified) + nit covered with `test_get_pipeline_health_excludes_both_on_hold_variants` (`RoleService.__new__` + mocked repo). 10/10 tests, ruff clean, pushed. 3 QUESTIONs left on the report: ACTIVE_STATES ripple (`Talent.has_active_applications`, `Role.active_candidates_count`, talent source-change gate now hard-blocks On Hold as `active_application`), un-normalized `application_status_history` literals, pipeline-health metric shift for pre-existing title-case rows.
- **2026-08-04 (Bug 24010 fix PR)** — [[On Hold candidates invisible in pipeline buckets (Bug 24010)]] — "On Hold" candidates fell to `category=other`, which the FE never queries → invisible in Roles → Candidates. [#1973](https://github.com/taller-projects/echo-backend/pull/1973) → dev: both variants ("On Hold" + Jazz "ON HOLD") now categorize **active**, writes normalize to canonical "On Hold" (validator on `ApplicationCreate`, inherited by all update schemas), migration `1u4ash4rwbxj` collapses existing rows, pipeline-health exclusions preserved. Scope deliberately On Hold only — Meli's analysis found **8** orphan statuses (incl. the four interview "Done" states); follow-up ticket for the other 7 still to file.
- **2026-08-04 (prod release: dev → qa + dev → main PRs)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — team approved promoting the full dev batch; opened **[#1980](https://github.com/taller-projects/echo-backend/pull/1980)** (`deploy/qa_08_04` → qa) and **[#1981](https://github.com/taller-projects/echo-backend/pull/1981)** (`deploy/prod_08_04` → main, ship after QA). Scope pinned to `dev@68e1a085` (excludes #1974 notifications + #1978). 21 PRs, 6 new migrations. `git merge dev` into each release branch → **exactly 1 conflict** (the On Hold migration `1u4ash4rwbxj`, forked: dev re-chained it onto `tp7dq2mk9v4x`, qa/main had it on `n8vc3kq7wx2m`) → took dev's version → **single alembic head verified**. Migrations run in the deploy pipeline on merge. Enhancement queue ships **inert** (both Vault flags off = legacy sync, zero queue writes — verified all enqueue sites are flag-gated). Next: merge QA → smoke-test → merge PROD → re-enable worker idle (revert ttit #10520) → only Vault flags pending (Task 24024).
- **2026-08-04 (⚠️ prod worker CRASHLOOPED → reverted)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — enabling the prod worker (#10519) crashlooped: `No module named app.worker` — the prod image is built from `main`, but `app/worker.py` is only on `dev` (queue code not promoted to main/qa yet), so it crashes before the idle guard. **Fixed by [ttit #10520](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10520)** (`worker.enabled: false` → ArgoCD prunes it). LESSON: a new-module worker Deployment can't precede its code reaching prod — the prod worker block belongs to M4, enabled only after code→main. Turn-on task [24024](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24024) now also gates on code-to-main.
- **2026-08-04 (prod worker Deployment MERGED + turn-on ticket filed)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — ttit #10519 **MERGED**; Task **23971 CLOSED**. Filed the operational turn-on task **[24024](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/24024)** (flip `ENHANCEMENT_WORKER_ENABLED` then `ENHANCEMENT_QUEUE_ENQUEUE_ENABLED` in prod Vault, after the 56-conn pooler check + Grafana contact point/unpause) under US 23871. **(Superseded same day — see crashloop above.)**
- **2026-08-04 (prod worker Deployment PR)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — the M4 prerequisite we forgot for the dispatcher last time: infra PR **[ttit #10519](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10519) → master** (Task [23971](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23971), linked via ArtifactLink) adds a `worker:` block to `values-prod.yaml`, mirroring the dev worker — diff vs dev is exactly `replicaCount` 1→**2** and `releaseLabel` nonprod→**prod**. Safe to merge ahead of cutover: `app/worker.py` idles before building the engine, so an idle pod holds **0** pooler connections and can't CrashLoop (no HTTP probes); enqueue flag also off → legacy path. `replicaCount 2` = 16 slots so the tenant cap of 8 is `ceil(fleet/2)` (real fairness) and makes M4 a pure Vault flip. **QA/staging deliberately excluded** — confirmed they run no dispatcher/worker at all (web-only envs). M4 pre-check: verify prod pooler headroom for `2×28 = 56` worker conns before flipping the flags.
- **2026-08-03 (#1965 reviewed + nits fixed)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — full /pr-review (3 reviewers) on the Bug [23974](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23974) fix [#1965](https://github.com/taller-projects/echo-backend/pull/1965): **READY WITH NITS** (0 blockers; arch 11/0, tests-security 7/0, ticket 5/5). Both nits fixed: regression test parametrized over the second pre-fix-broken combo (whitespace-only `short_description` → `""`, and `"" or None` is still `None`) in `093a396b`; PR body corrected — create-from-open-job was never affected (it always passes `requirements=job.description`, a non-nullable `str`). Review verified the layer choice (relaxing `ProjectCreate.requirements` to accept `None` would loosen the live `POST /projects` contract) and that `""` validates downstream on both flag states. **Found a kforce sibling bug**: identical pre-fix expression at `origin/kforce-dev:app/modules/project/service.py:446` → needs its own ticket + copy+adapt PR. Awaiting CI + merge.
- **2026-08-03 (QA cont'd: multi-tenant + dead-letter + fault-injection tooling)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — seeded the CC `member` row → verified worker-processing tenant isolation (CC role enhanced, job tenant-scoped) + `is_internal` serialization (internal job payload `is_internal=true`); dead-letter path verified via injected malformed/unknown-kind jobs (→ `dead`, no retry). **Fairness (corrected — not a bug)**: the per-tenant cap is fleet-wide; default 8 = ceil(2-pod prod fleet 16 / 2) = correct for prod. Dev's single worker pod (fleet 8 = cap 8) just makes contention fairness unobservable in dev. Retry-path fault injection: closed **#1968** (raise-hook, rejected) → **[#1969](https://github.com/taller-projects/echo-backend/pull/1969) → dev** (independent `PROJECT_TEAM_BUILDER_USE_MOCKSERVER` toggle — reroutes ONLY team-builder to MockServer, after user flagged the shared-flag coupling) + infra **[ttit #10479](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10479)** (MockServer JD 500-on-`MOCKFAIL` + default stub; test procedure in the delivery note). Taller JWT session revoked mid-session (needs a fresh token).
- **2026-08-03 (QA on dev + bug fix)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — ran the queue end-to-end on dev (Taller, both flags on): `POST /roles` → 201 ~1s, transactional `background_job` enqueue, worker drained in 50–60s → role `enhanced`; dedup 409s (`enhancement_in_progress` / `role_already_enhanced`) confirmed; 0 orphan jobs. Found + fixed a pre-existing bug — standalone create with no `job_description`/`short_description` → 500 (`ProjectCreate` requirements=None) → **Bug [23974](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23974)** + **[#1965](https://github.com/taller-projects/echo-backend/pull/1965) → dev**. Still to QA: failure/retry→dead-letter, generate_team→TEAM drain, multi-tenant fairness, is_internal loop, chaos (kill -9 / rollout), ~220 burst replay.
- **2026-08-03 (Grafana observability delivered)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — built + applied via an Editor SA token: dashboard [`enhance-role-worker-echo-backend`](https://grafana.taller.ai/d/enhance-role-worker-echo-backend) (ECHO folder, mirror of the outbox dispatcher, 25 panels on `background_job_*`) + 5 alert rules (folder `alertas`, group `enhance-role-worker`; `enhrole-worker-down` paused until cutover; no contact point yet → default policy). All show No Data until the dev worker runs. Note: the default `GRAFANA_API_KEY` is viewer-only — writes needed `GRAFANA_EDITOR_TOKEN` (read from `~/.zshrc`, which the non-interactive shell doesn't source).
- **2026-08-03 (tracking sync + prod-enable task filed + Grafana access restored)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — M3 [#1947](https://github.com/taller-projects/echo-backend/pull/1947) MERGED to dev (`e832fa94`); all M0–M3 now in dev. Closed Azure tasks 23872–23875, moved US 23871 → Ready to Test; updated Notion PRD 7444 (Estado, implementation-state table, per-milestone headers, Grafana blocker row). Corrected a stale fact: infra [#10299](https://dev.azure.com/TallerInternTools/Snapshot%20Exploration/_git/taller-ttit-kubernetes/pullrequest/10299) is MERGED (not open). Filed the **prod-enable infra task [23971](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23971)** assigned to Gonzalo under US 23871. **Grafana access is back** (token valid, grafana.taller.ai HTTP 200) → dashboards/alerts now actionable; the dispatcher's `Outbox Dispatcher - echo-backend` dashboard (uid `outbox-dispatcher-echo-backend`, Thanos datasource) is the mirror template.

- **2026-07-31 (M3 #1947 round 1 — double review converged, all nits fixed `8bfe30c2`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — own /pr-review (3 reviewers) and Leo's round 1 hit the same head `7be17d0f` and the same finding set: zero code blockers, READY WITH NITS both (Leo APPROVED). All nits (union) in one commit: parameterized `ContextVar`/`stage_observation`/`_run`/`_enhancement_stage` types; `tenant_id` on every `enhance.*` event via a new `_enhancement_stage` param (legacy path was start-only); dead-outcome attempts tests for both observe sites (NonRetryable + unknown-kind — existing dead tests ran `metrics=None`, zero coverage); contextvars-reset pinned (raising job → clean second job, same thread); **enhance.* log contract pinned end-to-end** (`capture_logs` over the full pipeline with `ProjectTimelineRequest`/`TeamBuilderRole` patched: start → 5 named stages → complete + field names; failed path re-raises). 127 background_job tests (+5), ruff clean. Questions routed to author (reaped-terminal attempts gap, worker never runs `configure_logging()` — logs render via structlog defaults, Grafana-deferral closure).
- **2026-07-31 (M2 #1946 MERGED to dev `59d1023c`; M3 #1947 un-stacked + READY FOR REVIEW `7be17d0f`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — final link of the stacked chain: merge of dev into `23875/worker-observability` hit the 3 predicted conflicts (M3's merge-base predates every post-stack M1/M2 fix); reconciliation = M3 instrumentation layered ON dev's rewrites (payload `data` threaded into the new `_run`, attempts histogram extended to the dead-letter path, `enhance.start` after the ENHANCED guard); 1 dev-side `__new__`-fixture test needed `tenant_id`. 122/122 module tests; A/B vs pristine-dev git-archive baseline: failure sets byte-identical (30-flake mode both sides), +6 passed = the 6 new M3 tests. Title/body via `gh api`, undrafted, MERGEABLE. Chain: #1944 ✅ → #1945 ✅ → #1946 ✅ → #1947 in review.
- **2026-07-31 (M2 #1946 conflict vs dev resolved — merge `e0bb6bf8`, PR MERGEABLE again)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — Pedro's urgent prod-mitigation [#1953](https://github.com/taller-projects/echo-backend/pull/1953) landed the same 3 session-release hunks #1946 owned (byte-identical to the pre-nits blob), and the nits commit `6abd5189` had rewritten them into `release_db_session()` → 3 conflicts, HEAD kept everywhere (0-line diff vs tip post-merge); his `enhance_role` in-transaction regression test rides along (union merge, passes against the helper). Full-suite A/B vs pristine dev confirmed byte-identical post-push — merge regression-free.
- **2026-07-31 (M2 #1946 round 1: all 7 nits fixed — `6abd5189`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — worker pool becomes a HARD cap (`max_overflow=0` plumbed DependencyInjector→engine; 28 was silently 128 under QueuePool) pinned at 3 links; `create_match_applications` releases the session per evaluation dispatch (the fire-and-forget loop was parking an idle read transaction in the match thread); release pattern dedup'd into `BaseService.release_db_session()` (6 sites); `_generate_skills` stops returning a detached Role on failure; `update_vector` clean-session contract docstring + honest `-> None`; new mt pin: tenant GUC + scoped read + tenant-scoped write all survive a mid-context `close()`; chaos tests reuse unit conftest helpers + shut their executor down. Full-suite failure set byte-identical to a TRUE baseline (pristine `c16aeb31` via `git archive` tree — local suite is bimodal, 30-flake mode hit on both sides). Questions/PRD corrections + PR replies still open.
- **2026-07-31 (M2 #1946 round 1: Leo's 2 blockers fixed — `c16aeb31`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — B1 (`_generate_skills` = only close-site with no real-session test) → persistence pin mirroring `test_generate_matching_instructions_persists`; B2 (worker inherited the web's 5-min idle-in-transaction timeout through the shared `after_begin` listener, PRD-forbidden) → `BACKGROUND_WORKER_IDLE_IN_TRANSACTION_TIMEOUT_MS=30000` plumbed per-process like `application_name` (entrypoint → DependencyInjector → DatabaseResource → listener), claim/heartbeat/reap sessions bounded too. 5 pins, one per link; 6/6 mutants killed. Full unit+mt suite fully green (3270). Nits/questions/PRD items deliberately left for the author; round 2 = verification.
- **2026-07-31 (M1 #1945 MERGED to dev `7a7337fd`; M2 #1946 un-stacked + READY FOR REVIEW `1567a67b`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — update ritual on #1946: merge of dev, one trivial conflict (worker pool_size), dependencies.py collapsed out of the diff (M1 round-4 fix was a verbatim copy by design), effective diff 4 files, title/body refreshed, undrafted. 110/110 module tests + 2/2 lease chaos post-merge. Chain: #1944 ✅ → #1945 ✅ → #1946 in review → #1947 (M3) still stacked/draft.
- **2026-07-31 (M1 #1945 round 4: READY WITH NITS + carryover fixed — `1846e0c2`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — Leo round 4 = closure verification: zero blockers, 4/4 surviving mutations now caught (verified live), 20/23 ✅, zero scope creep; only req-23 dev E2E evidence stands. Round-1 carryover finally in M1: worker connections stamp `application_name=echo-backend-worker` (mirrors #1946 verbatim → clean rebase) and the session-liveness pool binds lazily (worker never builds it) — unblinds the M2 conexiones_por_job pg_stat_activity filter. Pinned by TestWorkerProcessIdentity ×3. Small round-4 nits + PRD housekeeping left open on the note.
- **2026-07-31 (M1 #1945 round-3 nits, all addressed — `11c2b679`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — Leo's full nits list closed in one commit: dead `priority`/`on_active_job` params removed (commit-race now a real 409 via `DuplicateError.constraint`); drain-rule literals bound from `IN_FLIGHT_ENHANCEMENT_STAGES`; reap bounded per poll + handlerless-terminal warning + callable `on_terminal` check; savepoints moved to `writer.enqueue_job_if_absent` + one-round-trip batch pre-check; `CrossTenantRoleError`; org-job `commit()` shadow deleted; cross-table duplicate errors stop leaking index names; malformed payloads salvage `project_id`; dead-letter reasons drop input values; M8/M12 mutations pinned; TestGenerateTeamQueueBranch/TestOnEnhancementSettled/TestProjectCompletionSignal rewritten on real rows; scalar `get_generation_stage`; partial index `ix_project_generating_updated_at` (`tpls3jaidxpa`). Declined w/ reasoning: `application_name`+pool → stacked #1946; `SessionLivenessEngine` binding lazy (0 connections); failed/dead retention = M0-locked operator decision. PR body refreshed (Pregunta 2). 105 background_job tests, full suite byte-identical to stash baseline.
- **2026-07-31 (M1 #1945 round 3: Leo's 4 new blockers fixed — `00132dde`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — verdict CHANGES REQUESTED at `aa69178b` (round-2 blockers all closed, mutation-verified; the 4 new ones were small and concrete): backstop now gated by `ENHANCEMENT_QUEUE_ENQUEUE_ENABLED` (R2-4: flag-off = pre-queue dev verbatim — ungated it TEAMed projects whose roles the sweep just failed, irreversibly); TEAM write guarded in the UPDATE's WHERE via new `ProjectRepository.complete_generation_if_still_generating()` (no resurrecting a concurrent FAILED, kills the 26-col re-select too); snapshot tenant-filter pinned by a real-handler multitenancy test (worker bypasses RLS — that filter is the only isolation on the drain read); backstop wiring pinned via scheduler-entrypoint convergence + flag-off-dark tests. All 4 of Leo's surviving mutations re-applied locally and confirmed killed; 185 tests green, ruff clean. Still pending: prod row measurement (no prod creds), PR body refresh, PR replies rounds 1-3, dev E2E evidence. Round 4 = verification only.
- **2026-07-30 (M1 #1945 round 2: on_terminal redesigned lossless — `aa69178b`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — Leo's 3 new blockers (all in the completion mechanism round 1 introduced) fixed as edge-fast-path + level-backstop, design pinned in the PRD before round 3 (his iteration-limit warning): project_id in payload, reaper fires hooks via RETURNING, malformed payload → dead-letter, GENERATING backstop sweep, single-tx fan-out with stage+job drain condition, id/stage projection. ENHANCED guard gated behind the flag (req 20 verbatim). is_internal now required. Round 3 = verification only.
- **2026-07-30 (M1 #1945: Leo's blockers fixed — `bdc611c5`)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — B1 via worker-side completion signal (`on_terminal` hook fired strictly AFTER the job leaves active statuses — from inside the handler the drain check would count its own running job and never converge; `complete_generation_if_team_done` is job-based so FAILED-being-re-driven roles keep the project GENERATING); B2 via partial index `ix_background_job_running_tenant` (migration `w7qk2mv9x4tp`); B5 via real-DI two-tenant isolation test pinning SET LOCAL + TenantScopedRepository both directions; ENHANCED guard moved into the pipeline. Nit 5 declined (commit-time IntegrityError unreachable on a fresh UUID). Full suite byte-identical to baseline again. PR reply to Leo pending.
- **2026-07-30 (M1 #1945 review rounds: own rubric + Leo's, first fixes pushed)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — #1944 merged to `dev`, #1945 rebased on it (M1-only diff) + undrafted. Own rubric round 1: CHANGES REQUESTED, 2 test-only blockers (flag-on `_create` atomicity unproven through the real path; PRD item-3 `is_internal` outbox-loop regression absent) — all blockers+nits fixed in `a3281ff0` (real-`_create` atomicity suite incl. standalone 3-row rollback, worker outbox-suppression test w/ False control, ACTIVE_JOB_STATUSES + is_final_attempt centralized, JobContextPayload validated both sides, honest request_enhancement return + role= param; full unit+mt failure set byte-identical vs true stash baseline). The cross-tenant 409-leak nit was already fixed in `a0f7e3b4` by a parallel session — reviewers cite line numbers from the *pushed* diff, always re-verify against the worktree before "fixing". Leo's review (5133234973) crossed in flight: his B3/B4 + 2 nits were already covered by `a3281ff0`; still open = B1 (generate_team sets TEAM at enqueue → FE polling stops while roles pending — verified against useProjectGenerationPolling), B2 (tenant-cap COUNT needs partial `(tenant_id) WHERE status='running'` index; his EXPLAIN shows 476ms→2.9ms and a stall at ~130k backlog), B5 (worker tenant-isolation tests — worker context is `authenticated=False` → admin BYPASSRLS role, so isolation rests entirely on TenantScopedRepository, which silently drops the filter without context). CI red on the PR = Sonar connect-timeout flake only (pytest 3170 green, ruff clean).
- **2026-07-30 (M0 #1944 rubric-reviewed → findings fixed same-day)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — full /pr-review on [#1944](https://github.com/taller-projects/echo-backend/pull/1944): architecture 12/1/3, tests-security 10/1/5, PRD 34/36 with 0 missing and 0 scope creep. Single blocker was **test-only** (retry-budget boundary attempts=3 unpinned in both paths — a `<=`→`<` mutation survived the suite); fixed in `bfd90b34` together with the nits: poll housekeeping failure-isolated like the dispatcher (the arch reviewer traced an unguarded stats/purge error → poll abort → stalled heartbeats → expired healthy leases → duplicate execution), `processed_total` completed/lease_lost outcomes, duration-histogram lease-lost misattribution, typed `_in_flight`/BoundLogger, `purge_all_jobs` de-coupling sibling tests (25→28 tests, all green in 6s; ruff clean). Open questions parked on the note: retry jitter, RLS-on-`background_job` at M1, deleted-entity semantics, idle-mode metrics port vs #10299 probes.
- **2026-07-30 (enhancement queue v2 → implementation, M0 shipped)** — [[Enhancement queue v2 Postgres-backed (PRD 7444)]] — PRD flipped to In Progress (M1 checklist now carries the **6th enqueue site** `organization/job/routers.py:113` that #1933's merge created post-review); US 23871 + milestone tasks 23872–77 created; **M0 = [#1944](https://github.com/taller-projects/echo-backend/pull/1944)** (migration `n3vw8kq2r7pd`, `app/modules/background_job/`, `python -m app.worker`, 25 tests; baseline-diffed the local suite — the 30 unit + 20 multitenancy local failures are identical on clean `dev`, the known TestClient-404-local pattern). Decisions worth remembering: attempts increment **at claim** (poison-job convergence), lease = heartbeat-renewed 180s TTL, purge keeps failed/dead, metrics port 8002. Also: hypothesis for the incident's 9 unswept `pending` roles = cleanup's advisory lock held by a hung session silencing every pod's sweep — Loki check blocked (Grafana key missing from `~/.zshrc` AND `grafana.taller.ai` connection-refused).
- **2026-07-29 (#1943 reviewed + its nits fixed)** — [[Create Role from Open Job (US 23849)]] — rubric review on the nits PR itself: **READY WITH NITS** (6/6 delivered, 0 blockers, 0 scope creep; both reviewer questions dissolved on verification — the route already ran `get_current_user` via the router-level `Protected([Permission.Companies])`, and the system tests pass 10/10 at the head). All 8 review nits fixed in `663883b8`: two stale pointers the PR itself created, the "always accepted at runtime" docstring wording (a bare `PermissionSet` actually raised `TypeError` in `enforce` at request time), `get_user_tenant_id -> uuid.UUID`, mutant-proof parametrised cache tests + `get_user_tenant_id` fallback coverage, an `access_role=None` unrestricted-scope test, and a merged duplicate import. 3078 green, ruff clean. Pedro's ten remaining round-2 items still lack a tracking ticket.
- **2026-07-29 (merged, verified on dev, then the nits PR)** — [[Create Role from Open Job (US 23849)]] — #1933 merged to `dev`; migration `r5e4wdsxzb6l` live. **Dev pass**: 409 (with the existing `role_id`), 404, colon-namespaced PK, overrides, `POST /roles` with and without a JD, retry reachable, **0 orphan placeholder projects after a real 409**, and the open job row untouched (`updated_at` still 2026-04-15). `POST /roles` without a JD covered the case I'd called untestable on dev. **Then [#1943](https://github.com/taller-projects/echo-backend/pull/1943)** for the six nits — all still open on `dev`, since the branch commits mentioning "blockers 1, 4, 6" were a different set. The one worth remembering: `getattr(request.state, "current_user", user_service.get_by_id(...))` evaluates its default **eagerly**, so the user SELECT fired on every cache hit, once per dependency, app-wide — nit 6 was unfixable without that. Also corrected twice-wrong reporting of my own: the 409 `detail` **is** a dict (the FE helpers read `detail.code`/`.message`), and the PRD now carries the verified body.
- **2026-07-29 (US 23849 round 2 — Pedro's review → fixed same-day)** — [[Create Role from Open Job (US 23849)]] — `856b2c51`: migration rebuilt on the `fe10vb1aflw3` CONCURRENTLY pattern (round 1 had wrongly called the lock "repo practice"); **409 shape decision reversed** to the PRD dict `detail = {code, message, role_id}` after Pedro showed the FE helpers read `detail.code` and the plan-limit error already ships that shape; M1's "no external call inside a write transaction" AC now enforced by a fresh-session durability assertion; plan-limit 403 pinned over HTTP via `custom_limits`. His blockers 2/3/5 were already closed by `8e9f5e69`.
- **2026-07-29 (US 23849 review round 1 → fixed same-day)** — [[Create Role from Open Job (US 23849)]] — ran the full rubric review on [#1933](https://github.com/taller-projects/echo-backend/pull/1933) (3 parallel reviewers), then addressed everything in `8e9f5e69`: **blocker 1** explicit-null overrides 500ing (None-filter in the merge, revert-verified), **blocker 2** Task 23853's system test delivered for real (`ENABLE_ACCESS_CONTROL=True`, 201/404/403/409 over HTTP, barrier-synced concurrent double submit against the partial unique index), `tests/multitenancy` wired into CI, and all seven nits. New gotchas recorded: `create_entity` detaches shared fixtures passed as m2m; `Tenant.enabled_modules` is a property (insert `TenantModule` rows); 10 pre-existing env-dependent `tests/system` failures verified against a clean HEAD baseline.
- **2026-07-29 (US 23849 built end to end — M0-M3 + PR)** — [[Create Role from Open Job (US 23849)]] — created the US + 5 milestone tasks, then implemented all four in one branch off `origin/dev` (local `dev` was 3 commits behind and those commits touched the very files involved). **[#1933](https://github.com/taller-projects/echo-backend/pull/1933) → `dev` open**: `role.organization_job_id` + partial unique index; `repo.save(commit=False)` so the placeholder project and its role share one transaction; sync JD generation moved after the commit and guarded; `POST /company/jobs/{job_id}/roles` with authorization in the **service** answering 403 (`Protected` raises 404, and no permission gate can express the policy's data-scope clause); 409 translation carrying the existing `role_id`; `retry-enhancement` widened to `roles.edit`. **Two things worth remembering**: the `short_id` sequence *commits the session*, which silently split the shared transaction until the number was reserved beforehand — and my first M1 test passed for the wrong reason (it patched `_create` out entirely, before that commit), which the M2 racing test caught. Both atomicity fixes are now verified by reverting them and watching the tests fail. Full `tests/unit` green (2963), ruff clean; first-ever coverage of `POST /roles`'s service path. Three adjacent defects left alone and filed instead: RLS denials → 500 repo-wide, `top_tech`'s string default on a JSONB column, and open-job reads not scoped to tracked organizations.
- **2026-07-28 (second pass on PRD A's resolution — 6 items, 3 of them correcting my own answers)** — [[Create Role from Open Job (PRD 887e)]]: Pedro re-reviewed the resolution; 10 of 13 held, six came back and **all six were valid**, verified in the repo. The two that mattered most: `Protected` raises **404**, not 403 (`app/user/permissions.py:87-94`), so the permission-gate criterion I wrote didn't exist and would have collided with the job-not-found 404; and the gate never eliminated the RLS 500 anyway, because the project INSERT `with_check` has a second AND over `access_role_organizations` (`project.consumer_id = aro.organization_id` OR no orgs assigned) that no permission check replicates — with the data already available, so unlike the tracked-org item it isn't new design. Fixed by moving authorization into the **service** with `AuthorizationError` (403) replicating the whole policy. Also folded: write order declared (atomic transaction is DB-only, the guarded sync JD generation runs *after* commit — items 6 and 9 were holding a write txn open across the LLM call); the shared-helper change to `POST /roles` / `POST /internal/roles` declared as an intentional failure-contract change, with the note that `repo.save()` commits unconditionally so item 6 can't be built on it; **items 4+10 were a real dead end** (with `companies + roles.edit` you could create the role but neither retry nor delete it — both routes are `Protected(ProjectsEdit)` — so a failed enhancement blocked the job forever), closed by widening `retry-enhancement` to `ProjectsEdit | RolesEdit` while `DELETE` stays an escalation; and section 5's testing table actually rebuilt this time (it was unchanged despite item 12 claiming otherwise). **Estimate 4-6 → 5-7 days, QA 3-4 → 5-6 h.** First-pass items 2, 6, 9, 10, 12 now carry `↳` supersession markers in the PRD.
- **2026-07-28 (both reviews resolved and folded back into Notion)** — every item on both PRDs now has a decision written into the page, plus in-place body corrections (criteria, contract code block, risk rows, scope rows, estimates, open-question status), and both moved to *Draft — revisión resuelta, listo para `Approved`*. **A**: permission gate `companies AND (projects.create OR roles.edit)` declared on the route so a denial is a 403 before the DB instead of an untranslated RLS 500; 409 becomes an explicit translation with `detail = {code, message, role_id}` (which also closes the "should the 409 carry the role_id" question — yes); project+role become one transaction; the JD overwrite is accepted and stated as a criterion; `requirements` always seeded from `job.description` regardless of overrides; 3-5 → **4-6 days**; open questions 1-4 closed, only Capa 1 left. **B**: sweeper excludes roles with an active job and the worker's skip rule stops trusting role state alone (both into M1); the SLO splits into four criteria so the burst gate becomes "drains ≤120 min with zero losses" instead of an impossible ≥99%/<10 min; synthetic context with an enumerated field list and zero JWT at rest; `priority` + per-tenant in-flight cap; dedup index committed; connection-budget formula + ~48-client initial budget; 600s grace + preStop drain; `generate_team` pulled M5→M1 with a single enqueue point in `RoleService`; two flags with drain-then-disable; 8-13 → **10-15 days**; open questions 2-5 closed. Two new follow-up tickets fall out of A: repo-wide RLS-denial translation (42501 → 403) and tenant scoping for the open-jobs read paths.
- **2026-07-28 (technical review landed on both PRDs — 22 items, all verified)** — reviews arrived as **in-page sections, not Notion comments** (PRD B's signed by Pedro Rocha; PRD A's unsigned, same date/method, and additionally swept `origin/kforce-dev` + `echo-frontend`). Both of my earlier findings were independently caught, one of them **corrected**: the companies-only user doesn't quietly gain a role-creation path, they hit RLS 42501 on the placeholder-project INSERT, which `handle_commit_errors` doesn't translate (it matches `IntegrityError` only) → **HTTP 500**, invisible locally because `ENABLE_ACCESS_CONTROL` defaults to `False`. My `short_description or job_description` precedence point is the one item neither review covers. I re-verified every load-bearing claim in the repo — cleanup sweeper, `generate_team` in-request fan-out, `save()`'s immediate commit, `DuplicateError.status_code = 400`, the project INSERT RLS policy, `organization_job` on kforce-dev, the ingest upsert, `RoleUpdate = @partial_model(RoleBase)` — **all hold**. The three PRD-B blockers are the load-bearing ones: the 15-min stale-role sweeper marks queued roles `failed` and the worker's own skip-on-terminal rule then discards them; `≥99% ENHANCED <10 min` needs ~150-175 concurrent jobs vs the proposed 16 (500 jobs drain in ~110 min); and `is_internal`/JWT claims mean "serialize the context" either reopens the TrackerRMS sync loop or puts credentials at rest for 7 days. Both delivery notes now carry the full item lists.
- **2026-07-28 (two technical PRDs authored + pushed to Notion, then reviewed against the code)** — [[Create Role from Open Job (PRD 887e)]] and [[Enhancement queue v2 Postgres-backed (PRD 7444)]], both Draft under [Echo Backend — Task Docs](https://app.notion.com/p/356aedca11f081ac871ede4f5c6caf45) and cross-linked (the new creation path feeds the same enhancement and migrates to the queue at cutover, with no contract change). **A** (Tier B, 3-5 d): `role.organization_job_id` + partial unique `(tenant_id, organization_job_id)` → 1 role per tenant per open job (409), open job row never mutated, server-side field derivation, ships on the current `BackgroundTasks` enhancement. **B** (Tier C, 8-13 d) iterates Pedro's [v1](https://app.notion.com/p/35daedca11f081d988fbeefa56bb11b6) and revises only the broker: **PG job queue reusing the outbox pattern** (new `background_job` table + new worker pod, dispatcher skeleton) instead of Dramatiq+Redis — because the enqueue rides the role's own transaction and closes the dual-write window that persisted Redis cannot, and because the pattern is already operated in prod; Redis kept as the documented v3 path with an explicit trigger. **Verification pass over both PRDs found 2 things worth folding in**: (1) `POST /company/jobs/{job_id}/roles` would inherit `Protected([Permission.Companies])` from the jobs router (`app/routers.py:475-483`) while `POST /roles` and `POST /projects/{id}/roles` have **no** permission gate — include-level deps can't be dropped per-route, so Option 1 quietly changes authorization; (2) `requirements = short_description or job_description` (`project/service.py:733`) + `enhance_role` regenerating the JD from `requirements` alone means a `short_description` override silently discards the posting text. Everything else checked out: `organization_job` PK is a plain `str` with no `tenant_id` (composite FK impossible), `max_standalone_roles` + `role.created` event confirmed, sync in-request JD generation confirmed at `project/service.py:747-748`, dispatcher lease TTL really is 60s (`config.py:258`) and zero Redis exists in repo or charts.
- **2026-07-27 (review round 2 + fixes)** — [[Role enhance pool timeout unhandled (Bug 23808)]] — second `/pr-review` on #1911 came back **CHANGES REQUESTED**: the round-1 regression test never guarded the bug (it called `enhance_role` directly, but the escaping fetch was the *router helper*'s — so it passed on `dev` too) and its single `stage == FAILED` assertion was vacuous ~25% of the time because `RoleFactory` randomizes `enhancement_stage` despite the schema default. **Fixed + pushed**: `a8cb45a7` reroutes the test through `_enhance_role_with_context`, pins the seed to `PENDING`, and adds three cases (double-failure when `_mark_role_failed` also times out; internal-router wiring enqueues the UUID; missing-role path logs and writes nothing); `a35af463` narrows `enhance_role` to `role_id: uuid.UUID` (no caller passed a Role anymore, and the unwrap sat outside the try — same hazard class), documents the never-raises contract, annotates `_reload_solution_with_context`. Proved the new test discriminates by temporarily restoring the old body (raw `TimeoutError` propagated). Whole `tests/unit` suite green (2757), ruff clean. Also recorded: the FAILED mark is best-effort under sustained exhaustion (backstopped by `RoleCleanupSchedulerService`), and `logger.exception` records at ERROR even when the text says "CRITICAL:".
- **2026-07-27 (review + nits)** — [[Role enhance pool timeout unhandled (Bug 23808)]] — **formal 3-agent `/pr-review` on #1911: READY WITH NITS** (0 blockers, CI green; arch 13/0, tests-security 7/0 — reviewer executed both new tests locally, ticket compliance 6/6, follow-ups correctly out of scope). Key verifications: `_mark_role_failed` writes via the repo's own `get`, so the regression test's `get_by_id` patch can't self-defeat; tenant/RLS unaffected (SET LOCAL is per-transaction from the forked context). **Nits fixed + pushed (`ebf77385`)**: internal `create_role` hands `new_role.id` to the background task (Role object = attribute-expiration DB access before the protected block — same 23808 class); `enhance_role` short-circuits deleted roles with a warning instead of a false-alarm CRITICAL via `_mark_role_failed`; helper typed (`req_ctx: RequestContext`, `-> None`); +1 test — 3/3 green, ruff clean. Out-of-scope routed: the two follow-up tickets (pool-exhaustion investigation, `add_task` hardening) still need creating.
- **2026-07-27 (Sentry investigation + fix)** — [[Role enhance pool timeout unhandled (Bug 23808)]] — diagnosed Sentry [7585920278](https://taller-wn.sentry.io/issues/7585920278/) (prod POST /roles 500-looking noise, 2026-07-01): NOT a roles bug — the `enhance_role` background task died unhandled on a SQLAlchemy **pool checkout timeout (30.00s, pool 20+10)** during two prod pool-exhaustion windows; the 201 was already sent, roles stayed stuck `PENDING`. Smoking gun via the native-exception twin issue `7585920304` (log-message events truncate tracebacks at 1024 chars). Root cause: `_enhance_role_with_context` pre-fetched the role **outside** `enhance_role`'s try/except. Fix = pass the UUID through ([#1911](https://github.com/taller-projects/echo-backend/pull/1911) → dev, open); role now lands `FAILED` → FE retry works. [Bug 23808](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23808) created + Active + PR-linked. Verified live with a prod JWT: no duplicate roles, property costs ~1s worst-case today. 76/76 role tests green.
- **2026-07-27 (stale-ticket sweep)** — [[Application comment RLS write denial (Bug 23112)]] — **backfill + ticket closed**: checked stale Bug 23112 (all `application_comment` writes default-denied — RLS enabled with a SELECT-only policy by `aigtlrr5wami`) — turned out fixed the same day it was filed by [#1573](https://github.com/taller-projects/echo-backend/pull/1573) (policy recreated `FOR ALL`, migration `dvmw0w899yrl`, on dev/qa/main → prod). Verified DEV DB: `polcmd = *`, 36 comments written since, latest 07-21. Ticket **Closed "Fixed and verified"** with resolution comment; delivery note backfilled (vault predates the fix).
- **2026-07-27 (review + nit)** — [[Roles company filter (Feature 23719)]] — **formal 3-agent `/pr-review` on #1907: READY WITH NITS** (0 blockers, CI green; arch 13/0, tests-security 10/0, ticket 5/6 — the 6th is the FE surface). Key verifications: no leak into career-page `PublicRoleFilter`, no operator-allowlist change needed, cross-tenant company UUIDs yield zero rows, FE candidates already use URL param `company_id__in` (1:1 match). **Nit fixed + synced to all 3 PRs**: empty `company_id__in=[]` → always-false clause pin test — `f8053ebb` on #1907, cherry-picked `6d7dbcc8` → #1908 (qa) and `844e39ac` → #1909 (main); 15/15 green locally. Out-of-scope flag: FE half untracked on the Feature (no child ticket / FE PR).
- **2026-07-27 (cherry-picks)** — [[Roles company filter (Feature 23719)]] — **promotion PRs [#1908](https://github.com/taller-projects/echo-backend/pull/1908) → `qa` + [#1909](https://github.com/taller-projects/echo-backend/pull/1909) → `main` opened** (cherry-pick of feature commit `94e89e28`; #1907 → dev still open, so they carry the branch commit, not dev's future squash SHA — content-identical). Clean picks on both bases, 14 filter tests green on each. Merge commits, never squash; main after qa. PR links commented on the Feature ticket.
- **2026-07-27 (back from a week out — full PR/ticket/vault reconciliation)** — swept every backend PR since 07-20. **[[Generic proposal template & case studies (US 23670)]]: backend COMPLETE + IN PROD** — Pedro merged #1883 (07-22) and shipped follow-ups [#1887](https://github.com/taller-projects/echo-backend/pull/1887) (PDF design fidelity: white wordmark, Mona Sans, 16:9, fixed footer rule), [#1891](https://github.com/taller-projects/echo-backend/pull/1891) (PPTX mirror), [#1899](https://github.com/taller-projects/echo-backend/pull/1899) (outbound `tech_stack` null→[] — settles the Task-23674 question: the 422 was BOTH directions), [#1903](https://github.com/taller-projects/echo-backend/pull/1903) (optional `next_steps`/`contact`, Sentry ECHO-BACKEND-BA); [#1890](https://github.com/taller-projects/echo-backend/pull/1890) was a 4-minute stale duplicate (closed). FE ungating [#3025](https://github.com/taller-projects/echo-frontend/pull/3025) merged 07-24. **Azure reconciled**: Tasks 23671–74 → Closed, US 23670 → Ready to Test (direct PATCH to the custom states worked this time), Feature [23541](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23541) → In development (Pedro, 07-22); **PRD status line updated** (was still "Draft for review"). **[[Open Jobs 1-5 matching (US 23640)]] SHIPPED end-to-end**: Data US 23610 Closed 07-21 cleared the blockers → #1875/#1876 merged 07-21, US 23640 + FE US 23644 Closed 07-22. **[[Interview assessment change history (US 22248)]]** rode the same PRs to prod; US Ready to Test (Melina, 07-24). **[[Interview scheduled email evaluate link (US 23179)]]** merged 07-20 → prod 07-23; US Closed 07-24. **[[Outbox skips internal-originated emits (Bug 23656)]]** #1871/#1872 merged 07-21 → fix in prod; Bug in Testing; dispatcher re-enable unconfirmed. **[[Untrack committed vault directory (PR 1860)]]** #1861/#1862 merged — done on all three branches. **New task picked up: Feature [23719](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23719) "Roles - Company filter (all tenants)"** — handed over by Pedro 07-27, In development.
- **2026-07-27** — [[Roles company filter (Feature 23719)]] — **PR [#1907](https://github.com/taller-projects/echo-backend/pull/1907) open → `dev`**: added `company_id__in` to `RoleFilter` (one field — the scalar `company_id` already existed; the gap vs candidates was the multi-select). Compiles via the `Role.company_id` column_property to a correlated `project.consumer_id` subquery — no JOIN/row multiplication, same native path as candidates' `last_application_company_id__in`. +2 compile-to-SQL tests (gotcha: `select(Role.id)`, not `select(Role)`, else `company_name`/`company_logo` subqueries trip the no-JOIN assertion). 14 filter tests + 116 role-scoped tests green, ruff clean. PR linked on the Feature ticket.
- **2026-07-21 (latest — #1883 remaining nits)** — [[Generic proposal template & case studies (US 23670)]] — **addressed the remaining `/pr-review` nits on #1883 (`c2fd9703` → `dev`)**. The M4 typed `TeamBuilderTenantNotRegisteredError` (a `DependencyMissingError`, not an `ExternalApiException`) slipped past the two `except ExternalApiException` soft-fail blocks in `case_study/service.py`, so the `case_study.generation` `outcome="error"` telemetry was silently dropped for the tenant-not-registered case — precisely the dominant failure mode during ungating until TB accepts every tenant (the set was still safe: persistence runs only post-success). Fix: `generate()` + `_request_single_study()` now also catch the typed error, log at **warning** (no stack trace — expected precondition, no Sentry spam) and still record `outcome="error"`; transport failures keep error+traceback. +3 regression-lock tests (typed 409 propagates, set untouched across generate/regenerate/find-similar, telemetry fires). 85 tests green, ruff clean. The other agent's cosmetic nits (`926571d1`) were already recorded. **Still open (Pending):** the `tech_stack` request-vs-response 422 direction (confirm before closing Task 23674) + paired FE PR + TB registration long pole.
- **2026-07-21 (latest — #1883 pr-review + nits)** — [[Generic proposal template & case studies (US 23670)]] — **formal 3-agent `/pr-review` on #1883 (M2+M4): READY WITH NITS** — 0 blockers, **CI green** (`test and lint` pass), **15/15 M2+M4 acceptance criteria** satisfied (arch 14/14 PASS, tests-security 10/10 PASS, PRD 12 fully + 3 verify). Verified: the `ProjectSolutionService.post` chokepoint covers **all 13 TB methods** incl. the 4 named + both flows; **soft-fail preserved** (typed 409 propagates before any destructive write); "no migration" **safe** (`has_feature` does a raw-string membership check, never `TenantFeature(stored)`, so the orphan `case_studies_ppt_export` string is inert); zero dangling refs to the removed enum. **Nits fixed (`926571d1`)**: `_load_logo` logs a warning on unreadable bundled logo (no silent logo-less deck); corrected the stale `export/service.py` timeline comment (post-M2 the gate feeds both PDF+PPTX); typed `_iter_tb_error_messages(detail: object)`; +2 TB-error boundary tests (list-detail pass-through + intended broad "not registered" match). 110 affected tests green, ruff clean. **One open question routed to Pending** (not a blocker): confirm the `tech_stack` 422 is response-side (which #1883 fixes) vs the still-outbound `proposal_builder.py:72` null, before closing Task 23674.
- **2026-07-21 (latest — M2+M4)** — [[Generic proposal template & case studies (US 23670)]] — **M2+M4 → PR [#1883](https://github.com/taller-projects/echo-backend/pull/1883) open → `dev`** (worktree off `origin/dev`, which already carries M1+M3 via #1878 merge `1aa8afec`). **M2**: parameterized the Navitec-hardcoded PPTX palette/font into a `ProposalTheme` dataclass (`NAVITEC_THEME` unchanged + teal `ECHO_THEME`), `_load_logo(None)`→bundled `echo_logo.png`, renderer timeline gate widened to "phases present" (service owns the per-template gate), **removed the `CASE_STUDIES_PPT_EXPORT` 403 guard + enum value** (PPTX now ungated, no migration). **M4**: mapped TB's opaque "tenant not registered" 404 → typed **409** `TeamBuilderTenantNotRegisteredError` (a `DependencyMissingError`, plain-string detail) at the `ProjectSolutionService.post` chokepoint (covers proposal + case-study + team gen); `mode="before"` validators coerce `tech_stack` null→[] on `CaseStudyGenerated`+`ProposalResourceAllocation`. **381 unit tests green, `ruff check app` clean.** Azure tasks 23672/23674 PR-linked. **Correction to the earlier note**: proposal *export* is NOT independent of TB — `export_proposal` calls `generate_proposal` (a TB call) every time, so it's blocked by the same TB-registration long pole; M4 only makes the failure legible, it does not register the tenant.
- **2026-07-21** — [[Interview assessment change history (US 22248)]] + [[Open Jobs 1-5 matching (US 23640)]] — **combined both features into ONE promotion PR per env so they release together**. Folded US 22248 (#1848) into the open-jobs cherry-pick PRs: appended its 5 commits `a0e330c8..4cae3e48` onto #1875 (qa)/#1876 (main) fast-forward (no force-push); each PR now carries both features. **Correction:** I first (mis)read the ask and opened *separate* interview PRs #1879/#1880 — the ask was to add the feature TO #1875/#1876; closed #1879/#1880 + deleted their branches. **Avoided the merge-commit `parent1..parent2` pollution trap** (that diff carried unrelated case_study deletions from a separate PR #1848's branch never rebased over) by cherry-picking `merge-base..^2`. Each PR is a self-contained single-head chain `q8vd3mzk7w2p → 3gufg5ykw01g (interview backfill) → ehjnwsqitqve (open-jobs wipe)` (verified `alembic heads`; `ehjnwsqitqve.down_revision` re-pointed onto `3gufg5ykw01g`; `51r81g9s8arp` skipped; **no cross-PR merge order**). **qa + PROD DBs both confirmed at `q8vd3mzk7w2p`** (read-only, per the user's connections). All 4 PR titles + descriptions updated. #1876 still DO-NOT-MERGE for prod (Open Jobs blockers: Data US 23610 + FE M2).
- **2026-07-21 (#1878 pr-review)** — [[Generic proposal template & case studies (US 23670)]] — **formal 3-agent `/pr-review` on #1878 + second-round test nits pushed (`70f6c661`)**: verdict **READY WITH NITS** — 0 code blockers, **CI green** (`test and lint` pass 9m14s), 138 unit tests pass locally; architecture 16/16 PASS, PRD 15/21 fully (PPTX→M2, R5 FE, R6 M4 correctly deferred), tests-security confirmed **cross-tenant isolation rests on the explicit `tenant_id` repo filter (not RLS — the testcontainers harness bypasses it)** so the surviving cross-tenant-404 tests are true-green, role/auth gates intact, no dangling refs to the removed `TenantFeature.CASE_STUDIES`/`CaseStudiesFeatureDisabledError`. Fixed 3 test-coverage nits: (1) Echo-logo fallback render test (masthead/cover/closing use bundled `echo_logo.png` when the tenant has no logo — R2), (2) no-rate em-dash assertion on the resource row, (3) logo-URL attribute-context escaping under the block-scoped autoescape. `test_proposal_graphics.py` 24 passed. Two non-code merge gates stand: **paired FE PR must ship together**, and **design-fidelity QA reconcile** vs the approved 10-slide deck (build couldn't fetch the claude.ai design — sections inferred).
- **2026-07-21 (later)** — [[Generic proposal template & case studies (US 23670)]] — **self-`/pr-review` on #1878 + nits fixed (`bef5b680`)**: verdict **READY WITH NITS** (0 blockers; PRD 10/12 satisfied + PPTX-flag PDF-first tradeoff + 2 cover/closing design-fidelity partials routed to QA; cross-tenant isolation, auth and role gates all verified intact after the tenant-gate removal). Fixed 3 nits: (1) dropped the vestigial tenant `SELECT` on the update/delete/regenerate/find-similar write paths (only `generate` needs the tenant); (2) **block-scoped `{% autoescape %}`** over the generic template body so TB/user free-text is HTML-escaped in the PDF — scoped locally because the shared Jinja env has no autoescape and other templates rely on `| markdown` raw HTML (a global flip would regress CV/JD/Navitec); (3) added tests for the M1 timeline-gate widening (service level), empty resource-table/tech-stack branches, all bill-rate shapes, and the autoescape hardening. export + case-study unit suites green, ruff clean. **Merge gate reaffirmed: hold #1878 → `dev` until the paired FE PR ships together.**
- **2026-07-21** — [[Generic proposal template & case studies (US 23670)]] — **M1 + M3 (PDF-first) → PR [#1878](https://github.com/taller-projects/echo-backend/pull/1878) open → `dev`**: new `generic_proposal.jinja` (Echo teal design, per-page masthead+footer, slide-6 case-study table); template-less tenants render it (replaces dead `proposal.jinja` fallback); removed `CASE_STUDIES` gate + export 404 guard, `has_custom_export_project` always-true; **kept `case_studies_ppt_export` gate → M2**. 137 export/case-study unit tests green, ruff clean, 9-page render verified. Added the PRD 'Backend Milestones' section. Azure US 23670 Active + tasks 23671/23673 PR-linked (task states rejected transitions). Deferred M2 (23672) + M4 (23674) + paired FE PR.
- **2026-07-21** — [[Open Jobs 1-5 matching (US 23640)]] — **promotion cherry-picks opened**: [#1875](https://github.com/taller-projects/echo-backend/pull/1875) → `qa` and [#1876](https://github.com/taller-projects/echo-backend/pull/1876) → `main`, each = the 11 feature commits of #1854 cherry-picked cleanly onto their base (**byte-identical to dev** for all 17 touched files) + one `chore` commit rebasing the wipe migration `ehjnwsqitqve`'s `down_revision` from `51r81g9s8arp` (dev-only, unpromoted — belongs to interview-assessment backfill `3gufg5ykw01g` + contact-index `51r81g9s8arp`) onto the current qa/main alembic head `q8vd3mzk7w2p`; `uv run alembic heads` = single head both branches, ruff clean. **#1875 mergeable; #1876 flagged DO-NOT-MERGE** (Data endpoint US 23610 still New → prod would 502/504, and the migration irreversibly wipes existing prod matches; FE M2 not built). Merge order qa→main, merge commits. Done via isolated scratch worktrees off `origin/qa` / `origin/main`.
- **2026-07-20 (10)** — [[Outbox skips internal-originated emits (Bug 23656)]] — **#1870 MERGED → `dev`** (merge commit `475a2201`) **+ promotion cherry-picks opened**: [#1871](https://github.com/taller-projects/echo-backend/pull/1871) → `qa` and [#1872](https://github.com/taller-projects/echo-backend/pull/1872) → `main`, each = the 3 feature commits (`92cdd22e`/`7781ef17`/`1782228f`) cherry-picked onto their base, clean auto-merge, **byte-identical to dev** for all 8 touched files, no migrations. Both MERGEABLE; merge with a **merge commit** (main after qa). Azure Bug 23656 linked. Branch convention `cherry-pick/<scope>_{qa,main}`, done via isolated scratch worktrees.
- **2026-07-20 (9)** — [[Outbox skips internal-originated emits (Bug 23656)]] — **full-loop e2e regression pushed (`1782228f`) on #1870**: new `tests/system/test_outbox_loop.py` runs the whole pipeline vs real Postgres with the dispatcher's HTTP mocked (no real TrackerRMS): FE-side create emits → dispatcher `_process_event` delivers (success + completed + `entity_external_links` row) → writeback PATCH re-enters via `/internal` → **no re-emit, nothing left to re-deliver** (pre-fix: pending `contact.updated` re-appears) + non-vacuity control (same update outside `/internal` emits). Gotcha found: `TenantScopedRepository` only auto-injects tenant inside `is_in_request_context()` — the FE leg must run in `background_request_context(ctx)` or the INSERT falls back to the hardcoded Taller `server_default` tenant. System suite → not run by CI; unit wiring tests remain the CI guard. Related outbox files re-run green (78 passed).
- **2026-07-20 (8)** — [[Outbox skips internal-originated emits (Bug 23656)]] — **/pr-review round 1 on #1870: READY WITH NITS (0 blockers, 6/6 ticket compliance, CI green) + all 4 nits fixed (`7781ef17`)**: (1) new `tests/unit/test_outbox_internal_surface.py` pins the suppression wiring — dependency membership on the internal root router + flag-setter test + end-to-end probe (internal contact create → 201, **no** outbox row); CI runs unit only, so the guard lives there; (2) writer tests on the autouse `cleanup_request_context` fixture; (3) `outbox_emit_skipped_internal` debug log so "never emitted (internal origin)" ≠ "emitted and lost"; (4) EventBus worker comment: `is_internal` doesn't propagate to handlers (none emit today). Review verified: single write point (`IntegrationOutbox` built only in the writer), 100% `/internal` coverage (single root router, `app/main.py:98`), writeback loop path confirmed via HTTP `/internal`.
- **2026-07-20 (7)** — [[Outbox skips internal-originated emits (Bug 23656)]] — **diagnosed the Navitec TrackerRMS sync loop + shipped the fix (PR [#1870](https://github.com/taller-projects/echo-backend/pull/1870) → `dev`)**. Root cause: outbox events written even for internal-originated changes → the sync writeback re-enters via `/internal`, re-emits, dispatcher poll (~70 min) repeats → loop. Prod evidence (read-only, tenant `2445fa20…`): 385k deliveries in 5 days, a role ping-ponging `Covered↔Archived` every ~70 min, 24 contacts' company flipping (or to null). Fix = dedicated `is_internal` flag set globally on the internal app router + guard in `write_outbox_event` (single write point, never the dispatcher). [Bug 23656](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23656) created + Active. 53 unit tests green, `app/` lint clean. **Layer-1 only** — change-detection/coalescing/company-semantics/stale-external-ids/alerts pending.
- **2026-07-20 (6)** — [[Interview scheduled email evaluate link (US 23179)]] — **Pedro APPROVED #1865**; his 2 non-blocking nits fixed (`266264d7`): (1) renamed the test module `test_interviewer_changed_email.py` → `test_interview_scheduled_email.py` + rescoped docstring (both handlers render the one template); (2) renamed the link "mixins" (they declare `interview_id`) to `InterviewLinkPayloadBase` / `InterviewEmailLinksPayloadBase` + docstrings. Autoescape XSS surface stays a pre-existing hardening follow-up. 71 unit tests green, `app/` lint clean.
- **2026-07-20 (5)** — [[Interview scheduled email evaluate link (US 23179)]] — **external review on #1865 addressed (`3aa501ed`)**: leoassontaller reviewed **READY WITH NITS** (0 blockers, CI green, ticket 4/4). Fixed the one valid nit — a regression test locking that the Slack-only `InterviewEventPayload` has **no** `evaluate_link` (the mixin split from `9191872e`). Other nit (`__new__` fixture) = awareness-only; pre-existing PII payload logging folded into the hardening follow-up. 12/12 in `test_interviewer_changed_email.py` green.
- **2026-07-20 (4)** — [[Interview scheduled email evaluate link (US 23179)]] — **`/pr-review` nit fixes pushed (`9191872e`) on #1865**: (1) DRY — the `link`/`evaluate_link` computed fields were duplicated across the interview payloads; collapsed into `InterviewLinkMixin` (`link`, shared by all) + `InterviewEmailLinksMixin` (adds `evaluate_link`, scheduled-email payloads only; Slack `InterviewEventPayload` stays link-only). (2) added `TestNotificationServiceInterviewScheduledHandler` driving `interview_scheduled(event)` end-to-end so the scheduled path's rendered evaluate link is asserted directly (was only covered transitively). 64+25 local tests green, `app/` lint clean. The review's two out-of-scope items (jinja autoescape off; unescaped `talent_name`/`role_title`) left for a separate hardening ticket.
- **2026-07-20 (4)** — [[Open Jobs 1-5 matching (US 23640)]] — **M1 backend MERGED → `dev`** (PR [#1854](https://github.com/taller-projects/echo-backend/pull/1854), merge `8e702ef8`): 11 commits incl. 2 review rounds + legacy-wipe migration `ehjnwsqitqve` (runs at dev deploy — existing lists empty out, rebuild as 1-5) + `TalentService.get_top_matches` refactor. Tasks 23641/42/43 Closed, US Active (real e2e vs Data endpoint US 23610 still New — merged against the mock), PRD §6/§8 updated. Next: Data staging validation, FE M2, feature QA.
- **2026-07-20 (3)** — [[Interview scheduled email evaluate link (US 23179)]] — **PR [#1865](https://github.com/taller-projects/echo-backend/pull/1865) → `dev` (open)**: new `evaluate_link` computed field on `InterviewScheduledPayload` + `InterviewerChangedPayload` → `/interviews/assessment/{interview_id}/evaluate`; the jinja gets a second "Evaluate Candidate" `.btn`; both email dispatch paths covered. FE route confirmed against echo-frontend (page already exists — no FE work). Tests extend the real-template render assertion + new `TestEvaluateLink`; 43 passed, lint clean. Worktree off `origin/dev` to dodge the concurrent open-jobs branch.
- **2026-07-20 (2)** — [[Open Jobs 1-5 matching (US 23640)]] — **review Q4 dissolved via refactor (`4b9ea905`)**: the job repo's cosine query joined `organization_job` only to reference `job.vector`; passing the vector *value* removes the cross-module coupling entirely → query moved to `TalentRepository/TalentService.get_top_matches`, job repo drops `_talent_model`/`_experience_model`. Q3 audited: nothing branches on 500-vs-504, body unchanged — strictly better; caveat = Grafana alerts keyed to exactly 500. Baseline-verified (identical local 404 quirk set), 5/5 system tests green. Same pattern remains in `ApplicationRepository` (Roles) — possible follow-up.
- **2026-07-20** — [[Open Jobs 1-5 matching (US 23640)]] — **round-2 follow-up pushed (`be34ce85` + `678b8225`)**: verified Pedro's review against the PR head and found `tests/unit/test_external_api_service.py` **claimed in `e2171b0e` but left untracked** → committed. Settled Q1: the legacy-cap starvation edge (new to this PR — pre-PR had no cap) is dissolved by **wiping legacy untagged `matched_talents` at rollout** — data-only migration `ehjnwsqitqve` (strips only untagged entries → idempotent; verified read-only vs dev DB: 100/288k jobs with entries, 0 non-array, 0 with ≥30 legacy). Supersedes PRD "no migration" + "legacy keeps current behavior". Q2: Spanish notification drafted for the Data team (view `view_smart_search_companies_open_jobs` exposes the tagged JSONB raw). Azure tickets + Notion PRD amendment done same session.
- **2026-07-20** — [[Untrack committed vault directory (PR 1860)]] — `echo-backend/vault/` (Obsidian notes) was accidentally committed via deploy-merge #1112 and stayed tracked despite being in `.gitignore` (ignore rules don't untrack already-committed files); Obsidian's `workspace.json` churn kept blocking `git pull`. Confirmed sole contributor across all refs = me. `git rm -r --cached vault/` (local files kept). [#1860](https://github.com/taller-projects/echo-backend/pull/1860) → `dev` **merged**; qa+main carried the identical tree so removed there too via isolated scratch worktrees → [#1861](https://github.com/taller-projects/echo-backend/pull/1861) → `qa` + [#1862](https://github.com/taller-projects/echo-backend/pull/1862) → `main`, both open (merge commits). Left `vault/Echo/` doc references (CLAUDE.md/skill/agents/code comments) untouched — flagged as a separate cleanup.
- **2026-07-17** — [[Open Jobs 1-5 matching (US 23640)]] — **CI red after round 2 → fixed (commit `6e7de88b`)**: nit-4 (matched_talents off the write schemas) made new jobs fall back to the model column default `"[]"` — a *string*, stored as a JSONB scalar → `DataError` in `highest_match_score`'s `jsonb_array_elements` (8 failed + 3 errors in `test_organization_jobs`; would've 500'd job reads in prod too). Fix: model default `"[]"` → `list` + a service-level regression guard. **Lesson logged:** the local TestClient-404 quirk *masked* this — I wrongly called it pre-existing off local results; "green→red in CI across the push" is the real baseline.
- **2026-07-17** — [[Open Jobs 1-5 matching (US 23640)]] — **review round 2 on #1854 fixed + pushed (commit `e2171b0e`)**: addressed Pedro's 4 blockers + nits. **B1** lazy vectorize leg now bounded (tight timeout, no retries) + normalized to 502/504 + new `OpenJobNotVectorizableError` 422; **B2** intra-batch dedup by `talent_id` + `OpenJobMatchResult` rejects `ok`-without-score (→502); **B3** per-call `log_body=False` keeps talent email/notes out of the shared client's DEBUG logs; **B4** PRD §5 full-flow system test (match→5 scored→grow to cap→no-op→404) driven through the service vs real Postgres (local TestClient 404s `/company/jobs/*`, a documented quirk; system tests never run in CI). Nits: `highest_match_score` float→int, `matched_talents` stripped from write schemas (read-only), private-method typing, stale docstrings. 16 new tests + 4/4 system tests green, lint clean; the local TestClient-404 + factory-scalar failures verified pre-existing (models.py untouched). Merge still gated on Data US 23610.
- **2026-07-17 (EOD sweep)** — Weekly ticket + vault reconciliation across the week's PRs: [[Case studies per-card management (US 23613)]] promotion cherry-picks [#1852](https://github.com/taller-projects/echo-backend/pull/1852) → qa + [#1853](https://github.com/taller-projects/echo-backend/pull/1853) → main **merged → in production** (status `shipped-prod`); [[Kforce Last Contacted By filter (PR 1846)]] [#1850](https://github.com/taller-projects/echo-backend/pull/1850)/[#1851](https://github.com/taller-projects/echo-backend/pull/1851) **merged**, Bugs [23638](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23638)+[23639](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23639) **Closed**; created [Bug 23648](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23648) for the ticketless [[Outbox flat payload fix (PR 1838)]] prod fix and Closed it; [[Open Jobs 1-5 matching (US 23640)]] tasks 23641/42/43 → **In development** + PR link (PR [#1854](https://github.com/taller-projects/echo-backend/pull/1854) still open).
- **2026-07-17** — [[Open Jobs 1-5 matching (US 23640)]] — **/pr-review #1854 (round 1) + fixes pushed (commit `614aa7ab`)**: PRD compliance 15/16, CI green, no scope creep. Fixed 1 BLOCKER — deprecated **internal** GET job routes leaked cross-tenant `matched_talents` (API-key tenant A could read tenant B's matches on the shared tenant-less row) → now routed through `get_job_for_tenant`/`get_jobs_for_tenant` via `_api_key_tenant_id` dep — plus nits: malformed Data 200 body → 502 (was 500), `repo.commit()` encapsulation, `tenant_id: | None` typing. **Also (user request):** split the talent payload's `skills` into separate `core_skills` + `additional_skills` lists (contract change for Data US 23610; PRD §8 updated). +1 unit test (malformed→502), assertions updated; 14 matching tests green, lint clean, failure set identical to baseline (no regressions).
- **2026-07-17** — [[Open Jobs 1-5 matching (US 23640)]] — **M1 backend implemented → PR [#1854](https://github.com/taller-projects/echo-backend/pull/1854) → `dev`** (open): sync client + per-call timeout/504 mapping (tasks 23641), `match_talents` re-impl to 1-5 + tenant-tagged JSONB + row-lock RMW + cap 30 + tenant-aware `highest_match_score` SQL + service read-filtering (23642), 502/504 error mapping + structured logs + unit/system tests (23643). 14 new unit tests + 2 system tests green, ruff clean; merge gated on Data US 23610. **Contract update (commits 2-4):** now sends the full open job + curated talent data inline (no ping-pong / no Data→Echo callback); path finalized `/candidate_matching/evaluate_open_job_match`; response gained `error_message`; talents also carry `notes` (internal) + `ats_notes` (via ATSNoteService batched) + `email` (reverses earlier no-PII stance, per request); PRD section 8 + tickets 23640/23610 updated. Earlier same day: feature resumed from [PRD](https://app.notion.com/p/39eaedca11f081ff95f4c0b20b6b3aab), tenancy risk confirmed, PRD §7 plan, Azure tickets created under Feature 23494 · [[Kforce Last Contacted By filter (PR 1846)]] — #1846 proved insufficient (pagination COUNT still timed out); round-2 fix [#1850](https://github.com/taller-projects/echo-backend/pull/1850) (nested EXISTS + `contact_last_interaction_id_idx`, >2min→201ms) + [Bug 23638](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23638) · Taller preventive port [#1851](https://github.com/taller-projects/echo-backend/pull/1851) ([Bug 23639](https://dev.azure.com/TallerInternTools/Echo%20Core/_workitems/edit/23639): dev count already at 11.8s of the 20s budget → 41ms; **/pr-review caught a latent alembic multi-head** — migration cut off `q8vd3mzk7w2p` but dev head advanced to `3gufg5ykw01g`, would 2-head dev on merge; fixed by rebase onto dev head + concurrent index build + tenant/test nits, commit `fc5876cf`) · [[Case studies per-card management (US 23613)]] — dev e2e vs real TB **PASSED** (all 6 endpoints; Navitec-only QA, company-name dedup nuance); US → Ready to Test; then QA/PROD promotion cherry-picks opened ([#1852](https://github.com/taller-projects/echo-backend/pull/1852) → qa, [#1853](https://github.com/taller-projects/echo-backend/pull/1853) → main, byte-identical to dev, no migrations) · [[Interview assessment change history (US 22248)]] — fixed PR #1848 CI (polyfactory FK pins) + backfill-migration ordering no-op; full review (0 blockers) + nit fixes pushed (jsonb-null NULLIF, marker-collision skip, repo-level scoping)
- **2026-07-16** — [[Case studies per-card management (US 23613)]] merged ([#1842](https://github.com/taller-projects/echo-backend/pull/1842) + [#1843](https://github.com/taller-projects/echo-backend/pull/1843)) · [[Kforce Last Contacted By filter (PR 1846)]] merged
- **2026-07-15** — [[Outbox flat payload fix (PR 1838)]] → dev/qa/main
- **2026-07-13/14** — [[Industry-agnostic Echo (PRD 398aedca)]] M1–M5 + /users/me merged; dev QA passed · [[Deep pagination selectin fix (23553)]] both envs · [[Project creation consumer_id 422 (Bug 23571)]]
- **2026-07-10** — [[Client Active significant activity (US 23536)]] both envs · [[Kforce Client Active no-job gate (Bug 23545)]] · [[Kforce contacts custom sorts (23546)]] · [[Inactive vendor notifications (US 23531)]]
- **2026-07-09** — [[Moved-away Client contacts Past (Bug 23519)]] → prod · [[ATS deep-links Open in ATS (US 23507)]] logo slice · [[Outbox poll backoff (Bug 23522)]]
- **2026-07-07/08** — [[Client Active gate relaxation (PR 1745)]] → prod · [[Alumni label removal (PR 1742)]] → prod · [[WeasyPrint 62 to 68 upgrade (US 23479)]] · [[Kforce relationship aggregates M6 (US 23424)]] · ATS deep-links M1+M2
- **2026-07-02/03** — [[Kforce Contact Relationships port (US 23370)]] M1–M4 merged · [[Kforce multilevel groups (US 23339)]] seed applied · [[Future-dated interactions fix (Bug 23383)]]
- **2026-07-01** — [[similar_role null-vector 500 (Bug 23362)]] hotfixed to prod
- **2026-06-25/30** — [[Generic Contact Relationships (US 23240)]] M1→DROP+hotfix · [[Contact bulk_track IntegrityError (Bug 23251)]] · [[UUID typing for id__in filters (23253)]]
- **2026-06-22/24** — [[Pentest 4.1.3 session-liveness (23014)]] · [[Pentest 4.1.5 no-store headers (23016)]] · [[Roles statement timeout fix (PR 1600)]] · [[Pending interview notifications (US 23321)]] step 1
- **2026-06-18/19** — [[External links writeback (US 23126)]] · [[Sentry observability rollout (US 22295)]] · [[Upload file-type validation (23013)]]

## 🗂 All deliveries

Browse `Deliveries/` (40 notes, 2026-06-17 → 2026-07-24), or start from a Map above.
